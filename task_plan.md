# 2026-08-07 对外失败原因统一展示

## 2026-09-01 群广告扫描艾特通知

### 目标
- 在锅巴后台以清楚的“广告扫描”入口管理群广告检测。
- 命中广告提醒阈值后，在群内真实艾特配置的通知管理员；全局和每群名单均可配置。

### 阶段
- [x] 阶段 1：审计现有群风控、后台 schema 和通知链路
- [x] 阶段 2：补齐可配置的群内艾特通知并优化后台文案
- [x] 阶段 3：定向回归、部署和生产运行态验证

### 2026-09-01 补充边界
- [x] 将通知名单在后台明确命名，并增加“只使用后台配置 QQ”的回归保护后重新部署。

## 2026-08-10 人设模板去重与模型路由范围修复

### 目标
- 移除 AI/机器人身份话题的固定示例锚点，避免希洛反复生成近似文案。
- 以生产模型配置、实际请求和路由决策为准，修复非必要对话落入昂贵主语言模型的范围错误。

### 阶段
- [x] 阶段 1：审计生产提示词、模型配置、路由决策和调用日志
- [x] 阶段 2：实现人设提示去模板化和模型路由边界修复
- [ ] 阶段 3：补定向回归、最小化部署并验证运行态

## 2026-08-11 长对话卡面 Markdown 代码渲染

### 目标
- 将长对话卡面中的 Markdown 围栏代码、行内代码和列表按语义渲染，避免 Java 等代码块被压成普通正文。

### 阶段
- [x] 阶段 1：审计 `TextImageTool` 的模板、Markdown 解析和现有渲染回归
- [x] 阶段 2：实现代码块及相关 Markdown 的卡面渲染
- [x] 阶段 3：用用户给出的业务错误码示例完成定向和视觉验证
- [x] 阶段 4：最小化部署、生产渲染回归与运行态验证

## 2026-08-11 YouTube 授权后台配置

### 目标
- 在 Guoba 后台提供可编辑、脱敏的 YouTube 登录 Cookie 与 PO Token 配置，并让自动搬运链路把已配置授权安全传给 `yt-dlp`。

### 阶段
- [x] 阶段 1：审计现有媒体配置、Guoba schema 与 yt-dlp 调用参数
- [x] 阶段 2：实现授权配置、运行时参数和脱敏边界
- [x] 阶段 3：补回归、最小部署并验证生产运行态

## 2026-08-12 叙事卡片前言与正文呈现诊断

### 结论
- 当前输出确实由代码刻意拆成两条：模型的短开场先作为文本消息发送，后续带标题正文再作为 document 卡面发送。
- 用户示例用了全角 `＃`、标题与正文同一行，未命中现有拆分规则，才导致整段进入卡面；应同时收紧模型格式提示并扩展解析兜底。

## 2026-08-12 卡面即时确认与完整正文

### 目标
- 对故事、知识解释和明确文档请求，在模型生成前单独发送简短确认；随后卡面只含完整的故事或解释正文，不再混入“我试试看”等过程性前言。

### 阶段
- [x] 阶段 1：确认可在请求前判定的卡面类别与现有叙事拆分边界
- [x] 阶段 2：实现即时确认、正文卡面契约与旧格式兼容
- [x] 阶段 3：补回归、最小部署并验证生产运行态

## 2026-08-11 YouTube 失败卡片信息修正

### 目标
- 去除 YouTube 自动搬运失败卡片的重复业务前缀；失败时不再伪装为“未命名视频”的正常分享，并以已确认的实际访问限制说明原因。

### 阶段
- [x] 阶段 1：定位重复拼接、失败格式化与生产原始下载器错误
- [x] 阶段 2：修正失败卡片文本并补定向回归
- [x] 阶段 3：最小部署并核验生产运行态

## 目标
让希洛及管理命令在真实执行失败时给出可行动、脱敏后的实际原因，而不是统一回复“失败”“请看日志”或“没有完成”。

## 阶段
- [x] 阶段 1：审计主聊天、工具失败和直接命令回复出口
- [x] 阶段 2：实现统一脱敏错误格式化器并接入主链
- [x] 阶段 3：接入高频工具与管理命令的直接失败出口（B 站、音乐、消息记录、表情包、投递及统一工具回传）
- [x] 阶段 4：定向回归、线上验证与部署

## 边界
- 只公开已确认的状态码、上游错误短语、超时和无可用渠道等可行动事实。
- 隐藏 API key、Bearer token、签名 URL 参数、磁链和内部堆栈；不把权限或用户输入错误伪装成上游故障。
- 强制工具调用失败不能由文本降级伪造执行结果。

## 2026-08-07 上游过载话术补充
- [x] 阶段 1：将无状态码的上游过载错误归类并本地化
- [x] 阶段 2：补回归、上线和运行态验证

## 2026-08-07 人格自主性与关系边界
- [x] 阶段 1：定位人格偏好未进入主提示、关系指令缺少自主边界
- [x] 阶段 2：接入人格偏好/边界、补回归、核对线上覆盖配置并上线

## 2026-08-08 骰娘安全随机源统一
- [x] 阶段 1：核对群 `953676639` 实际骰点、执行日志与缓存/固定种子路径
- [x] 阶段 2：以加密随机源替换普通骰和表达式后备随机源，保留规则包独立审计种子
- [x] 阶段 3：补回归、上线并验证运行态

# 2026-08-05 统一群记忆语义提交链路

## 目标
消除群别名、群知识和群工作流各自用正则直接写入 Redis 的分叉链路。所有共享持久状态先由模型判断意图并拆成原子提案，再由统一提交层完成身份、权限、冲突、来源和删除处理；故事与临时设定只留在本轮上下文。

## 阶段
- [x] 阶段 1：审计全部写入、注入、删除与失败路径
- [x] 阶段 2：定义并实现统一语义提案与提交数据契约
- [x] 阶段 3：替换别名、群知识、群工作流的正则直写入口
- [x] 阶段 4：实现统一提交网关、来源关联删除、冲突策略和非阻塞失败状态
- [x] 阶段 5：补全回归、线上部署与运行态验证

## 约束
- 模型决定是否为长期状态、类型和原子拆分；代码不再以关键词判断写入意图。
- 代码只校验成员/文件真实性、写入权限、来源、冲突和幂等。
- 裁决失败不能阻塞正常聊天，也不能让希洛谎称“记住了”。
- 保留用户自己的个人记忆与群内显式协作规则能力，但共享状态不能被普通成员静默覆盖。
- 统一提交在每个群的既有串行队列内一次读取和写入 aliases、knowledge、workflows；禁止在其内部再进入同一队列。
- 工作流属于未来执行权限，只允许 owner/admin/master 建立或覆盖；普通成员仍可新增其自己的静态定义。

## 当前验证
- 本地定向回归 53/53，通过共享语义裁决、群知识、工作流、后台提取和别名存储的组合检查。
- 本地最终验证：定向回归 72/72 通过，目标模块 `node --check` 与 `git diff --check` 通过。广测 718 项中 669 通过、45 跳过、4 失败；本次新增的忘记工具断言已修复，余下失败为既有 `moment`/TRSS/Puppeteer 缺失及归档时序问题。
- 已部署生产：仅同步 `apps/test.js` 的群记忆入口区块及 5 个记忆相关运行文件；备份位于 `/opt/trss-yunzai-backups/shiloh-plugin-group-memory-20260805-180000`。生产目录回归 59/59，目标文件 SHA-256 与 staging 一致；服务重启后 `trss-yunzai.service=active`，PID 880762，MessagePipeline 持续收到事件且未见 `SyntaxError`、`ReferenceError` 或模块加载错误。

# 2026-08-03 科普知识卡版式与结构化修复

## 2026-08-04 知识解释执行路由治理

### 目标
让“能力可见”与“本轮必须调用工具模型”解耦：无工具动作的知识解释走聊天后端，保留知识卡；表情、搜索、媒体和权限动作只在确有执行意图时进入工具路由。

### 阶段
- [x] 阶段 1：用两条线上 MC 请求定位模型、卡面和发送耗时
- [x] 阶段 2：定义独立的执行路由策略及保留边界
- [x] 阶段 3：接入初始请求、补回归与观测字段
- [x] 阶段 4：线上基准验证与部署

### 验证记录
- 本地定向回归 25 通过、2 个 Chromium 截图用例因本机缺浏览器跳过；线上从 TRSS 根目录运行路由 mock、知识卡截图和策略测试均通过。
- 已部署最小补丁：`replyExecutionRoute.js`、`TextImageTool.js`、`apps/test.js` 路由入口及测试；备份位于 `/opt/trss-yunzai-backups/shiloh-plugin-execution-route-20260804-150000`。
- 服务重启后 active，`shiloh-plugin`、MessagePipeline 正常初始化，加载 34 个插件。下一条真实知识解释将用 `route=chat` 和 `textImageTool timing` 日志验收端到端加速。

## 目标
让群内科普与技术入门保留图片卡面，并根据题目给出明确选择、前置条件、实现边界与验证路径。卡面必须具备可扫描的知识结构，不能只是把长文本截图；也不得以回复字数作为是否转卡的依据。

## 阶段
- [x] 阶段 1：读取群 609235590 的真实请求、模型原文和发送链路
- [x] 阶段 2：定位自动长图和缺少科普结构提示两个根因
- [x] 阶段 3：错误地收紧为文本呈现策略，已由用户纠正，必须撤回
- [x] 阶段 4：实现并接入专用知识卡模板
- [x] 阶段 5：补齐路由/渲染回归验证并部署

## 约束
- 用户明确要求纯文本、Markdown 原文或代码原文时，不强制转图。
- 科普与技术入门按请求语义走知识卡，不按内容长度判断。

## 验证记录
- 纠正后本地和线上定向回归均通过：`solutionExplanationStyle` 5/5，`textImageDocument` 3/3；线上 Chromium 实际生成 `1960 x 2106` 的 knowledge PNG。
- 线上仅同步 `TextImageTool.js`、`solutionExplanationStyle.js`、最小 `apps/test.js` 路由补丁和相应测试。备份：`/opt/trss-yunzai-backups/shiloh-plugin-knowledge-card-20260803-213000`。
- 清理了阻碍加载的 7 个 AppleDouble 元数据文件并移入同一备份目录。重启后服务 active，`shiloh-plugin` 初始化、加载 34 个插件，后续启动日志无 `SyntaxError`。

# 2026-08-03 单数群主艾特路由修复

## 目标
修复“艾特群主”被错误扩展为全体管理员艾特的问题；群主必须定位为当前群唯一 owner，管理员集合仅在用户明确使用集合措辞时才允许。

## 阶段
- [x] 阶段 1：复核生产调用证据并定位语义分类误选
- [x] 阶段 2：加入精确 owner 快路与三层管理员集合防护
- [x] 阶段 3：本地/线上回归、部署并验证运行态

## 约束
- 不修改现有全体管理员通知的能力；仅收紧触发条件。
- 单数群主请求不得退化为管理员集合，也不得在无法唯一定位 owner 时猜测。

## 验证记录
- 本地与线上定向回归均为 23/23；线上 `node --check` 通过。
- 初次整体同步本地 `apps/test.js` 时带入尚未同步的本地依赖，线上缺 `utils/solutionExplanationStyle.js` 而未加载插件；已立即从本次备份还原，确认 34 插件恢复后，改为只对线上版本应用最小补丁。
- 最终重启后服务 active，PID 2096627，`shiloh-plugin` 初始化一次、MessagePipeline 启动、共加载 34 个插件。

# 2026-08-03 磁链元数据真实解析路径

## 目标
针对用户明确提供且确认可用的 BTIH 磁链，找出并验证一条当前可用的元数据解析路径，再接回工具；未拿到元数据前不下载内容。

## 阶段
- [x] 阶段 1：确认当前一次性发现链路与该磁链的真实失败位置
- [x] 阶段 2：实测多种元数据来源并验证返回种子的 info hash
- [x] 阶段 3：用有效路径替换无效的持续重试设计并接回工具
- [x] 阶段 4：本地/线上验证目标磁链的实际解析结果

## 约束
- 仅处理用户当前明确提供的磁链；元数据通过前不得下载内容。
- 不做无限后台重试；只保留一次任务内的多路径尝试和最终结果。
- 已删除误创建的目标 Redis 持久任务，确认 key 不存在。
- 已确认 HTTPS 直连缓存可返回并验证该目标种子；缓存优先、P2P 后备已上线。

# 2026-08-03 磁链按文件选择下载

## 目标
在磁链元数据识别完成后，让发起人仅下载选中的文件；所有选择均受 50 MB 总大小、单文件和 ZIP 上传限制约束。

## 阶段
- [x] 阶段 1：复核现有可信元数据、下载及自动触发链路
- [x] 阶段 2：实现短期选择会话、文件索引和 aria2 定向下载
- [x] 阶段 3：接入后续自然语言选择路由及用户输出
- [x] 阶段 4：补回归、部署并以线上服务验证

## 约束
- 选择会话按群和发起人隔离，30 分钟过期；不存储文件内容。
- 真正下载前重新读取并验证种子 info hash，且重新做所有 50 MB 校验。
- 无待选会话、选择歧义或已过期时不猜测、不开始下载。

## 验证记录
- 定向测试 `node --test tests/torrentDownload.test.js tests/toolIntentManifests.test.js`：35/35 通过。
- `npm test`：647 通过、44 因本机缺既有 TRSS/Chromium 依赖跳过；2 项失败均不涉及磁链修改：`messageStorageIdempotence` 的临时归档文件缺失、`umaRaceBalance` 缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`。
- 已部署到 `/opt/trss-yunzai/plugins/shiloh-plugin`；备份位于 `/opt/trss-yunzai-backups/shiloh-plugin-torrent-selection-20260803-191809`。服务安全重启后 active、PID 1987584、加载 34 个插件；线上无发送运行探针通过。

# 2026-07-29 群 821466122 会话链路修复

# 2026-08-03 群 609235590 合并转发可读性核查

# 2026-08-03 合并转发持久化与解释风格提取

## 目标
让 forward-only 群消息不再被预筛丢弃，并在消息管道中展开、持久化为明确的转发上下文；上线后重放群 `609235590` 的最新事件，读取其中解释风格并转为希洛可用的抽象表达策略。

## 阶段
- [x] 阶段 1：设计并实现预筛、消息管道、近期上下文和归档的统一转发上下文
- [x] 阶段 2：本地定向回归和语法检查
- [ ] 阶段 3：线上部署、运行级回归和旧事件安全重放
- [ ] 阶段 4：提取解释风格并接入希洛表达策略

## 错误记录
- 线上上传包后首次 SSH 执行遇到密码认证临时限流；尚未解压替换生产代码，等待恢复后从已上传包继续。

## 目标
核对群 `609235590` 最新合并转发的实际内容是否已被机器人解析和持久化；本轮只读取证，不修改线上链路。

## 阶段
- [x] 阶段 1：定位最新原始事件、Redis 持久条目和群消息归档
- [x] 阶段 2：核对转发节点读取能力与预筛顺序
- [x] 阶段 3：给出可验证结论；等待用户是否要求修复

## 结论
- 最新事件时间为 2026-08-03 14:30:55，转发 ID 为 `7669689123275068986`。原始消息、Redis 和归档均只含 `CQ:forward`，没有节点内容。
- `groupContextResolver.collectForwardContext()` 本身可通过 `group.getForwardMsg()` 展开转发，但本消息在此前的 `empty_content` 预筛被跳过，解析器没有执行，因此内容没有被保留，无法从现有机器人存储事后恢复。

# 2026-07-29 工具等待回复去机器人化

## 目标
修复绘图、改图、识图等慢工具在最终结果前发出“正在处理图片”式冷漠状态播报的问题。保留不编造结果、不过度亲密和不打断实际任务的边界；不以固定卖萌话术替代模型判断。

## 阶段
- [x] 阶段 1：定位正常聊天与工具等待回复的人设链路差异
- [x] 阶段 2：收紧状态播报过滤，并补齐紧凑人设提示与出口守卫
- [x] 阶段 3：补回归并运行定向验证
- [x] 阶段 4：已备份、部署并安全重启；线上定向回归与运行态验收通过

## 验证记录
- 本地与线上 `contextualProgressReply`、`toolResultGrounding` 定向回归均为 18/18；目标模块 `node --check` 和 `git diff --check` 均通过。
- 改图/识图运行级用例在本地因缺少 Yunzai 宿主依赖而跳过；线上本轮先跑不依赖该宿主的精确回归，再以真实服务启动日志、目标哈希和 OneBotv11 重连验收。
- 线上备份：`/opt/trss-yunzai-backups/shiloh-plugin-tone-progress-20260729-232330`。安全重启后 PID=`3056052`，服务 active，插件仅初始化一次、加载 34 个插件，OneBotv11 于 23:24:51 重新连接。

## 目标
修复已在线上群 `821466122` 复现的文生图误走编辑、绘图任务重复恢复、引用任务被闲聊过滤遮蔽、成员展示名泄漏和亲密化语气问题；补定向回归，完成后再决定是否部署。

## 阶段
- [x] 阶段 1：复核线上证据和现有实现边界
- [x] 阶段 2：修复图片任务类型与 durable queue 互斥
- [x] 阶段 3：修复引用任务路由、成员显示名和 persona guard
- [x] 阶段 4：补回归、运行定向测试和静态检查
- [x] 阶段 5：部署并以线上日志验证

# 2026-07-29 群 981339693 多图识别超时修复

## 目标
修复引用合并转发图片识别在单一视觉模型超时时完全失败，以及失败话术串入旧话题；保留图片来源顺序和不编造内容边界。

## 阶段
- [x] 阶段 1：从生产日志定位请求、素材数量和第一失败
- [x] 阶段 2：核对视觉候选配置与多图降级策略
- [x] 阶段 3：实现可恢复识图与失败话术隔离并补回归
- [x] 阶段 4：生产验证、部署和运行态验收

# 2026-07-29 群 821466122 异常会话审计

## 目标
以生产群消息、出站回执和服务日志还原希洛在群 `821466122` 的错误回复；逐条区分路由、工具、模型、上下文和发送层问题。当前仅诊断，未经用户明确要求不修改、部署或重启。

## 阶段
- [x] 阶段 1：恢复既有部署与排查上下文，确认生产目录和取证边界
- [x] 阶段 2：提取近期群消息、希洛出站和错误日志并建立时间线
- [x] 阶段 3：逐条关联调用链，确定根因、影响范围和复现条件
- [x] 阶段 4：给出按严重度排序的修复建议与验证方案

## 错误记录
- 无生产读取错误；SSH 与 Redis 只读取证均成功。

# 2026-07-20 自然语言遗忘群知识

# 2026-07-20 Embedding 语义表达召回

## 目标
利用已配置的 Embedding 服务，在回复前按当前话题召回匿名化的表达节奏/句式策略，让希洛更自然；不得注入或复述任何群友原话，也不得把向量写入或查询放到正常回复的串行关键路径。

## 阶段
- [x] 阶段 1：核对全局表达学习、现有 Embedding 配置和主提示词注入点
- [x] 阶段 2：实现后台样本向量化、匿名模式提取、短超时查询缓存与召回
- [x] 阶段 3：接入主回复、补配置和 Guoba 控制项
- [x] 阶段 4：补定向回归、线上部署与运行态验证

## 验证记录
- 本地及线上定向回归均为 7/7：相关样本只输出匿名模式、低相似度不命中、查询失败静默降级、重复入队合并、旧脱敏样本后台回填，以及既有多消息节奏不回归。
- 线上 `embeddingAiConfig` 使用既有 `BAAI/bge-m3`；健康探针返回 HTTP 200 和 1024 维向量。未打印密钥、用户内容或向量值。
- 已备份 `/opt/trss-yunzai-backups/shiloh-plugin-semantic-style-20260720-230647.tar.gz`，安全重启后 PID=675779、34 个插件加载完成；下一条真实群消息会启动低并发旧样本回填。

# 2026-07-21 语义表达质量与可靠性升级

## 目标
把已上线的 Embedding 风格召回从“原消息主题相近”升级为“匿名对话场景相近”，接入主人明确的风格纠正，并补齐可观测性、置信度/冲突控制与可恢复的历史回填；不得把群友原文、反馈原文或机器人回复原文放进语义索引或提示词。

## 阶段
- [x] 阶段 1：确认线上索引已满 240 条、仅有 6 个模式，且当前没有语义分段指标
- [x] 阶段 2：实现匿名场景向量、单策略置信度选择、指标和历史索引迁移
- [x] 阶段 3：接入主人明确反馈、回填重试、配置与管理命令状态
- [x] 阶段 4：补场景/反馈/冲突/失败回归，线上部署与实测

## 验证记录
- 本地/线上定向回归均 17/17，通过场景输入不含原文、场景索引不含原文、歧义静默、主人反馈权重、回填失败重试、多消息节奏和既有 persona guard。
- 主回复最多等待语义 prompt 350ms；超出后回复继续，后台调用仍会完成并填充缓存。Embedding 请求本身保留 1200ms保护。
- 已备份 `/opt/trss-yunzai-backups/shiloh-plugin-semantic-scene-20260721-011236.tar.gz`；安全重启后 PID=704266、34 插件加载完成。迁移为惰性触发：线上索引仍是旧 schema 0/240 条，等待下一条真实消息执行安全重建，未向群发送测试消息。

# 2026-07-21 自主风格进化闭环

## 目标
让希洛在不需要主人每次手动纳入的前提下，基于多轮匿名互动结果自行采纳或撤销场景化回复策略；主人明确反馈仍是最高权重，但普通群聊只能形成低风险候选，必须跨轮、跨人、达到正反馈比例后才影响回复。

## 阶段
- [x] 阶段 1：确认现有场景策略只来源于静态观察，尚未关联“回复后互动结果”
- [x] 阶段 2：实现匿名回复轮次、正负结果判定、候选统计与自动晋升/降级
- [x] 阶段 3：接入机器人回复发送点、场景 prompt、配置和状态报告
- [x] 阶段 4：补多轮/跨人/负反馈撤销/隐私/回归，线上部署验证

## 验证记录
- 本地与线上均 18/18：新增用例覆盖跨两名用户三次认可自动晋升、四次“还是不行”撤销、状态不含请求/回复/认可原文；原有场景、反馈、回填和 persona guard 均未回归。
- 已备份 `/opt/trss-yunzai-backups/shiloh-plugin-auto-evolution-20260721-012250.tar.gz`，安全重启后 PID=706932、34 插件加载完成。线上配置插入时发现已有同值块而出现重复键，已立即压缩为单一块后再执行回归。

## 约束
- 不保存群聊原文、机器人原文、QQ 号或群号到自主学习状态；只保存哈希身份、场景类别、回复风格类别、计数和时间。
- 只有明确的后续认可/纠正/重复失败信号才算证据；沉默、普通换话题和随机表情不得算正反馈。
- 自动候选至少 6 次证据、2 个不同发言者、75% 正反馈才晋升；活跃策略出现足够反例自动撤销。

## 约束
- 向量服务输入只能是固定类别组成的匿名场景描述，不能是用户原话、昵称、群号或机器人回复。
- `.希洛反馈` 仍只接受主人明确操作；只保存分类标签和抽象策略，不能自动把普通抱怨当长期风格训练。
- 不确定、冲突、缓存失败或 Embedding 不可用时，宁可不注入场景策略，保留既有全局规则和 persona guard。

## 约束
- Embedding 仅选择已脱敏样本的匿名表达模式；最终 prompt 绝不包含原文、昵称、群号或可识别内容。
- 观察侧在后台排队；查询侧与既有异步上下文并行，失败/超时仅降级为原有全局表达规则。
- 只使用 `embeddingAiConfig` 明确配置的端点、模型和密钥，不虚构备用通道。

# 2026-07-20 Agent 证据与执行基础设施

## 目标
把希洛的工具调用收敛为可验证的证据链：结构化工具结果要保留成功/失败类别和安全错误摘要；最终回复按实际工具类型表达失败；图片识别要避免同图重复处理、记录 HTTP/下载/空响应第一失败原因，并支持受配置约束的同能力备用通道。现有群知识、工作流和任务状态作为可回答/可执行上下文继续复用。

## 阶段
- [x] 阶段 1：梳理当前工具结果序列化、事实边界、图片识别与任务状态入口
- [x] 阶段 2：实现通用 ToolOutcome 解析、工具特定不可用回复和证据日志
- [x] 阶段 3：实现图片 URL 去重、超时、上游错误保留与受配置备用通道
- [x] 阶段 4：补工具结果、图片成功/失败/备用、引用同图和回复文案回归
- [x] 阶段 5：完整依赖验证、部署和真实群验收

## 验证记录
- `toolResultGrounding` 兼容现有文本/JSON 结果并识别 `tool_outcome`；图像工具错误现在按链接过期、下载失败、超时和上游无结果输出不同自然文案，禁止按 Steam 常识补全。
- 图片识别对相同 QQ `fileid` 去重；主/备用候选仅从 `analysisAiConfig.providers` 显式读取，每次共享原图与原 prompt。默认超时为 25 秒，日志只保留 provider/model、失败类别和脱敏摘要。
- 本地定向 15/15；线上 tool grounding 5/5 和结构化超时文案探针通过。安全重启后 PID=642574、34 插件、OneBotv11 已建立；等待下一条真实图片诊断确认第一失败类别。

## 约束
- 模型不能从错误摘要或历史补造诊断结论；没有有效证据时只能明确尚未识别到。
- 图片备用通道必须使用同一批原图与原始用户问题，不能改写 prompt；没有显式候选配置时不虚构回退。
- 错误日志不输出 URL 查询参数、密钥、图片 base64 或用户私密内容。

---

# 2026-07-20 对话延迟优化

## 目标
降低希洛普通对话、工具请求和慢媒体任务的可感知等待：避免无必要的额外模型分类、为短闲聊收紧上下文，并把主回复与工具阶段的真实耗时记录下来；保留复杂任务的工具规划、上下文理解和媒体可靠性。

## 阶段
- [x] 阶段 1：以生产日志量化分类、上下文、工具、媒体和服务器资源耗时
- [x] 阶段 2：收紧语义工具分类的开放条件，并对短闲聊使用较小历史预算
- [x] 阶段 3：补全端到端耗时日志与慢任务即时反馈边界
- [x] 阶段 4：本地/线上回归、部署并用真实消息确认延迟分段

## 验证记录
- 点名合并已从 3000ms 热更新为 1000ms；短闲聊历史预算为 recent=6/relevant=2/max=8，复杂、媒体、引用和指代请求仍保留完整预算。
- 语义 Planner 只在媒体、明确工具/搜索/实时请求或已命中 Agent Skill 时运行，普通被点名聊天不再产生额外模型调用；独立策略回归覆盖这两侧。
- 初始主模型请求新增 `[模型耗时]` 日志，包含群号、工具选择、历史数量、compact/full 模式和耗时；已有总对话、分类、工具和媒体日志共同构成完整分段。
- 搜索工具超过 2.5 秒会发送自然进度反馈，20 秒无结果则超时失败；线上模拟通过。线上目标回归 22/22，安全重启后 PID=636201、34 插件、OneBotv11 正常。

## 约束
- 不移除复杂请求的 Planner、确定性工具或跨消息指代理解。
- 不把慢上游错误伪装为“已经完成”；图片、视频和搜索仍应如实完成或失败。
- 只减少不产生用户价值的等待，媒体发送的可靠性与顺序不能回退。

---

## 目标
让希洛能理解并执行“忘掉/删除某个记忆”的明确请求，精确删除当前群匹配的结构化群知识，并如实说明删除结果；不能因普通聊天或含糊表达误删记忆。

## 阶段
- [x] 阶段 1：梳理群知识删除 API、命令入口与 Agent 工具过滤链路
- [x] 阶段 2：实现受当前群和明确用户请求约束的自然语言遗忘工具
- [x] 阶段 3：补精确匹配、多条歧义、跨群隔离和正常聊天不删除回归
- [x] 阶段 4：以线上完整依赖验证并部署（如需重载则遵守当前服务状态）

## 验证记录
- 本地定向 19/19、线上同一组 19/19 通过；覆盖自然语序解析、本人/跨群隔离、歧义不删、普通“忘记”不触发和直接回复策略。
- 线上完整依赖成功注册 `forgetGroupKnowledgeTool`；裸 Node 注册探针首次缺 TRSS 全局 `logger`，补最小运行时注入后通过，未影响服务。
- 已备份 `/opt/trss-yunzai-backups/shiloh-plugin-forget-group-knowledge-20260720-175343.tar.gz` 并部署；安全重启后 PID=602270、service active、加载 34 个插件、OneBotv11 WebSocket 已建立。

## 约束
- 只删除结构化群知识；不把“忘记”误解成删除聊天归档、群文件或其他 Redis 数据。
- 当前请求必须含明确删除意图和可识别的记忆目标；多条候选时先列出候选，不擅自选择。
- 只能操作当前群；删除结果必须基于工具实际返回，不可编造“已忘记”。

---

# Grok 图片协议兼容计划

# 群工作流记忆与定向艾特计划

# 结构化群知识记忆计划

## 目标
让希洛能明确记住按群归属的成员定义、成员集合、群文件及其自定义关系。例如“群文件里的 A.zip 是我做的地图”保存为文件名、拥有者 QQ 和“我的地图”称呼；之后问“我群里的地图是什么”应回答 A.zip，而不依赖普通事实召回碰巧命中。

## 阶段
- [x] 阶段 1：核对群文件消息结构、现有显式教学和上下文注入边界
- [x] 阶段 2：实现通用群知识条目存储、覆盖、查询和删除
- [x] 阶段 3：接入成员/成员组和群文件的明确教学，按当前请求生成高优先级回答上下文
- [x] 阶段 4：补文件所有权、成员集合、自然提问、跨群隔离、覆盖/删除和普通聊天不误记测试
- [ ] 阶段 5：线上完整依赖验证、部署和真实群验收（部署和定向验证完成，等待真实群验收）

## 约束
- 只保存明确教学或清晰定义，不从普通聊天推断“谁做了什么”。
- 资源名称、文件 ID/URL（若消息段可得）、成员 QQ 和显示名分开保存，避免只记一段脆弱文字。
- 新知识是可回答事实，不是可执行指令；不得触发 @ 或其他动作。
- 同一群隔离，同一称呼/拥有者关系的新教学覆盖旧条目。

## 验证记录
- 本地群知识、工作流和记忆回归 34/34；生产完整依赖定向回归 40/40。
- 生产备份：`/opt/trss-yunzai-backups/shiloh-plugin-group-knowledge-20260720-171905.tar.gz`。同步使用 `COPYFILE_DISABLE=1` 和 `tar --no-xattrs`，确认未带入 AppleDouble 文件。
- 安全重启后 PID=593388、service active、OneBotv11 已连接；`maxGroupKnowledge=100`、`knowledgePromptMaxEntries=8` 已生效，启动日志无模块或语法错误。

## 目标
让群成员能明确教会希洛群内工作流，例如“有人要挂团时找 @甲 和 @乙”；后续在同一个群中提出“有人要挂团，帮我艾特”时，Agent 能拿到可执行规则并用真实 QQ @ 提醒目标成员。规则不能混入普通长期记忆，也不能靠每个场景新增关键词分支。

## 阶段
- [x] 阶段 1：核对长期记忆、成员解析、Agent 上下文与 @ 发送的当前真实链路
- [x] 阶段 2：设计并实现 Redis 持久化的群工作流规则、明确教学/更新/删除/查询入口
- [x] 阶段 3：把当前请求匹配到的规则作为可执行上下文交给 Agent，并提供精确成员 @ 原语
- [x] 阶段 4：补教学、自然调用、跨群隔离、无明确执行请求、成员失效和更新删除回归
- [ ] 阶段 5：完整依赖验证、部署并以真实群消息验收（部署与定向验证完成，等待真实群验收）

## 约束
- 只有表达明确教学意图的群成员话语才可创建或修改工作流规则。
- 规则按群隔离，目标持久化为 QQ 号和显示信息，不只存名称。
- 当前消息必须包含清晰的执行请求才触发 @；单纯讨论“有人要挂团”不得自动刷屏。
- 工作流规则是高优先级的可执行上下文；普通群记忆仍保持“仅用于理解语境”。
- 复用通用“@ 指定成员”能力，不为“挂团”等单一事件固化专用工具。

## 验证记录
- 本地与线上定向回归均 39/39 通过，覆盖解析、更新、跨群隔离、无执行请求、清除、通用成员艾特及现有记忆/工具回归。
- 生产已备份并部署；安全重启后 PID=589778、service active、OneBotv11 已连接，注册表确认 `mentionMembersTool` 可用。
- 首次同步带入了 macOS AppleDouble `._*` 伴生文件，插件加载器误将 `._test.js` 当插件而报语法错；已删除全部 11 个伴生文件并二次安全重启，当前启动日志无此错误。
- 本地完整 `npm test` 失败仅因本机缺 `exceljs` 与 `js-yaml`；线上完整跑受现有图片/API 测试对生产运行态依赖影响而失败，不能代替已通过的定向完整依赖回归。


## 目标
兼容 Krill Grok 图片接口的教程契约：文生图使用 `grok-imagine-image`，图像编辑使用独立的 `grok-imagine-image-edit`；先修复可被教程完整证明的文生图 JSON 请求，不为缺失参考图字段的编辑示例臆造协议。

## 阶段
- [x] 阶段 1：用线上配置复现教程精确请求并确认响应字段
- [x] 阶段 2：实现 Grok 文生图 payload profile 与配置兼容
- [x] 阶段 3：补请求体、fallback、普通 provider 不回归测试
- [x] 阶段 4：线上部署、真实 Grok 生成验证与安全重启

## 约束
- 不输出 API Key 或图片 base64。
- 用户指定 Grok 时仍禁止跨名称 fallback。
- 只根据教程确认的字段实现；图像编辑没有参考图传参示例时不猜 multipart/JSON 字段。

## 错误记录
- 使用线上 Grok Key 精确复现教程 payload（model/prompt/size/quality，无 n/response_format）仍返回 `400 Invalid request format`；随后 `/models` 仅返回 `grok-4.5`，没有两个图片模型，确认当前 Key/渠道组本身未暴露教程模型，无法靠代码兼容使当前配置成功。
- 本轮检查 BananaTool 错误分支时，一次 `rg` 命令因 shell 双引号未闭合而失败；改为单引号模式后完成，只读检查未修改文件。
- 首次部署后的组合核验命令被本地 zsh 在 `set -u` 下提前展开远端变量，报 `apple: parameter not set`；代码与备份此前已经完成，随后拆成单引号远端命令重新执行测试与任务检查，结果正常。

---

# B站视频聊天记录富化计划

## 目标
识别群 `609235590` 中刚发送的 B 站视频消息，搬运其标题、作者、BV/AV、页面链接、封面和视频资源标识到新的结构化聊天记录，使后续群聊上下文可以准确引用该视频；不总结或分析视频内容。

## 阶段
- [x] 阶段 1：从线上日志和消息存档确认真实 OneBot 消息段及当前入库结果
- [x] 阶段 2：梳理 MessageArchive/MessageManager 的结构化记录与上下文渲染链路
- [x] 阶段 3：实现共享 B站卡片解析、元数据补全与安全缓存/超时边界
- [x] 阶段 4：补 JSON 卡片、链接、短链、失败降级、封面和视频信息回归测试
- [x] 阶段 5：部署、线上真实消息回放、安全重启和记录验证
- [x] 阶段 6：聊天记录查询时附带封面；总时长不超过 30 分钟才现场解析并附带视频本体，超长视频明确省略本体
- [x] 阶段 7：视频本体请求最低可用分辨率，优先 qn=6；不提高码率或清晰度
- [x] 阶段 8：收到 B站卡片后自动把信息、封面和符合时长规则的视频本体回复到群里；静默入库不再是唯一可见结果

---

# 抖音分享链接解析与搬运计划

## 目标
识别群聊文本中的抖音分享短链，提取经公开分享页可验证的作品信息、封面、互动数据和播放资源，并复用现有单条合并转发与 30 分钟视频本体规则。

## 阶段
- [x] 阶段 1：用群 609235590 的真实短链验证重定向、页面数据位置和字段可用性
- [x] 阶段 2：实现短链识别、重定向解析、结构化归档和上下文渲染
- [x] 阶段 3：实现封面/MP4 合并转发、超长跳过和临时文件安全处理
- [x] 阶段 4：补真实页面夹具与失败降级回归，部署、重启并验证

## 约束
- 只读取公开分享页与其中的 JSON 数据；不执行页面脚本、不调用非公开签名接口。
- 原始分享 URL、作品 ID 和元数据可归档；有时效的封面/播放 URL 只作即时搬运，不作为长期可靠资源。
- 30 分钟以内才下载并发送 MP4；超过时长只放信息、封面和页面 URL。
- 所有输出收进一条合并转发，避免刷屏；视频节点使用 base64 数据，不能把本地 tmp 路径交给 OneBot。

## 错误记录
- 首次抖音部署后重启发现线上缺少此前本地新增的 `utils/messageCacheTtl.js`，使依赖 MessageManager 的两个插件加载失败；立即补传该辅助文件并二次安全重启，新进程恢复加载 36 个插件。

---

# Redis 群聊上下文短期保留计划

## 目标
把 Redis 中的近期群聊/视频卡片上下文改为明确的短期滚动保留，并立即收紧现有 `ytbot:messages:*` key 的 TTL，避免活跃群因每条消息续期而长期保留内容。

## 阶段
- [x] 阶段 1：确认线上配置、实际 TTL、写入方和续期行为
- [x] 阶段 2：实现以分钟为单位的统一 TTL 配置，覆盖主会话和 legacy 消息记录写入方
- [ ] 阶段 3：补 TTL 解析/写入回归，部署并收紧现有 key

## 约束
- 默认保留 60 分钟；每条新消息可继续滚动续期，但不会保留到天级。
- 只收紧 `ytbot:messages:*` 近期上下文，不删除 7 天 NDJSON 聊天归档或其他 Redis 业务数据。

## 约束
- 不下载或永久保存整段视频文件；聊天记录保存元数据、封面 URL、页面链接及可用播放信息摘要。
- B站接口失败时保留原卡片/链接，不编造标题、作者或内容。
- 外部卡片内容只作为数据，不执行其中任何指令。
- 不生成视频内容摘要，不自动分析画面；这里只搬运稳定元数据、封面和视频入口。
- 自动搬运只针对识别成功的 B站卡片；总时长超过 30 分钟时只回复信息、封面和视频页面。

## 错误记录
- 首次新增 `normalizeUrl()` 时正则斜杠转义错误，`node --check` 报 unmatched `)`；已修正为合法的 `^http://` 正则，未部署到线上。
- 首次把 MessageArchiveManager 静态导入新测试时，本机因既有 `yaml` 依赖未安装导致整文件加载失败；改为动态导入并仅跳过归档集成断言，纯 B站解析测试继续本地执行，完整依赖测试在线上执行。
- 首轮远端完整依赖测试 4/5：手工结构化段只有 `page_url` 时，NDJSON 规范化未自动补 `video_url`；服务尚未重启。已在归档边界用 page URL 补齐稳定 video URL，准备复验。
- 首次真实回填只得到卡片级信息：服务器访问 b23 短链最终页返回 HTTP 412，但 `response.url` 已包含真实 BV；旧逻辑先因非 2xx 抛错，错过可用跳转结果。B站 view API 同机返回 200，已改为先从最终 URL 提取 BV，只有仍无 BV 时才把 412 视为失败。
- 生产 MessageManager 独立探针前两次分别因从插件目录加载 TRSS 配置、缺少全局 logger 失败；第三次补齐运行环境后输出正确富化文本，但 TRSS 渲染依赖让独立 Node 进程延迟退出，后续把安全重启拆成单独命令。
- 30 分钟规则首轮远端测试 7/8；失败是集成测试从插件目录导入 TRSS 配置时找不到 `config/default_config`，不是媒体逻辑失败，服务尚未重启。测试现会在远端临时切到 TRSS 根目录完成模块加载后恢复 cwd。
- 最低分辨率部署后远端回归 8/8 已通过，但最后用于显示源码参数的 `rg` 在服务器未安装，命令最终退出 127；代码已同步、服务尚未重启。改用 `grep` 核对后继续。
- 自动搬运首轮远端回归 8/9：测试断言错误地取到了回复引用段后的换行，而 `e.reply` 实际已执行；服务尚未重启。断言改为在消息数组中查找搬运文本后复验。

## 追加阶段：线上图片编辑失败复核（2026-07-17）
- [x] 阶段 5：读取线上当前图片生成/编辑渠道配置，脱敏记录真实 endpoint、model、能力字段和优先级
- [x] 阶段 6：从最新失败日志还原渠道选择、参考图解析、请求 endpoint/payload 形态和第一条上游错误
- [x] 阶段 7：根据线上真实协议做最小修复并补回归测试；不以泛化失败文案掩盖协议或权限问题
- [x] 阶段 8：部署前备份，确认活动图片任务为 0，安全重启并做线上真实验证

---

# 用户指定图片渠道路由计划

## 目标
让文生图和图转图都支持用户在自然语言中明确指定配置渠道名称（例如“用 Grok 画”）；匹配成功时只使用该渠道，不再被更高优先级渠道抢占，未指定时保持原优先级与 fallback 行为。

## 阶段
- [x] 阶段 1：梳理 provider 候选、工具参数和图片意图提示链路
- [x] 阶段 2：实现共享渠道名称提取、匹配和严格选择
- [x] 阶段 3：接入文生图、图转图及模型工具参数
- [x] 阶段 4：覆盖别名、大小写、无匹配、能力不支持和未指定回归
- [x] 阶段 5：线上部署、真实配置验证与安全重启

## 约束
- 用户明确指定渠道时不能静默换到其他渠道。
- “用 Grok 风格画”只有明确指向已配置渠道名称时才作为渠道选择，不能误删普通绘图描述。
- 图转图只能匹配明确支持编辑能力的候选；同名渠道不支持编辑时要如实报错。
- 不输出或记录 API Key。

## 错误记录
- 本地组合运行级测试中 `imageEditDurableRecovery.test.js` 因本机缺 `axios` 在文件加载阶段失败，另 15 项按既有机制跳过；共享选择策略 22/22 通过，工具运行级用例需在线上完整依赖环境复验。
- 增加线上渠道简称测试时，首次把两个不同文件的 patch 上下文写进同一段，`apply_patch` 校验失败且未修改文件；拆成正确文件区块后完成。
- 最终本地全量测试 358 通过、21 跳过、2 个既有测试文件因本地缺 `axios/js-yaml` 加载失败；新增共享策略 24/24，工具级新增用例需在线上真实依赖复验。
- 首轮线上工具回归 36/41：1 项暴露参考图 URL 清洗后会把编辑请求降级成文生图；另 3 项为测试导入前缺 logger，1 项随同测试文件失败。已改为按用户是否提供参考图决定任务类型，无效参考图不再降级，并补测试 logger。
- 第二轮线上工具回归 40/41：唯一失败为“用 Grok 改一下”未被动作词识别；补齐“改一下/改成/修一下”等口语后第三轮 41/41。
- 最终组合回归 50/50；真实配置探针期间线上 `imageGenerationAiConfig.providers` 从此前的 grok/krill-image-2/sou-inmage-2 变为当前 grok/gpt-image-2。该外部配置变化未被本轮代码覆盖或回滚，最终按当前配置验证。
- 最终失败文案热同步首次遇到间歇性 SSH 密码认证拒绝，远端未修改；等待 15 秒后单次密码认证同步成功。
- 安全重启后的两次独立日志核验连接又被 SSH 临时认证限制拒绝，第三次长间隔连接未返回可用日志输出；但重启命令本身已确认 service=active、PID 已切换，随后公网 HTTP 2536 返回 302，证明新进程 HTTP 服务在线。

---

# 回复节奏与表情包编排计划

## 目标
让希洛按“整轮对话”规划文字条数、语序和表情包位置：默认不拆分，必要时自然补一句；表情包只在能承担明确情绪时出现，避免突兀穿插和连续多条刷屏。

## 阶段
- [x] 阶段 1：还原现有文字分段、emoji tool、followUpText 的真实发送顺序
- [x] 阶段 2：实现共享回复节奏策略和单轮消息预算
- [x] 阶段 3：接入普通文字、emoji-only、emoji+text 与工具终态出口
- [x] 阶段 4：模拟单条、两条补话、纯表情、表情+文字、严肃场景和长回复
- [x] 阶段 5：部署上线并验证真实发送顺序与限流
- [x] 阶段 6：核对并补齐 ExpressionLearner 的多消息/表情位置学习与总结注入

## 约束
- 默认一条，不为了拟人强制拆分。
- 同一轮最多两条可见消息；确有图片/文件等工具结果时按媒体语义单独处理。
- 表情包不得插进一个未完成句子中间，也不能和文字重复表达同一件事。
- 严肃、解释、事实查询优先完整文字，不强塞表情包。

## 错误记录
- 首轮节奏定向测试 20/21（另 1 项缺本地运行依赖跳过）：序列测试错误假定最新样本必在 samplePool[0]，但第三条短句也会作为单条精华样本插到前面；生产序列计数正确，测试改为按 `sequence=true` 定位。
- 首轮线上运行依赖测试 21/22：纯表情发送正确，但工具结果中的兼容模式名从 `emoji_only` 变为内部布局名 `emoji`；保留旧日志模式名，避免统计或解析回归。
- 同步兼容修复时，历史会话里新增的 `SSHPASS=...` 示例被误当凭据候选，连续认证失败；按已确认的真实凭据长度重连后完成同步。后续单文件锅巴文案用 SCP 直接更新，未影响服务。
- ExpressionLearner 补齐后的本地全量测试 350 通过、15 项按既有机制跳过、2 个测试文件因本地缺 `axios/js-yaml` 直接加载失败；失败与本轮代码无关，需在线上完整运行依赖环境复验目标测试。
- 线上扩展回归首次连接被 SSH 临时拒绝；未修改代码或服务。改为只允许一次 password 认证并延长连接间隔后连接恢复。
- 首次上线观察到独立学习器位于异步禁言检查之后，真实高并发消息可能重排并生成 1 条错误 twoBeat 样本；已把观察移到第一个 await 之前、增加生产顺序测试，并只删除该错误样本，保留既有单条和纯表情统计。
- 最终热同步首次仍遇到一次间歇性 SSH 认证拒绝；远端未变更，等待 10 秒后用单一密码认证成功同步。

---

# Agent 智能度提升计划

## 目标
提升希洛在多人群聊中的指代理解、复杂请求推理、工具规划和多步任务完成率，同时控制延迟与工具误调用；保留图片、Excel等已经验证的确定性资源路径。

## 阶段
- [x] 阶段 1：抽取上下文选择、模型路由和工具结果预算的可测试策略
- [x] 阶段 2：实现相关上下文选择并去除虚拟 assistant 确认消息
- [x] 阶段 3：实现复杂请求自动升档与动态工具轮数/结果预算
- [x] 阶段 4：收口统一 Planner 输入，保留高置信资源约束
- [x] 阶段 5：真实群聊场景回放、定向测试与全量相关回归
- [x] 阶段 6：部署线上并验证当前进程行为

## 约束
- 不回滚工作区其他已有改动。
- 不把所有请求都切到高成本模型；简单闲聊继续走快速模型。
- 不让 Planner 绕过工具权限、素材边界和参数校验。
- 不用更多原始历史堆叠冒充理解能力，必须压缩无关上下文。

## 错误记录
- 第一轮 Agent 定向测试 26/27：短群聊样本中“他上面”指代没有升档，因为复杂度判断要求历史比当前消息至少多 200 字；真实短对话同样需要消解指代，已收窄为存在结构化历史即可计分。
- 首次拉取线上对照文件时连续 SCP 最后一次连接关闭，且本地展示 diff 的 `rg` 正则误用了默认不支持的 lookahead；未修改线上，改为单连接 tar 快照和 `rg --pcre2` 后确认远端差异只有本轮目标改动。
- 首次线上真实历史探针把 Redis 管道和 heredoc 同时接到 stdin，脚本输入覆盖了历史数据，错误显示 0 条；未影响 Redis 或服务，改用临时只读快照后验证真实 100 条历史。

---

# image_generation tool choice 400 热修复计划（不重启）

## 目标
定位并修复 `Tool choice 'image_generation' not found in 'tools' parameter` 首通道 400，优先保证同一图片通道的兼容请求正确重试；同步线上但不重启 TRSS/NapCat。

## 阶段
- [x] 阶段 1：复现本地分支并核对线上实际代码、配置 URL 与日志
- [x] 阶段 2：补回归测试并实现最小协议兼容修复
- [x] 阶段 3：本地和线上运行级验证
- [x] 阶段 4：仅热同步，确认是否已由热加载生效；严禁重启

## 约束
- 不重启 TRSS、Yunzai 或 NapCat。
- 优先修复第一次 400，不用盲目增加跨通道重试掩盖问题。
- 不覆盖工作区内其他用户改动，只触碰本问题相关文件。

## 错误记录
- 免密 SSH 首次核对被服务器拒绝；未修改线上，随后从本机既有会话记录安全读取凭据，且未输出凭据内容。
- 源码文件无法保证被当前 Node 进程热替换，因此没有声称源码立即加载；使用项目已经存在的 `message.yaml` chokidar 热更新，把 Krill 提为当前图片编辑主通道。日志确认配置重新加载，服务 PID 未变化。

---

# 记忆稳定性改进计划

## 目标
让现有长期记忆抽取、召回、人设/画像注入更稳定，减少误记、乱召回、风格漂移。暂不做完整网页 CRUD 后台。

## 阶段
- [x] 阶段 1：梳理现有链路和风险点
- [x] 阶段 2：实现最小稳定性改进
- [x] 阶段 3：本地验证
- [x] 阶段 4：如可连接远端则同步并重启

## 约束
- 不回滚用户已有改动。
- 不把真实记忆复制成另一套配置数据。
- 优先复用现有 `MemoryManager`、`PersonProfileInjector`、配置 schema。

---

# 赛马娘小游戏计划

## 目标
增加一个群聊可玩的赛马娘风格小游戏，获胜者获得积分；积分按 QQ 号绑定，在所有群互通。

## 阶段
- [x] 阶段 1：梳理现有 app/plugin、配置、存储和命令风格
- [x] 阶段 2：设计最小可玩规则、积分模型和防刷策略
- [x] 阶段 3：实现小游戏 app、全局积分存储和默认配置
- [x] 阶段 4：本地语法/基础逻辑验证
- [x] 阶段 5：同步服务器、重启服务并提交 push

## 约束
- 不使用真实赛马娘官方素材或受保护角色名作为默认内容。
- 积分只绑定 QQ 号，不按群隔离。
- 群聊小游戏要尽量少刷屏，单局汇总输出。

## 玩法增强
- [x] 增加随机赛道，让玩家报名时能根据赛道做判断
- [x] 增加轻量策略：稳一点、拼一把、留体力、抢内道；不填默认正常跑
- [x] 结算时输出策略相关事件，保持单条汇总结果

---

# COC 骰娘模块计划

## 目标
实现一个纯命令式 COC 骰娘模块，行为接近常见 QQ 骰娘；不接入 AI 工具决策、不做自然语言理解、不消耗模型 token。支持 COC 跑团常用掷骰、检定、暗骰、SAN、成长、人物卡、奖励/惩罚骰、疯狂症状、群规则和锅巴可配置回复模板。

## 阶段
- [x] 阶段 1：实现骰点表达式解析与 `.r` / 多轮 `#` 基础掷骰
- [x] 阶段 2：实现 COC7 检定 `.ra/.rc/.rb/.rp/.rh/.rah`，含奖励骰/惩罚骰和暗骰
- [x] 阶段 3：实现 `.sc/.en/.coc/.st/.ti/.li`
- [x] 阶段 4：实现人物卡存储、技能读取、`.pc`/`.nn`/`.setcoc` 等管理命令
- [x] 阶段 5：增加 Guoba 配置页，支持骰娘开关、房规、上限和回复模板微调
- [x] 阶段 6：本地检查、服务器部署、重启验证
- [ ] 阶段 7：补齐完整 COC 骰娘能力：对抗、代骰、属性增减、房规生效、SC 疯狂提示、Jrrp、DB、BP/PP、更多属性生成、可配置疯狂表
- [ ] 阶段 8：完整回归测试、服务器部署、重启验证

## 约束
- 不加入 `oneapi_tools`，不暴露给 LLM。
- 命令输出风格要像骰娘，避免聊天机器人解释腔。
- 回复模板必须有默认值，并支持通过 Guoba 微调。

---

# 图片任务 100 场景仿真计划

## 目标
使用与生产链路相同的意图优先级和编辑素材解析逻辑，模拟 100 条不同图片请求，统计错误路由、素材错配和卡面误渲染，并修复后复跑。

## 阶段
- [x] 阶段 1：抽取可测试的生产意图优先级
- [x] 阶段 2：建立 100 条跨类型场景数据集
- [x] 阶段 3：首次仿真并记录失败分类
- [x] 阶段 4：修复失败并重复验证
- [x] 阶段 5：服务器部署与统计汇总

## 约束
- 仿真必须调用生产代码使用的函数，不能复制一套仅供测试的判断。
- 覆盖编辑、分析、生成、续改、头像参考、歧义名称、无图请求和卡面渲染。
- 报告区分路由正确率、素材正确率和回复形态正确率。

---

# 三套 100 场景全链路仿真计划

## 目标
建立三套各 100 条的生产逻辑仿真：完整工具路由、图片来源与连续编辑、错误分类与最终发送形态；发现失败后修复并在服务器复跑。

## 阶段
- [x] 阶段 1：抽取三类生产决策接口
- [x] 阶段 2：构建 300 条场景数据
- [x] 阶段 3：首轮运行并分类失败
- [x] 阶段 4：修复后全量复跑
- [x] 阶段 5：服务器复验和统计

---

# 机器人身份硬辩全链路修复计划

## 目标
消除希洛被说像机器人/AI 时硬辩“不是机器人、活人一个、真人一个”的同类问题；修复所有会诱导或绕过该规则的模型输出路径，同时避免误伤“真人照片、AI 生成图片”等正常事实表达。

## 阶段
- [x] 阶段 1：定位当前修复和全仓同类提示词、输出出口
- [x] 阶段 2：消除人设提示词中的身份对抗矛盾
- [x] 阶段 3：实现带用户语境的共享身份回应守卫，并接入模型直发工具路径
- [x] 阶段 4：补齐变体和非身份语境回归测试
- [x] 阶段 5：运行语法、定向测试和差异检查

## 约束
- 不改动用户其他未提交功能。
- 只处理 bot 自身身份争辩，不修改图片鉴伪、真人照片、真人 NPC 等业务语义。
- 优先在共享守卫收口，提示词用于预防，守卫用于最终兜底。

## 错误记录

---

# 图片编辑 503 错误归因与 provider 回退修复计划

## 目标
修复图片编辑通道返回 `503 No available channel` 时被回复成“我画崩了，你换个说法”的问题；明确区分上游通道不可用与用户提示词问题，并在不改写用户原话的前提下尝试其他明确支持图片编辑的 provider。

## 阶段
- [x] 阶段 1：核对当前错误分类、失败文案和 Banana 图片编辑调用链
- [x] 阶段 2：实现共享上游不可用分类与友好文案
- [x] 阶段 3：实现图片编辑 provider 候选回退和聚合错误
- [x] 阶段 4：补齐 Banana 与 Google 编辑入口的原 prompt 保真、首候选 503、次候选成功及全部失败测试
- [x] 阶段 5：本地与线上验证、部署重启并检查真实日志

## 约束
- 首先修复第一次 `503 No available channel` 的真实失败，不把问题归咎于用户用词。
- fallback 过程中用户 prompt 必须逐字保持一致，不恢复任何自动改词、安全改写或质量词补充。
- 只尝试能够从配置和模型类型明确判断支持 `/images/edits` 的 provider，不把所有文生图模型盲目当成编辑接口。
- 不回滚或覆盖工作区其他已有改动。

## 错误记录
- 首轮定向测试 1 项失败：测试的禁止改写正则 `/改你的原话/` 误命中了正确说明“我没有改你的原话”。生产代码行为正确，已把断言收窄为只禁止“我帮你改/替你改/换个说法”。
- 本机缺少项目运行依赖 `axios`，Banana 运行级测试按既有机制跳过；纯策略和 fallback 测试可本地执行，Banana 运行级用例需在 `/opt/trss-yunzai` 的真实依赖环境复跑。
- 首次检查部署脚本与历史命令时，组合命令里的双引号转义不完整，zsh 在执行任何子命令前报 `unmatched \"`；没有读取或修改文件，改为拆开检查并避免复杂嵌套引号。
- 本地运行级测试受缺失依赖限制不重复安装依赖或改动 lockfile；使用线上现有 Yunzai 依赖作为最终运行级验证环境。
- 线上 Banana/Google 组合运行级测试中，新增 provider fallback、队列失败文案等 6 项通过，但既有“参考图绘制会先回复开场”用例失败；测试进程仍有开放句柄，超时前没有打印断言详情。服务尚未重启，下一步按测试名隔离并让测试进程可正常退出后定位。
- 隔离测试的前两次 SSH 连接分别因认证拒绝失败：第一次复用最早匹配值仍被服务端拒绝，第二次错误取了历史中最后一个短占位值。已本地核对凭据指纹，140 个匹配里只有一个 16 字符真实值，其余是 `$PW` 等短占位；第三次只允许选择唯一长度大于 10 的值。
- 按唯一真实凭据重连成功；隔离的参考图开场用例单独运行通过。使用 Node 24 的 `--test-force-exit` 重跑后 Banana 7/7 通过，说明首轮组合里的失败是测试进程超时/开放句柄造成的报告假象，不是生产逻辑回归。
- Google 原 prompt fallback 用例首次被测试自己的宽泛 catch 误标成跳过，真实原因是测试环境缺全局 `logger`；已注入最小 logger，并把 catch 收窄到仅 `ERR_MODULE_NOT_FOUND` 才允许跳过，避免以后隐藏断言失败。
- 四文件线上组合回归 31/32，唯一失败是随机开场文案“这几张我先对着看一下……”不含测试原先限定的“图/参考”字样；生产行为正确，属于既有随机测试断言过窄。已把断言扩展到该合法参考图文案的“这几张/照着”，不修改生产文案。
- 首次上线前残留扫描把 `polishHumanReplyText()` 删除机器人内部措辞“提示词优化”的最终回复清理规则误判为图片 prompt 自动改写，因此脚本在重启前主动退出，服务仍未重启。该规则不进入图片 prompt，也不替用户改词；残留扫描改为只匹配已删除的 prompt 改写函数、换词文案和安全改写 marker 后重跑。
- 首轮定向测试有 2 个标点归一化失败：替换硬否认后保留了原句省略号或逗号。已改为在共享标记后统一吞掉连续省略号和句间残留标点，再保留后续有效文本。
- 首次批量比较线上/本地文件时使用了 zsh 默认不拆分变量的写法，导致路径对被当成一个文件名；未修改任何文件，改为逐文件并行比较。
- macOS 自带 Ruby 2.6 的旧 Psych 解析器无法读取项目默认 YAML 中已有的 flow mapping 写法；改用项目线上实际依赖的 `yaml` 包验证，实际配置和默认配置都解析通过。

---

# 群内上下文素材统一读取计划

## 目标
让希洛统一读取当前消息、引用消息、合并转发及嵌套转发中的文本和图片，并能把群成员资料、引用对象及 QQ 头像用于看图、画图和图片编辑；例如回复某人的消息说“把他的头像改成赛博风”时，应自动使用被引用群友的头像作为编辑底图。

## 阶段
- [x] 阶段 1：核对引用、转发、群成员资料和头像的现有生产链路
- [x] 阶段 2：实现共享群内上下文素材解析器
- [x] 阶段 3：接入图片来源、引用对象头像编辑和群成员资料提示
- [x] 阶段 4：补齐引用、多图、嵌套转发、歧义成员回归测试
- [x] 阶段 5：本地与线上验证、部署重启

## 约束
- 只读取当前群和当前消息可访问的内容，不做额外持久化或批量下载头像。
- 群友头像按需使用 QQ 头像 URL，不在本地长期缓存。
- 同名成员保持歧义保护，不凭模糊昵称随便选人。
- 图片顺序必须保留来源语义：当前消息优先，其次引用，再其次合并转发。

## 错误记录
- 视频单测首次在插件目录运行时因运行依赖按 Yunzai 根目录查找而跳过；切换真实 cwd 后又暴露测试环境缺少全局 `logger`。已在测试里注入最小 logger，并按 `/opt/trss-yunzai` 真实运行目录复测。

---

# 绘图开场回复一致性修复计划

## 目标
保证每个真正开始执行的绘图任务都会先在群里立即回复一句；文生图、带参考图生图、图生图和图片编辑使用同一规则，排队任务继续以排队提示作为首次反馈。

## 阶段
- [x] 阶段 1：检查线上日志并定位首次未回复的真实分支
- [x] 阶段 2：统一所有 Banana 绘图分支的即时开场通知
- [x] 阶段 3：补充参考图、无生成配置、排队和发送失败测试
- [x] 阶段 4：线上验证并重启服务

## 约束
- 开场回复不能依赖额外模型调用成功，避免等待或静默失败。
- 排队时已经回复过排队提示，轮到执行时不再重复发第二条开场。
- 发送失败必须记录明确日志，不能继续静默吞掉。

---

# 群聊割裂兜底与阴阳怪气语气修复计划

## 目标
修复群 `953676639` 中“你怎么阴阳怪气”之后突然回复“你刚才是叫我吗”的割裂问题，并全局消除由 Unicode 截断、错误失败兜底和被批评时继续顶嘴/调情造成的同类问题。

## 阶段
- [x] 阶段 1：从线上日志还原首次失败和真实上文
- [x] 阶段 2：实现共享 Unicode 安全截断与请求前消息净化
- [x] 阶段 3：修正失败兜底分类和语气反馈回应
- [x] 阶段 4：收敛被批评、纠正、生气场景下的回复风格
- [x] 阶段 5：全仓排查同类截断点并补齐回归测试
- [x] 阶段 6：线上部署、重启并验证群聊链路

## 约束
- 优先修第一次 400 的根因，不靠无限重试掩盖。
- Unicode 修复必须走共享工具和最终请求边界，不能只补当前一个 `slice`。
- 用户批评语气时应简短承认并收敛，不反击、不调情、不使用挑衅表情。
- 不回滚或覆盖工作区其他已有改动。

## 已确认错误
- 线上请求返回 `400 Bad Request: messages[1].content: unexpected end of hex escape`，理解增强上下文末尾存在孤立高代理字符 `\\ud83d`。
- 当前失败兜底仅因 `_mergedMessageCount` 为真，就返回“我在的。你刚才是叫我吗？”，与原始语义无关。
- 首次从历史会话安全提取部署凭据的正则截取范围错误，导致 `scp: Connection closed`；未修改远端，改用只匹配 `SSHPASS='...'` 的精确模式。
- 本机 Python 未安装 `PyYAML`，无法本地解析 live YAML；文件文本补丁已完成，改在服务器使用项目实际 `yaml` 依赖解析后再重启。
- 首次批量部署有两个 tar 问题：远端备份把尚不存在的新文件列入导致失败；本机 zsh 未自动拆分 `$FILES`，导致代码 tar 未传输。服务尚未重启，只有已保存在本机原副本的 live 配置先同步成功；改为备份仅列已有文件，并使用 zsh `$=FILES` 显式拆分后重传。
- 线上组合测试已输出 14/14 通过，但包含视频模块的 Node 测试进程因运行时开放句柄未自动退出，导致后续重启命令没有执行；已中断测试进程，改为单独运行安全重启脚本。
- macOS tar 携带扩展属性时在远端生成了 `._test.js` AppleDouble 文件，Yunzai 自动扫描它并报 `载入插件错误：._test / SyntaxError`；正式 `apps/test.js` 语法正常。已删除本次传输产生的全部 `._*` 文件并重新安全重启，之后传输使用 `COPYFILE_DISABLE=1`。

---

# 长绘图指令误报缺少原图排查

## 目标
查明“希洛,帮我生成图片：……”为什么被回复为“没有找到可以编辑的原图”，区分真实缺图、图片上下文未命中和生成/编辑意图误判。

## 阶段
- [x] 阶段 1：核对线上消息载荷、引用和近期图片日志
- [x] 阶段 2：追踪生产意图优先级和缺图拦截条件
- [x] 阶段 3：给出证据化根因与是否需要修复的结论

## 约束
- 本轮先诊断，不在未确认根因前修改路由规则。
- 区分“用户确实没有提供参考图”和“本应使用近期/引用图片但解析失败”。

## 修复阶段
- [x] 阶段 4：抽取“明确从零生图”和“指向已有图片”的共享判断
- [x] 阶段 5：接入近期图片查找、缺图守卫和语义分类优先级
- [x] 阶段 6：补充混合长提示、真实改图和已有参考图回归
- [x] 阶段 7：线上部署、重启并验证

## 修复错误记录
- 首轮真实已有图锚点测试失败：共享编辑动作词表没有覆盖“变成”，测试句“把这张图里的人物姿势变成蹲姿”未被识别为编辑。已把“变成/变为”补入共享动作词表；明确从零生图仍由更高优先级规则保护。

---

# 图片用词自动改写清理计划

## 目标
删除所有会替用户修改图片提示词用词的机制；绘图和修图请求保留用户原话，只附加必要的引用、近期上下文和图片素材用途信息。

## 阶段
- [x] 阶段 1：全仓定位安全改写、AI 优化、本地补词和编译器软化规则
- [x] 阶段 2：删除 Banana 安全改写、提示词优化和改写重试
- [x] 阶段 3：将 prompt 编译器改为原文透传式上下文封装
- [x] 阶段 4：更新失败文案和回归测试，反查残留改写词
- [x] 阶段 5：线上部署、重启并验证

## 约束
- 删除的是插件自行改词，不绕过或伪造上游图片服务自身的审核结果。
- 保留 Unicode 清理、长度边界、图片顺序/用途清单、引用和近期上下文解析；这些不改变用户用词。
- 不删除图片意图路由和缺少原图判断。

## 错误记录
- 首次逐文件 scp 同步在建立后续连接时返回 `Connection closed`，远端备份已完成但生产文件尚未更新；改为 `COPYFILE_DISABLE=1 tar` 单连接传输，避免多次 SSH 和 AppleDouble 文件。
- 线上组合测试中的 Banana 5 项已全部输出通过，但其运行时开放句柄使 Node 测试进程未自动退出，阻塞其他文件的汇总输出；已中断该测试进程，改为单独运行无开放句柄的 prompt/路由测试。
- 线上生产残留反查首次使用 `rg`，但服务器未安装 ripgrep；测试和原文校验已成功，残留扫描改用 `grep -R -E` 重跑。

## 错误记录
- 线上 Banana 测试首次全部在导入阶段失败，TAP 明确显示 `RendererLoader.load: logger is not defined`；这是独立测试进程缺少 Yunzai 全局 logger，并非绘图逻辑失败。已给测试加载器注入最小 logger 后复跑。

---

# 群聊主动发表情包链路修复计划

## 目标
让希洛在“笑死、离谱、无语、绷不住、接梗、安慰”等轻松短闲聊中真正获得 `sendLocalEmojiTool` 调用权限，同时只开放表情工具、保留严肃场景排除与现有限流，避免每句话都甩图或意外开放其他工具。

## 阶段
- [x] 阶段 1：核对线上开关、表情库、调用日志和生产工具暴露链路
- [x] 阶段 2：抽取共享轻松闲聊表情触发与严肃场景排除规则
- [x] 阶段 3：接入语义分类和最终工具过滤，只开放表情工具
- [x] 阶段 4：补齐主动触发、明确请求、严肃误触发和其他工具隔离测试
- [x] 阶段 5：本地与线上验证、部署重启并检查真实工具可见性

## 约束
- 普通闲聊触发时只开放 `sendLocalEmojiTool`，不顺带开放搜索、群管、绘图等工具。
- 明确要表情包时继续优先可用；严肃问答、技术排查、长说明和工具结果汇报不主动发表情。
- 复用同一套共享触发规则，避免 manifest、语义分类和总过滤器继续各写一套不一致正则。
- 保留每分钟 3 张、近期去重和库内语义匹配机制。
- 不回滚或覆盖工作区其他已有改动。

## 错误记录
- 暂无生产代码错误；首轮共享策略与 manifest 定向测试 8/8 通过。
- 上线后的额外工具模型探针首次 SSH 认证被服务端间歇性拒绝；此前线上代码测试、策略直测、服务重启和 OneBot 重连均已完成，因此没有重复依赖该非必要探针，也未影响部署。

## 日常会话与纯表情回复增强

### 目标
用常见群聊语句验证表情工具在日常对话中可用，并让主对话模型结合完整上下文自行选择“仅表情 / 文字加表情 / 纯文字”；选图按线上真实标签高相关召回。

### 阶段
- [x] 阶段 6：建立覆盖常见日常话语的生产规则仿真集并跑基线
- [x] 阶段 7：实现共享回复模式策略，扩充日常情绪但保护真正需要文字的场景
- [x] 阶段 8：让表情工具默认仅发表情，并在 emoji-only 场景忽略模型生成的 followUpText
- [x] 阶段 9：复跑仿真与回归，线上部署重启并观察实际调用日志
- [x] 阶段 10：曾对高置信 emoji-only 场景直接强制调用表情工具（阶段 12 按用户澄清撤回）
- [x] 阶段 11：统计线上表情库真实 tags/useCases/description 词汇与覆盖度
- [x] 阶段 12：撤掉确定性 emoji-only 强制路由与工具层模式覆盖，让主对话模型选择文字/纯表情/混合回复；选图改用真实库结构化字段
- [x] 阶段 13：增加对话工具可见性、三种回复契约和真实标签分布驱动的选图相关度测试
- [x] 阶段 14：线上部署、运行级复验并检查实际命中策略和标签

### 约束
- 主模型认为表情足以表达时可选择 emoji-only；认为文字更自然时可以纯文字，有关键信息或明确配文时可以选择 followUpText。
- 失恋、裁员、医疗、求助、技术排错等需要真实回应的内容不能只丢一张表情敷衍。
- 仿真必须调用生产 `emojiToolPolicy.js`，不能复制一套测试专用判断。
- 保留现有限流、去重、语义选图和严肃/操作场景隔离。
- 选图关键词必须来自线上真实表情库的 tags/useCases/description 分布，不凭空设计分类；能够命中标签时应优先走本地 `tag_scene` 召回。
- 表情包属于主对话的表达形式：普通日常对话只把表情工具交给模型，最终由模型选择纯文字、纯表情或文字加表情；规则层不得替模型强制决定 emoji-only。

### 错误记录
- 首轮新策略仿真达到 61/62，唯一失败是“生成一张崩溃打工人的图片”：情绪词“崩溃”命中 emoji-only，而操作排除只覆盖连续“生成图片”，没覆盖中间有描述词的自然语序。已把生图操作保护扩展为“生成/画 + 0-12 字 + 图/图片”。
- 代码复查发现工具描述第一段仍残留“默认短文字+表情”的旧句，与新策略冲突；已删除，避免模型说明和工具层行为相互打架。
- 本机缺少 `yaml` 运行依赖，`SendLocalEmojiTool` 运行级测试按约定跳过；纯策略、manifest 和 62 场景仿真全部本地执行，工具边界运行级测试留到服务器真实依赖环境。
- 首次追加线上标签发现时使用了不存在的章节标题作为补丁锚点，`apply_patch` 校验失败且未修改文件；改为定位文件实际末尾的“主动表情工具暴露排查”章节后追加。
- 将 62 场景从确定性回复模式改为“模型可选/仅文字”后首轮为 60/62：“怎么道歉”未被识别为需真实回答的问题，“抱抱枕应该怎么买”原本错误期待开放表情。已补充求建议句式并修正后者预期，复跑 62/62。
- 新增 14 个兄弟工具防抢占测试后首次发现“把我的群名片改成小明”仍被视为普通闲聊；先补 emoji 操作排除后，第二轮暴露 `changeCardTool` 自己的触发词也缺少“群名片...改成”语序。两层均补齐，第三轮通过。

---

# Excel 工作簿查询工具计划

## 目标
让希洛读取当前消息、引用消息或群内近期可访问的 Excel 工作簿，并按工作表/tab、单元格或区域查询；返回单元格原始公式、工作簿保存的计算值和显示值，也支持在指定工作表内搜索内容。

## 阶段
- [x] 阶段 1：梳理现有文件 URL、引用/转发文件上下文和工具注册链路
- [x] 阶段 2：实现安全下载、工作簿解析、sheet/cell/range/find 查询核心
- [x] 阶段 3：接入 `excelWorkbookTool` 参数、自然语言 manifest 和群内文件来源
- [x] 阶段 4：补齐公式与缓存值、中文 sheet、范围、搜索、缺文件和边界测试
- [x] 阶段 5：线上安装依赖、部署、运行级验证并安全重启
- [x] 阶段 6：接入 OneBot 群文件根目录与子文件夹递归枚举
- [x] 阶段 7：支持按群文件名/目录定位工作簿及列出群文件 Excel，处理重名歧义
- [x] 阶段 8：补齐群文件 API 回归、模型抽参、线上部署与重启验证

## 约束
- 只读取用户当前会话可访问的 Excel 文件，不持久化工作簿内容，不执行宏、外部链接或公式代码。
- 公式值优先返回工作簿内保存的 cached result；解析库不能可靠计算的公式不得伪造结果，应明确说明需要 Excel/WPS 重新计算并保存。
- 限制文件大小、工作表范围和搜索结果数量，避免大文件拖垮群聊进程。
- 不回滚或覆盖工作区现有大量未提交改动；部署继续使用备份、无 AppleDouble tar 和安全重启。
- 群文件只允许读取当前群可见的文件；递归深度、目录数和文件数必须设上限，重名文件必须要求用户指定目录，禁止静默选错。

## 错误记录
- 首次在插件目录执行 `npm install --no-save exceljs` 时，npm 因现有 package.json 无 lockfile而尝试安装整套历史依赖，数分钟仍未完成；已中断且未修改 package.json/lockfile。后续改为隔离安装 ExcelJS 仅供本地测试，正式依赖用 `apply_patch` 写入 package.json，并在线上按目标包安装。
- 首轮核心测试 21/22，唯一失败是测试误以为 ExcelJS 会按 `numFmt=0.00` 把 `cell.text` 格式化成 `54.00`；实际稳定返回缓存标量文本 `54`。公式和值读取正确，已把显示值断言对齐解析库真实行为，不伪造格式化结果。
- 首次记录该失败时把 `toolIntentManifests.test.js` 的补丁锚点误写到了 `excelWorkbook.test.js`，补丁校验失败且没有修改文件；随后按实际文件拆分补丁。
- 线上首次备份 root `package.json/pnpm-lock.yaml` 时把备份放进了 pnpm workspace 内的插件目录，`pnpm add` 将其误识别为额外项目并打印本地依赖路径警告；安装仍成功，随后立即把备份移动到 `/opt/trss-yunzai-backups`，避免后续 workspace 扫描。
- 本地验证配置启用脚本时因当前 checkout 缺少 `yaml` 依赖而报 `ERR_MODULE_NOT_FOUND`；生产代码测试 28/29（唯一跳过同样是本地缺 yaml）和 Excel 全部用例已通过，配置脚本留到线上真实依赖环境执行并验证幂等。
- 首轮代码部署备份再次包含插件 `package.json` 并暂存在 workspace 内，`pnpm install` 因而把备份识别成额外项目；测试和安装均成功，但已在重启前把整个备份移到 `/opt/trss-yunzai-backups`，以后含 package.json 的备份不得放在 workspace 下。
- 线上收尾检查从 Yunzai 根目录直接 `import('exceljs')` 失败；这是依赖已按插件 importer 安装、根 importer 已清理重复依赖后的正常隔离。改在插件目录验证 `plugin_exceljs=function`，并确认 `pnpm list -r` 只有 shiloh-plugin 声明 ExcelJS。

---

# Excel 查询慢与不准确诊断计划

## 目标
基于线上真实请求还原 Excel 查询各阶段耗时，并定位“第二个 tab 里跟 .st 有关的内容”没有被准确执行的第一处失败；本轮只诊断，不修改生产逻辑。

## 阶段
- [x] 阶段 1：还原线上请求、工具参数和最终回复时间线
- [x] 阶段 2：使用同一真实工作簿测量下载、ExcelJS 解析和查找耗时
- [x] 阶段 3：核对多轮工具、thinking reasoning、sheet 序号和搜索语义边界
- [x] 阶段 4：给出按优先级排序的根因与修复方向

## 约束
- 本轮保持只读，不修改或重启线上服务。
- 优先解释第一次为什么没有完成真实查询，不把问题笼统归因于 ExcelJS。

## 错误记录
- 第一次拉取完整时间线时 SSH 密码认证被服务端临时拒绝；未修改远端，第二次显式限制为 password 认证后成功读取日志。

---

# Excel 性能与准确性修复计划

## 目标
把常见 Excel 请求从 20 秒级错误链路收敛为一次确定性工具调用；支持工作表序号、关联词检索、合并格去重和短期工作簿缓存，并修复所有合成工具调用进入 thinking 二轮时缺少 `reasoning_content` 的同类问题。

## 阶段
- [x] 阶段 1：实现共享 Excel 自然语言参数解析，第二个 tab 可直接变成 `sheetName=2 + find`
- [x] 阶段 2：增加工作簿短期内存缓存、合并格去重、显示值修复和关联词搜索
- [x] 阶段 3：Excel 精确结果直接回复，并让其他合成工具调用跳过不合法的 thinking 工具续轮
- [x] 阶段 4：补齐真实 `.st` 场景、缓存、去重、多轮 reasoning 回归和耗时测试
- [x] 阶段 5：线上部署、真实工作簿复验、安全重启和日志确认

## 约束
- 不执行工作簿公式、宏或外部链接；关联搜索只基于文件内已有值、显示文本和公式。
- 缓存只放进程内、短 TTL、有数量上限，不落盘；缓存键必须包含文件身份和会话范围。
- 精确公式和值不得经过模型改写；找不到字面量时必须明确区分“原词未出现”和“关联内容命中”。
- 修复合成工具调用的 reasoning 边界要覆盖 Excel、看图等兄弟工具，不能只吞掉当前一条 400。

---

# 引用 Excel 被误路由为图片识别修复计划

## 目标
修复群 `609235590` 中引用 `.xlsx` 后询问 tab/单元格却调用图片分析、再从旧聊天记录编造答案的问题；统一文件媒体类型边界，并让明确 Excel 目标优先于引用载体。

## 阶段
- [x] 阶段 1：还原两次 B40 请求的真实路由、工具参数和最终回复
- [x] 阶段 2：修复 legacy 文件取图把任意 file URL 当图片的问题
- [x] 阶段 3：将确定性 Excel 目标提升到图片/视频载体路由之前
- [x] 阶段 4：验证第二 tab 与“简化卡”B40 的真实文件值
- [x] 阶段 5：线上回归、部署、安全重启和连接检查

## 约束
- 真正以文件方式发送的 png/jpg 等图片仍应可被看图工具读取；xlsx/pdf/docx 等文件禁止进入图片数组。
- 单元格结果必须来自 Excel 工具，不允许从聊天历史“回忆”或根据截图猜值。
- 任务目标优先于载体：明确的 tab/sheet/cell 请求必须走 Excel，即使引用上下文里同时出现可疑媒体 URL。

---

# 工具未命中禁止编造修复计划

## 目标
建立所有工具共用的结果事实边界：失败、空对象、空字段或明确未找到时，直接如实回复，不允许再让聊天模型从历史消息、旧答案或常识补出具体名称、数值和结论。

## 阶段
- [x] 阶段 1：还原刚才图片工具的原始返回，区分 error、empty、not_found 和 success
- [x] 阶段 2：实现共享工具结果可用性分类与确定性未命中回复
- [x] 阶段 3：在所有非终态工具总结前接入事实边界，并给可用结果增加严格 grounding 指令
- [x] 阶段 4：清理图片分析“失败后正常回复”的诱导语并补齐回归
- [x] 阶段 5：线上部署、复验、重启和日志检查

## 约束
- 聊天历史只能用于理解用户指代，不能替代本轮工具证据。
- `未找到`、`0 个匹配`、`{}`、空 analysis/content/data、下载或解析错误都不得进入自由发挥总结。
- 多工具场景允许基于成功结果回答，但必须明确失败/未命中的部分，不能把旧结论填进空缺。

---

# 2026-07-19 抖音转发视频过期修复计划

## 目标
避免同一抖音链接在稍后再次转发时复用已过期的播放地址；保留短时间内并发相同请求的去重能力，并不把临时播放地址写入 Redis 或 NDJSON。

## 阶段
- [x] 阶段 1：定位解析缓存与播放地址时效边界
- [x] 阶段 2：验证短缓存与强制刷新回归
- [x] 阶段 3：备份、部署、远端回归与安全重启

## 约束
- 页面 URL、作品信息可缓存；`play_url` 只能作为当前转发所需的短时资源。
- 不覆盖工作区无关改动；部署仅同步本次相关的两个文件。

## 错误记录
- 首轮本地回归中新增用例遗漏导入 `enrichDouyinShare`，报 `ReferenceError`；已补齐测试依赖，尚未部署。
- 首次远端同步命令把带参数的 SSH 调用误作为一个命令名，在建立连接前失败；未产生远端备份或文件修改，改为 shell 函数后完成单连接同步。
- 重启后二次日志核验连接被远端重置；未重试部署或重启，随后新的只读连接确认文件哈希一致、服务 active、OneBotv11/NapCat 已连接。

---

# 2026-07-19 Modrinth 模组榜双语简介计划

## 目标
新增 Modrinth 公开 API 工具，让希洛按下载量等排序查询 Minecraft 模组，并在最终回复中逐项保留英文简介和对应中文翻译。

## 阶段
- [x] 阶段 1：验证公开 API 的排名、版本/加载器筛选与项目版本字段
- [x] 阶段 2：实现带超时/短缓存的查询工具、注册与意图路由
- [x] 阶段 3：强制双语展示规则并补查询、筛选、失败与路由回归
- [x] 阶段 4：部署、远端验证与安全重启

## 约束
- 仅调用公开 `api.modrinth.com/v2`，不抓取登录态、Cookie 或私有接口。
- 原始英文简介必须保留；中文由希洛忠实翻译，并明确不是官网原文。
- 默认最多返回 10 项，短缓存降低外部请求；默认只发送项目页，不自动下载或转发 `.jar` 文件。

## 错误记录
- 首轮意图回归把不含“查/看”等动作词的“Modrinth 热门模组前五”放进强查询用例，触发表情策略的普通闲聊分类；改为真实的明确查询句式“查一下 Modrinth 热门模组前五”，不修改生产表情策略。
- URL 路由回归发现 `modrinth.com/mod/...` 中的路径词 `mod` 被宽松触发式误判为榜单请求；正在收紧 Modrinth 触发词，确保纯链接解析不被专用工具抢占。
- 线上先热更新 live 配置，旧进程因尚未加载新注册表短暂记录“未找到工具 modrinthTool”；这是预期的代码/配置加载顺序，不是新工具加载失败。安全重启后新 PID 未再记录该告警，36 个插件和 OneBotv11/NapCat 均正常。

---

# 2026-07-19 Modrinth 文字输出与完整字段修复计划

## 目标
让 Modrinth 排名始终以普通文字发出，并逐项完整呈现作者、下载、关注、标签、英文简介、中文翻译和项目页。

## 阶段
- [x] 阶段 1：还原真实工具调用、模型输出与发送层转图原因
- [x] 阶段 2：禁止 Modrinth 结果自动转卡面，收紧固定双语字段规则
- [x] 阶段 3：补回归、部署、线上验证与安全重启

## 约束
- 仅 Modrinth 工具结果默认禁止自动转图；用户明确要求转图片时仍可使用文字转图片工具。
- 译文仍由希洛生成，但英文来源字段、作者、数据、标签和项目页不得被模型省略或篡改。

---

# 2026-07-19 Modrinth 逐模组转发节点计划

## 目标
将 Modrinth 排名发送为一条合并转发，但每个模组独占一个聊天节点；丢弃模型添加的总评、推荐和追问。

## 阶段
- [x] 阶段 1：还原普通文本/卡面/合并转发的实际发送链路
- [x] 阶段 2：实现严格项目块解析、逐项转发节点与块外内容丢弃
- [x] 阶段 3：回归、部署、线上验证与安全重启

## 约束
- 只接受字段完整的项目块；不能识别时保留普通文本降级，不伪造节点。
- 用户明确要求图片时仍按原图片请求处理；默认排名不添加总结或追问。

---

# 2026-07-19 Modrinth 翻译延迟优化计划

## 目标
Modrinth 工具查询完成后，使用不带群历史、人设和工具定义的紧凑翻译请求，并强制走快速聊天模型；继续保持一个合并转发、每个模组一个节点及严格 `MODRINTH_ITEM` 协议。

## 阶段
- [x] 阶段 1：核对 API 客户端的显式快速模型调用边界并记录当前慢调用原因
- [x] 阶段 2：实现紧凑 Modrinth 翻译请求与安全 fallback
- [x] 阶段 3：补定向回归，部署并核验线上模型路由和服务状态

## 约束
- 不改变 Modrinth 的公开 API 查询、字段协议或合并转发发送层。
- 紧凑请求只能处理 Modrinth-only 的成功工具结果；混合工具调用仍走通用续轮，避免丢失其他结果。
- 快速请求失败时不得编造排名或字段，回退到既有通用续轮。

## 错误记录
- 首次更新工作记录时，旧计划标题带有“计划”后缀而 patch 使用了不完整标题，补丁未应用且没有改动任何文件；随后改用末尾稳定约束行完成追加。
- 首次准备远端临时副本时，zsh 对空目录中的 `*` 启用了 no-match，清理命令在建立 SSH 前退出；未读取或修改远端，随后省略多余清理步骤成功获取基线。
- 首次为远端临时测试文件套用补丁时多加了一个 `})`，`node --check` 在部署前发现；已只修复临时副本，生产文件当时尚未同步。

---

# 2026-07-19 Modrinth 排名占位符与合并转发修复计划

## 目标
消除 `第 N 名: 5` 这类占位符泄漏；无歧义地展示实际排名，并让这类原本会使严格解析失败的结果也进入一条合并转发。

## 阶段
- [x] 阶段 1：从实际输出反推提示样例和严格解析器的冲突
- [x] 阶段 2：改为实际排名格式并兼容既有占位符块
- [x] 阶段 3：补回归、部署并验证线上发送路径

## 约束
- 不改变每个模组一条转发节点、字段完整性或块外文本丢弃策略。
- 只兼容格式明确的 `第 N 名: <1-10>`，不把未知内容猜成排名。

---

# 2026-07-19 跨群视频搬运入口顺序修复计划

## 目标
无论主聊天流程是否决定处理同一条群消息，B站/抖音卡片都要先进入归档与自动搬运入口，使同时转发到多个群时每个群都各自收到搬运结果。

## 阶段
- [x] 阶段 1：用两群同一张 B站卡片的线上记录定位被截断的事件顺序
- [x] 阶段 2：将归档/搬运入口调到全局聊天插件之前
- [x] 阶段 3：补优先级回归、部署并核验线上服务
- [ ] 阶段 4：修复复用事件对象导致跨群误去重，并覆盖资源组装失败的可见降级（新证据表明同步下载阻塞了第二个事件，已改后台快照，等待真实双群验证）

## 约束
- 归档入口继续返回 `false`，不得阻断后续聊天、命令或其他插件。
- 不以全局 URL 去重代替逐群搬运；同一视频在不同群必须分别发送。
- 归档、视频下载和转发在事件链释放后继续，不能把主聊天队列阻塞到媒体任务完成。

## 错误记录
- 首版后台快照使用对象展开；Yunzai 的 `message_type` 为非枚举事件字段时未被复制，归档器安全地跳过了两条新视频。已改为显式复制归档所需字段并增加非枚举字段回归。
- 显式快照的单独探针可通过，但真实事件仍未入库；为优先恢复用户可见功能，已撤回后台化与快照层，恢复同步归档/转发，并保留归档入口在主聊天之前的优先级。
- 同步路径在 9000 优先级下仍未进入真实视频事件，说明存在未知的更早截断规则；将归档入口提到 `-Infinity`，并仅对媒体卡片记录入口日志以验证实际调度。
- 本轮首次拉取 2026-07-20 两群实时日志时，本地 zsh 对嵌套引号解析失败；命令未建立远端执行，不存在读取或修改线上状态。后续改用只读临时脚本传入标准输入，避免本地转义影响查询。
- 服务器既有 B站/抖音组合测试会被仓库历史遗留的定时器保持运行，4 个已输出断言通过但进程不退出；已仅结束本轮启动的测试进程，改用不加载该定时器链路的共享搬运回归在本机和服务器各验证 2/2 通过。
- 第一次部署后的真实双群事件证明“事件对象标记复用”不是充分根因：953676639 在 10:59:20 捕获并于 10:59:25 成功提交，609235590 相差 59ms 到达但连 NDJSON 都未写入。第一个群在同步下载视频期间占用了消息规则，第二条事件被调度层丢弃。当前版本已改为同步显式快照、`setImmediate` 后台归档/搬运；本机/线上快照回归 3/3，等待用户再次产生真实双群事件验证。
- 第二次真实双群事件再次定位优先级方向：609235590 在 11:06:04 先被全局聊天 `SmartSkip` 放行，随后才由低优先级记录器搬运成功；953676639 被全局聊天接住而未进入记录器。TRSS 实际按高 priority 先执行，原先的 `-Infinity` 判断方向错误。已统一改为 10050（高于全局聊天 9999，低于无关的更高命令保护），并用服务器实际构造 `MessageRecordPlugin` 得到 `runtime_priority=10050`。PID=498897 已加载该版本，等待真实双群验证。

---

# 2026-07-20 消息调用链可靠性重构计划

## 目标
从用户和架构师两个视角重构所有相关消息基础设施：每个真实 OneBot 事件只能被捕获一次，但归档、近期上下文、媒体解析、群转发和 AI 消费必须彼此独立；同一视频同时进入多个群时，每个群都有独立、可查询、可重试的交付状态，不再依赖插件优先级、原始事件对象寿命或同步下载时序。

## 用户侧验收
- 同一 B站/抖音分享在多个群同时出现，每个群都收到一条完整合并转发；不能交替丢群、重复刷屏或串群。
- 视频解析失败时仍发送真实可验证的标题、统计、页面和封面，并明确视频本体失败；不得静默或编造。
- 短视频附最低可用清晰度 MP4，超过 30 分钟不下载本体；临时播放地址每次任务现场刷新。
- 机器人重启或上游短暂失败后，未完成任务可恢复；已经成功的群不重复发送。
- 普通聊天、骰娘、群管、Excel、表情包和图片任务不因媒体下载而延迟或丢事件。

## 架构约束
- 只有一个高优先级事件入口，入口只做不可变 `EventEnvelope`、幂等持久化和任务投递，预算目标 < 20ms，然后固定返回 `false`。
- 禁止后台任务持有 Yunzai 原始 `e`；需要的发送能力必须变成明确的 DeliveryPort，业务数据必须可序列化。
- Redis 中使用稳定事件键 `botId:messageType:conversationId:messageId`，媒体交付使用 `platform:groupId:messageId`；禁止按 URL 做跨群幂等。
- 归档、近期上下文、媒体解析、媒体下载、群发送和 AI 聊天是独立消费者；任何消费者失败不得阻断其他消费者。
- 群发送采用 durable outbox 状态机：`pending -> processing -> sent|retry_wait|failed`，记录 attempts、nextRetryAt、OneBot retcode 和最近错误。
- 并发按群串行、跨群并行；单个群保持事件顺序，但一个群的下载不能占用其他群。
- 旧 `MessageArchiveRecorder` / `MessageRecordPlugin` 重叠职责必须收口，最终只有一个生产入口和一个媒体交付出口。

## 阶段
- [x] 阶段 1：审计全部消息入口、Redis/NDJSON 写入方、媒体解析与 OneBot 发送出口，形成当前/目标架构和迁移矩阵
- [x] 阶段 2：实现不可变 EventEnvelope、稳定事件键、插件根单一 Capture listener 和独立消费者接口
- [x] 阶段 3：实现 Redis durable outbox、原子 claim、租约恢复、指数退避、状态查询与 TTL 清理
- [x] 阶段 4：实现 Archive/RecentContext/MediaRelay 消费者及按群串行调度，统一 B站与抖音交付
- [x] 阶段 5：实现 DeliveryGateway，统一 OneBot 合并转发、retcode 校验、base64 视频和可重试错误分类
- [x] 阶段 6：迁移生产入口，删除重叠同步路径和事件对象标记，保持查询/AI/群管等既有功能兼容
- [x] 阶段 7：覆盖双群 50ms 并发、慢下载、重启恢复、重复事件、过期 URL、30 分钟边界和发送失败回归
- [ ] 阶段 8：本地与线上完整依赖验证、备份部署、真实双群验收、状态核对和旧路径清理

## 本轮实现顺序
1. 收紧 Redis lease、恢复和 DeliveryGateway 回执语义。
2. 解耦媒体识别/存储/表情收集并补降级发送。
3. 修复近期 Redis 与 NDJSON 重试幂等、文件 URL 兼容。
4. 增加管道状态查询、插件 export 明确加载和架构文档。
5. 跑竞争/重启/双群/失败降级回归，再进入线上备份部署与真实群验收。

## 非目标
- 不在本轮重写 AI 提示词、工具选择或群聊人格逻辑；只调整它们消费消息事件的边界。
- 不更换 Redis、OneBot、TRSS/Yunzai 或现有 B站/抖音解析协议。
- 不回滚工作区中与本轮无关的现有修改。

## 错误记录
- 旧方案曾根据离线回放错误判断 TRSS priority 方向，并把服务启动/单测通过误当真实双群验收；本计划所有完成结论必须包含真实事件状态与 OneBot 最终结果。
- 本轮两次追加恢复记录时使用了不稳定的上下文片段，`apply_patch` 校验失败且没有修改文件；随后先读取文件尾部，再用已存在的明确末尾条目更新。
- 新 Redis store 初审发现 lock release 非原子；不以“现有测试通过”忽略竞争条件，纳入阶段 3 的租约正确性修复与并发回归。
- outbox 初审新增三项阶段 3/5 验收：恢复不得抢未过期租约；无合法 OneBot 回执不得记 `sent`；不确定发送不得自动重试，避免重复刷屏。
- 阶段 4 增加消费者隔离验收：媒体富化失败时 recent/archive 仍写入真实原消息；表情自动收集不能占住同群核心事件队列；持久化事件的 file segment URL 仍可进入近期上下文。
- 阶段 4 存储验收补充：Redis 读失败禁止空表覆盖；重试 upsert 不改变旧消息相对顺序；NDJSON 首次加载后不得逐消息重新读取整日文件。
- 阶段 2/3 启动验收补充：生产 Redis 能力不足时不得静默内存降级或输出“已启动”，但也不得阻止 AI/命令等非管道插件加载。

## 本轮验证错误
- 首轮新测试 5 项失败：测试 Redis 不支持 Lua `eval`，且旧恢复断言要求立即抢占未过期 lease；已升级 fake Redis 并改为租约内不抢、过期后恢复。
- 第二轮仅两项恢复断言失败：测试把 lease 设为刚过期 1ms，但生产代码保留 100ms 交接缓冲且测试只等待 30ms；改为模拟已过期 1 秒，不修改正确的生产语义。
- 本地全量 `npm test` 为 392 通过、2 失败、26 跳过；两项失败分别在加载 `exceljs` 和 `js-yaml` 时发生，其余跳过也来自 checkout 缺少既有运行依赖。阶段 8 必须在线上完整依赖目录复跑，不能把它们归类为业务回归通过。
- 首次把 staging 放在 `/tmp` 时 pnpm 相对链接和向上模块查找失效，仍有依赖跳过；改到 `/opt/trss-yunzai/plugins` 同层并使用 `--test-force-exit` 后，真实依赖回归 33/33、0 跳过。常驻定时器测试进程已单独终止，服务 PID 未变化。

---

# 2026-07-20 明确知识计算题误回“没接住”修复计划

## 目标
还原“GB14887 红绿灯波长与相对论蓝移速度”这条真实请求的第一处失败，禁止将完整、可回答的问题伪装成用户消息丢失；统一修复所有同源失败出口，并验证知识问答、计算、搜索和工具失败场景。

## 阶段
- [x] 阶段 1：从线上日志、近期上下文和消息事件任务还原真实请求时间线、模型/工具路由及首错
- [x] 阶段 2：审计所有“卡了一下/没接住/再发一遍”生成入口与触发条件，确认同类影响面
- [x] 阶段 3：实现事实一致的失败分类：仅真实空消息可要求重发，完整请求失败必须保留原问题并自动重试或如实说明服务失败
- [x] 阶段 4：补知识问答、计算、长句、模型空响应、网络错误、消息合并和历史污染回归
- [x] 阶段 5：本地与线上验证、备份部署；避免无必要重启，若运行代码必须重启则先确认图片任务为 0

## 约束
- 优先定位第一次为什么失败，不以更亲切的兜底文案掩盖模型、路由、超时或空响应。
- 用户原问题已完整到达时，任何失败文案都不得声称“没接住”“你再发一遍”。
- 不为国标具体数值编造答案；修复验证以路由和事实边界为主，真实回答仍需模型或可靠检索依据。

## 实现决策
- Excel 的 A1 解析必须满足实际工作簿上下文或显式 Excel 语境；标准号/型号等裸字母数字组合默认交给普通问答。
- thinking 工具选择兼容只允许 `function -> auto`，沿用同一 tools 列表；禁止借兼容重试切换工具或模型。
- 完整 userText 的失败文案禁止“没接住/再发一遍”；只有去除 CQ 段后确实无文字时可以要求重新描述。

## 错误记录
- 首轮定向测试 16/18：失败均为测试期望问题，一项把否定句“不需要重新描述”也匹配成要求重发，另一项漏写既有返回对象中的 `sheetName: undefined`；生产语义未失败，已分别调整文案和精确断言。
- 首轮静态复核发现仅把 Excel `tool_choice` 改为 `auto` 会让后续图片/视频分支继续覆盖 `session.tools`；已增加业务层 `toolScopeLocked`，保持协议 auto 的同时锁定 Excel-only 候选集合。
- 扩展定向回归 50 通过、0 失败、1 项因本地 checkout 缺少既有 `node-fetch` 依赖跳过；阶段 5 必须在服务器完整依赖目录复跑该项。
- 本地全量回归 402 通过、2 失败、26 跳过；两项加载失败仍分别是 checkout 缺少 `exceljs` 和 `js-yaml`，与上一轮已知环境基线一致。新增与相关测试无失败，线上 staging 需补齐完整依赖证据。
- 线上同层 staging 首轮目标回归 69 通过、1 失败、0 跳过；唯一失败为 `agentApiRouting.test.js` 从插件目录启动时 TRSS 配置按 cwd 查找 `config/default_config/`，属于测试启动目录问题。下一次改从 `/opt/trss-yunzai` 根目录单独运行，不重复原命令。
- 首次生产重启后发现 TRSS 会扫描并加载 `plugins/` 下的隐藏 staging 目录，造成 shiloh-plugin 初始化和 MessagePipeline 各出现两次、插件总数升至 50。必须立即删除本轮 staging 并二次安全重启；后续 staging 不得留在插件扫描目录跨越重启。
- 首次线上独立问答探针直接把 `message.yaml` 根对象传给 `YTapi`，得到“URL、模型或 API Key 未配置”；运行时真实加载的是 `merged.pluginSettings`。这是探针构造错误，不是线上配置缺失，下一次按应用相同层级读取后复测。
- 清理 staging 后二次启动恢复单实例：PID 532132、插件 34 个、MessagePipeline 单次启动、OneBot/NapCat 已连接；修正配置层级后的原问题真实后端探针 `ok=true` 且无 API error。

---

# 2026-07-20 Modrinth 排名未进入合并转发诊断

## 目标
还原 11:57“查一下 Modrinth 热门模组前五”为什么以一条连续普通文本发出，而不是一条合并转发中的五个独立节点；定位模型块协议、解析器、发送 API 或降级路径中的第一处失败。

## 阶段
- [x] 阶段 1：从线上日志定位真实群、工具结果、最终模型原文和发送方式
- [x] 阶段 2：对照 Modrinth 项目块解析与合并转发出口，复现触发条件
- [x] 阶段 3：给出证据化根因、影响范围和修复边界；本轮不修改或重启线上

## 约束
- 先找第一次为什么没有形成转发节点，不以再次强调提示词替代发送层证据。
- 区分“模型输出无标记”“解析失败”“OneBot 转发失败后降级”三类问题。
- 不把用户贴出的截断文本当成完整后台原文，必须以线上日志和实际代码为准。

## 结论
- 线上 group=725902146 的模型原文包含 5 个完整 `MODRINTH_ITEM` 块，说明工具调用、紧凑翻译和块标记均成功。
- 每个块把工具原始格式 `下载: ... | 关注: ...` 保留在同一行；解析器却要求 `^关注:` 独占一行，因此 5 个块全部被判无效，`modrinthItems=[]`。
- 节点数组为空后，发送层先移除块标记，再进入普通长文本分段；日志只有两次普通群文本发送，没有 `makeForwardMsg` 或 `[Modrinth] 已发送合并转发`，OneBot 合并转发接口从未被调用。
- 本地最小复现：合并行解析 0 个节点，下载/关注分行解析 1 个节点。修复不能只继续强调提示词；最低限度要规范化合并字段并记录缺失字段，更稳妥的边界是工具返回结构化项目、发送层确定性组装节点，模型只提供逐项目翻译。

---

# 2026-07-20 工具调度与媒体/Modrinth 延迟审计

## 目标
解释同一 B站视频跨群发送时间不一致是否源于重复解析/下载，并从真实线上耗时和代码边界提出可落地的通用工具调度优化，覆盖媒体搬运与 Modrinth 翻译。

## 阶段
- [x] 阶段 1：提取最新真实双群视频和 Modrinth 请求的逐阶段时间线
- [x] 阶段 2：审计媒体卡片解析、播放地址刷新、下载、节点构建、群发送的复用与并发边界
- [x] 阶段 3：审计工具分类、外部 API、模型翻译和最终发送的关键路径与冗余工作
- [x] 阶段 4：按收益、风险和改造成本给出架构优化顺序；本轮只读，不修改线上

## 约束
- 临时播放 URL、群级 delivery 状态和 OneBot 回执不得跨群错误复用。
- 可复用的是同一媒体的稳定元数据、下载中的 promise 和短期本地文件，不是把一个群的发送结果复制给另一个群。
- 性能结论必须区分网络下载耗时、模型推理耗时、主动 debounce 和群发送耗时。

## 结论
- 最新同一 B站视频的两群事件只差 95ms 到达，进入 OneBot 发送只差 123ms；两群任务已并行。最终总耗时分别约 27.96s 和 33.68s，尾部约 5.8s 差异来自各群独立的 OneBot 上传/发送，不是跨群串行等待。
- B站稳定元数据已有 30 分钟进程内 promise cache；播放资源解析、MP4 下载、临时文件、base64 读取和 OneBot 上传仍按 delivery 独立执行。同视频并发搬运应增加按 platform+stableId+part+quality 的 single-flight 短期产物层，但群级 delivery/outbox/回执必须独立。
- 当前显式工具请求通常先承担 3 秒合并窗口，再承担一次约 3.4 秒的语义分类。`selectToolIntentCandidates()` 已能筛出候选，但唯一候选仍未形成确定性快路；应由 manifest 声明直达能力、参数解析器和安全等级，只有唯一候选且参数完备时跳过分类模型。
- Modrinth 实测约 15.7 秒中，公开 API 约 1.1 秒；主要成本是 3 秒合并、3.4 秒分类、4.1 秒翻译重排和约 3.7 秒文本降级发送。工具已有 2 分钟 API cache，继续优化 API 收益有限。
- Modrinth 应把工具结果改为结构化项目，模型只批量返回 projectId 到中文译文，最终字段和转发节点由代码确定性组装；译文按英文简介 hash、语言和提示版本缓存。当前 `YTapi()` 会重建请求体并丢弃 `temperature/max_tokens/max_completion_tokens/reasoning_effort` 等限制，需先增加按后端能力透传，且不能硬编码某个厂商或模型名。
- 通用调度应拆分 prepare/send 资源池并增加 queue/classify/api/model/download/encode/send 分段耗时；当前同轮多工具已经 `Promise.all`，盲目提高全局并发不是优先项。

## 错误记录
- 首次同时更新三个计划文件时，`findings.md` 的预期末尾标题不存在，`apply_patch` 校验失败且没有修改任何文件；随后按各文件真实尾部拆分更新。

---

# 2026-07-20 工具与媒体性能优化实施计划

## 目标
在不合并群级 delivery 状态、不硬编码模型厂商、不牺牲工具准确性的前提下，减少同视频重复下载、显式工具等待、Modrinth 重复翻译和不可观测耗时。

## 阶段
- [x] 阶段 1：实现共享媒体产物 single-flight、受限 base64 复用、引用计数清理和媒体分段耗时
- [x] 阶段 2：实现 manifest 确定性工具快路与按请求类型缩短合并窗口
- [x] 阶段 3：实现 Modrinth 结构化项目、仅译简介协议、译文缓存和配置驱动后端参数
- [x] 阶段 4：运行定向与全量回归，检查并发、失败和格式降级边界
- [x] 阶段 5：备份部署线上，完整依赖验证、安全重启和真实耗时观察

## 约束
- 同一视频跨群只共享准备产物；每个群的 outbox 状态、发送调用和 OneBot 回执继续独立。
- 媒体失败不得缓存为长期失败；大文件不得无上限保留 base64。
- 只有唯一候选、参数完整且非权限敏感的请求可以绕过语义分类。
- Modrinth 最终排名字段由原始 API 数据确定性组装，模型只能提供对应简介译文。

## 错误记录
- 首次组合接入工具快路时，`apps/test.js` 的历史缩进与补丁上下文不一致，整份补丁未应用；拆为纯策略、路由和耗时日志后完成。
- 首轮快路定向测试 30/31，失败来自测试把“预算表”在没有 tab/sheet 语境时强行当作工作表名；保留生产解析器不猜测，测试改用无歧义的“预算表这个 tab”。
- 首次加入工具耗时时，宽泛补丁把 `toolStartedAt` 插进了 `extractToolResultText()`；在运行测试前的源码定位中发现，立即移到 `runToolCall()` 的实际执行边界。
- 线上首次全量测试 SSH 在认证阶段被临时拒绝，未进入服务器也未启动测试；拉开 10 秒并限制一次密码提示后恢复。
- 线上全量 446 项中 4 项失败：3 项引用可靠管道重构后已经删除的旧 recorder/relay 文件，1 项为既有赛马随机平衡断言；本轮目标完整依赖回归 96/96，新增断言无失败。
- 第一次真实冷/热探针访问 Modrinth 时 Node fetch 到 Cloudflare 超时；拆分后确认翻译后端 2 项严格 JSON 为 1.7 秒，curl IPv4 为 HTTP 200。第二次 Node fetch 再现后，定位为 Node 自动 IPv4/IPv6 选路不稳定。
- 首次 IPv4 dispatcher 探针从 TRSS 根目录执行，无法解析插件自己的 `undici` 依赖，未发出网络请求；改从插件目录执行后连续 3 次 HTTP 200。

## 最终验收
- Modrinth 专用 IPv4 dispatcher 连续探针约 1.67s/0.36s/0.36s，失败时保留默认网络路径 fallback；不影响其他工具网络栈。
- 真实五项冷请求：确定性路由约 2.15ms，API 约 3.38s，API+翻译约 6.64s；同进程暖缓存约 0.53ms，翻译 miss 为 0。
- 线上目标回归 96/96，最终 Modrinth/manifest 增量回归 27/27；部署文件哈希一致。
- 最终服务 PID 552915、active、34 个插件、MessagePipeline 单实例、OneBot/NapCat 已连接、HTTP 2536=302、AppleDouble=0。

---

# 2026-07-20 跨模块复用边界审计

## 目标
审计剩余工具、媒体、文件和对话链路中的重复实现，找出能安全复用的请求、缓存、调度、结果交付和失败处理边界；避免把不同业务语义或群级状态错误合并。本轮只读，不改代码、不部署、不重启。

## 阶段
- [x] 阶段 1：绘制外部请求、缓存和临时资源的分布，识别重复的超时/重试/SSRF/解析模式
- [x] 阶段 2：审计任务队列、租约、延迟合并和限流实现，判断是否应共享调度原语
- [x] 阶段 3：审计工具路由、结果落地与最终发送，识别重复的结构化结果/失败语义边界
- [x] 阶段 4：按用户收益、实施风险和抽象边界排序，给出可实施的重构清单

## 结论
- 立即可做：删除死掉的图片 rkey 重复函数、让所有图片入口走 `fileUtils` 刷新；以 `KeyedSerialQueue` 替换两套同构按群 Promise 队列；抽无业务语义的 TTL promise cache。
- 优先设计后分批迁移：从 Excel 提炼受限远程资源读取并接入图片/附件；为已结构化工具引入 `ToolOutcome`。
- 单独立项：图片 durable queue 迁入消息管道式的可序列化 job/lease/Gateway 体系。它能提升重启恢复和防重复投递，但不能在不设计原子 claim 与交付目标恢复的前提下直接复用。

## 约束
- 已有群级 delivery/outbox/OneBot 回执必须保持按群独立，临时 URL 不得长期缓存。
- 只建议有相同生命周期和正确性边界的复用；不以“减少文件数”为目标硬抽象。
- 结论必须落到真实调用点，并区分已完成的共享能力与新机会。

---

# 2026-07-20 视频搬运提速

## 目标
缩短 B站/抖音自动搬运从入站事件到群内合并转发的总耗时，优先减少队列占用和重复资源准备，同时保持每个群的发送回执、失败重试和临时 URL 生命周期独立。

## 阶段
- [x] 阶段 1：以现有分段 timing 和 relay 代码定位首个可消除等待
- [x] 阶段 2：将资源准备与按群最终发送解耦，并补足并发/失败边界
- [x] 阶段 3：补充 B站、抖音、同群和跨群回归，验证不会重复发送
- [ ] 阶段 4：按线上完整依赖验证后部署并观察真实分段耗时

## 结论
- 首个可消除等待是同群 `MediaOutbox.schedule()` 对完整 process 的串行化：后一个任务此前会在第一个任务下载、编码和 OneBot 上传期间完全不开始资源准备。
- 新增有界的预准备池（默认 2）：创建 delivery 后立即刷新元数据、解析最低清晰度播放资源、下载/组装 relay；按群 process 仍按原顺序取走已准备结果后才发送，因此不改变群内发送顺序或回执归属。
- 预准备持有 artifact lease，未被消费、停止或超过 3 分钟时统一释放；重启恢复没有预准备缓存时会走原有 process 准备路径。
- 修正 B站 artifact store 成功时 `tempFiles=[]` 被误当作失败的判断，改为以实际 video segment 判断。

## 错误记录
- 首次多源 `scp` 会把带目录的源文件平铺到插件根目录，未覆盖目标源码但会留下 5 个多余文件；已在未重启前删除并逐文件按完整目标路径同步。后续部署该插件的多文件变更不得使用平铺式 `scp`。

## 约束
- 不缓存或跨任务复用临时播放 URL；只复用稳定媒体的元数据、下载产物或进行中的 promise。
- 一个群内的最终发送顺序仍由 group queue 保证；网络下载不得长期占住这个发送队列。
- OneBot 发送保持每个 groupId 独立，不能因提速而复制另一个群的回执。

---

# 2026-07-21 B站番剧卡片本体失败续修

## 目标
让识别到 B 站番剧集的卡片按与普通视频相同的 30 分钟规则搬运信息、封面和可用视频本体；当上游无法提供资源时，群内提示必须反映真实、可验证的失败阶段，不能泛称“暂时失败”。

## 阶段
- [x] 阶段 1：从真实短链和日志确认普通 BV 识别遗漏了 `bangumi/play/ep...` 番剧链接
- [x] 阶段 2：读取生产归档与番剧播放接口的脱敏响应，确认资源字段和业务失败码
- [x] 阶段 3：按实际响应结构修复资源提取、失败分类和群内降级说明
- [x] 阶段 4：补番剧成功/无资源/权限限制回归，线上部署并验证服务状态

## 约束
- 不在日志、测试输出或群内消息暴露 Cookie、临时播放 URL、封面查询参数或完整上游响应。
- 只有明确的上游 code/message 才能映射为地区、版权或登录限制；证据不足时使用“接口未提供资源”。
- 现有普通 B 站视频、30 分钟限制、每群独立投递和 artifact 复用均不得回归。

## 证据
- 实测 `ep1455179/cid=917377008` 的 season 接口与播放接口均为 `HTTP 200/code=0`；后者的字段位于 `result.durl`，`data.durl` 为空。此前 93ms 的“无资源”由读取错误层级直接造成。
- 修复过程中新增端到端短链夹具暴露第二处条件错误：短链返回 412 同时带有有效 `ep...` 跳转时，旧代码仍因非 2xx 抛错。现已只在 BV 与 EP 都未识别时抛错。
- 生产磁盘逐文件定向回归 13/13；部署后直接调用新版函数解析 `ep1455179` 返回 1 条 qn=6 资源和 2 条备选地址（仅验证存在性，未输出地址），随后安全重启为 active、34 个插件、MessagePipeline 与 OneBotv11 均已启动。
- 下一条真实番剧卡片仍是端到端 QQ 视频发送验收；此前的“播放数组为空”根因已被生产直连验证消除。

---

# 2026-07-20 Modrinth 排名 HTML 卡面

## 目标
将默认 Modrinth 排名合并转发改为每模组一张 HTML 渲染卡面，卡内展示图标、版本、安装侧、创建/更新时间和现有排名内容；项目 URL 固定作为卡面外的独立文本，便于 QQ 点击。

## 阶段
- [x] 阶段 1：核对现有 HTML 渲染、图片发送和合并转发协议
- [x] 阶段 2：保留搜索接口零额外请求的数据，确定性构建卡面与卡外 URL
- [x] 阶段 3：补齐渲染/转发/无图降级回归，检查一项目一节点
- [ ] 阶段 4：线上完整依赖验证、部署与发送结果检查

## 约束
- 禁止让模型重排卡面字段；排名、数据和 URL 必须来自结构化 Modrinth 响应。
- 默认榜单不追加每项详情 API，避免前五变为五次额外网络请求。
- 卡图失败时仍发送完整文本节点和卡外 URL，不能丢失排名结果。

---

# 2026-07-21 B站会员高清搬运

## 目标
完成服务器端 B站二维码授权、按群白名单的高清搬运和显式清晰度选择；自动搬运默认匿名最低清晰度，但 B站明确返回试看或登录限制时会用已授权账号复试完整资源。

## 阶段
- [x] 阶段 1：恢复现有二维码授权半成品，确认 Cookie 与自动搬运尚未连通
- [x] 阶段 2：补齐白名单配置、主人管理命令与显式高清搬运命令
- [x] 阶段 3：将授权 Cookie 限定传入播放解析和媒体下载，补定向测试
- [x] 阶段 4：本地回归、线上部署、授权与服务状态验证

## 约束
- Cookie 只存服务器权限为 0600 的授权文件；不得写入日志、模型上下文、Redis 作业、卡片或回复。
- 只有主人或当前群白名单成员可执行 `.高清搬运`；群管理员不自动获得会员权限。
- 自动搬运与历史记录搬运维持匿名 `qn=6`，不使用授权 Cookie。
- 普通自动搬运保持匿名；只有匿名播放结果明确为试看或要求登录时才可进行一次授权复试，且仍请求最低 `qn=6`。

# 2026-07-21 自定义骰娘规则包 V1

## 目标
按 `docs/dice-rules/骰娘自定义规则接入指南.md` 契约实现可导入 YAML 的有状态自定义骰娘：受限表达式、派生字段、自定义骰、随机表、条件分支、人物卡写入、显示名称、动态命令、版本/群启停/回滚/导出和精确校验错误。

## 阶段
- [x] 阶段 1：核对现有骰娘路由、状态、权限与 YAML 依赖，冻结 V1 边界
- [x] 阶段 2：实现规则 schema、表达式 AST、运行时与原子状态写入
- [x] 阶段 3：实现规则包版本仓库、导入预检/确认/启停/回滚/导出
- [x] 阶段 4：接入动态命令和规则包人物卡操作，补配置/帮助
- [x] 阶段 5：以教程三个样例和错误场景完成定向、全量及静态验证

## 约束
- 不使用 `eval`、`Function`、任意脚本、文件/网络访问或跨用户状态写入。
- 内置骰娘命令始终优先；动态入口未匹配时返回 `false`，不吞掉其他插件命令。
- 导入、升级与写卡必须原子化；失败不修改活动版本或人物卡。
- 规则包源 YAML、规范化 JSON、不可变版本和群活动引用分离存储。

## 错误记录
- 首次教程样例 schema 校验为 2/3：COC 示例的 action `value: 1` 被错误要求为字符串。表达式入口已兼容数字/布尔字面量，三份样例随后全部通过。
- 路径可迁移/原子输出收紧补丁首次因 `getExportFile` 上下文位置不匹配而整包拒绝，未修改文件；已改为按实际函数位置拆分补丁。
- 收尾记录首次按摘要中的章节标题追加失败；改用规划文件实际锚点后成功，业务文件未受影响。
- CQ/最长别名补丁首次混入错误的 schema 定位行而整组拒绝；拆为正确文件上下文后应用并通过语法与测试。
- 全量测试唯一失败为既有 UmaRace 测试无法导入本机缺失的 TRSS Puppeteer 文件；相关回归和插件加载测试单独复跑通过，未修改无关 UmaRace 代码。

## 阶段 5 当前证据
- 最终相关回归 26/26：覆盖三份教程 YAML 的预检/确认/启用/实际执行、表达式沙箱、导入确认、群隔离、回滚、迁移、中文句号、引用 YAML 下载、64 KiB 限制、运行时版本、保留子命令、全局骰数继承、临时字段、身份 fallback、最长别名、前缀冲突、插件导出加载，以及待确认/已确认文件篡改拒绝。
- 源 YAML 与规范化 JSON 均记录并复验 SHA-256；历史无规范化哈希记录仍通过源 YAML 重新规范化比对。
- 教程已改为实际 V1 文档，并明确私聊、惰性迁移、导出版本和归档删除边界；主 README 已增加入口。
- 全量 `npm test` 共 535 项：511 通过、23 因本机缺 TRSS/Chromium 运行依赖跳过、1 个既有 `umaRaceBalance` 文件级失败；唯一失败是缺少 `/Users/shiqi/lib/puppeteer/puppeteer.js`，不在本轮改动路径。新增骰娘测试在全量中全部通过。
- 所有新增/修改 JavaScript 通过 `node --check`，`git diff --check` 通过，`npm ls yaml --depth=0` 确认直接依赖 `yaml@2.9.0`。

## 验证
- `node --check` 覆盖授权、播放、下载与命令文件；默认 YAML 可解析。
- B站定向测试 18/18 通过，覆盖 `86101/86090/86038`、最小权限文件、主人/群白名单、授权 `qn`、实际降档、Cookie 仅到播放与下载 header。
- 线上根目录 `npm install` 因既有 `link:lib/modules/lodash` 工作区依赖拒绝，未使用该路径；既有 `qrcode` 可直接导入，插件目录既有 B站回归 14/14 通过。
- 线上安全重启后发现 macOS 打包带入的 `._*` AppleDouble 文件被 TRSS 误作插件；已立即删除并二次安全重启。最终 PID=893400、active、34 个插件、当前 PID 无加载错误、OneBot 已连接、配置已合并。

## 错误记录
- 首次并行 `scp` 触发服务器 SSH 临时认证拒绝，只有已哈希确认成功的文件保留；改为每批不超过 4 个连接、逐文件同步并在重启前完整校验。
- 首次独立渲染探针未模拟 TRSS 注入的 `logger`，renderer loader 在加载前抛出 `ReferenceError`；需补最小全局后重试，不能把此错误归为 HTML 模板渲染失败。
- 补 logger 后独立探针继续暴露 renderer 对全局 Redis 和 logger 方法的依赖；这类裸 Node 进程不能等价模拟 TRSS renderer。已停止继续伪造运行时，改用生产服务正常启动后的真实 Modrinth 请求验收。

---

# 2026-07-22 自定义骰娘规则包 V2 团务与权限

## 目标
保持固定点命令作为唯一执行入口，不接入 Agent 或自然语言工具路由；为自定义规则包增加多角色目标、NPC/群共享状态、声明式权限、暗骰/私密结果、对抗与群体动作、团录结构化审计，以及回合、先攻、持续状态、资源和声明式事件，并修复 V1 审计发现的文档/实现和并发可靠性问题。

## 阶段
- [x] 阶段 1：盘点现有人物卡、团录、暗骰、先攻和状态持久化接口，冻结 V2 兼容契约
- [x] 阶段 2：实现角色选择、状态作用域、权限判定与安全的跨角色动作
- [x] 阶段 3：实现暗骰、对抗、结构化骰点记录及现有 `.log` 接入
- [x] 阶段 4：实现回合/先攻/持续状态/资源与声明式事件模块
- [x] 阶段 5：修复保留 ID、队列分区、缓存/存储一致性等已知缺陷，并更新完整教程
- [x] 阶段 6：补充单元、端到端、迁移、并发和安全回归，运行全量验证

## 约束
- 只允许固定 `.规则前缀 子命令` 及明确管理命令触发；不注册 Agent Skill，不做自然语言猜测。
- 默认只能写当前人物卡；跨角色、NPC、群共享状态必须由规则命令声明目标和权限，运行时再次授权。
- 不开放任意 JavaScript、文件/网络访问、CQ 码、无权限跨用户写入或任意迁移函数。
- V1 规则包继续可导入、启用和执行；V2 新区块必须是可选、可校验的声明式模块。

## 错误记录
- 首次追加本阶段时使用了被工具输出截断的 V1 错误行作为补丁锚点，`apply_patch` 整包拒绝且未修改文件；改用规划文件真实末尾锚点追加。
- pending 文件清理补丁首次把方法签名写成 `resolveRulePath(relative)`，实际代码是 `resolveRulePath(file = "")`，整包拒绝且未修改；按真实上下文重新应用。
- V2 schema 首轮定向回归 18/20；两项均因官方 COC 示例使用参数 ID `target`，而新校验把所有嵌套 ID 都误当顶层命名空间。修正为仅对字段、模板、骰组等命名空间对象执行保留名检查，参数仍可安全使用 `arg.target`。
- 收窄后第二轮仍为 18/20：官方状态机示例已有字段 `status`。确认所有内部 ID 都通过 `attr.status`、`arg.target` 等限定路径访问，不会覆盖顶层上下文；最终决定修正教程的错误禁用说明，不新增破坏 V1 的限制。
- V2 端到端首轮 0/4，四项同源：导入预检仍按 V1 只构造 attr，合法的 `inventory.sword.equipped` 在 sample 阶段缺上下文，首项预检失败导致其余三项没有 pending 包。已补 actor、target、inventory、shared、session、opposed 的无副作用预览上下文。
- V2 第二轮 3/4；剩余失败在 `turn_start` 群共享 action 后，刷新上下文时误把 group draft 当角色 target，并用人物卡派生公式计算群字段，触发 `attr.hp` 缺失。已让 group action 刷新 shared 而不替换角色 target。
- 修正 group target 后先攻可启动，但单人先攻的回合结束和新轮开始分别加载同一 NPC 草稿，后者覆盖前者的中毒扣血。已改为整个回合推进事务按 `kind:id` 复用角色草稿。
- 新增第四份教程示例后定向回归首次为 29/30；失败来自旧测试把示例数量硬编码为 3，不是 YAML 校验失败。已改为断言至少保留三份 V1 示例和一份 V2 团务示例。
- 教程旧文案反查命令把搜索式中的反引号交给 zsh，意外尝试执行不存在的 `turns` 命令；没有发生文件修改。后续反查改用不含反引号的模式。

## 当前证据
- 最终骰规则定向回归 34/34；四份教程示例均通过严格 schema、预检、启用和第一条命令执行。
- V2 已覆盖：群成员和 NPC 目标、显式跨角色 action、GM/管理员权限、直接及派生 secret 字段访问控制、共享资源、团务自动 log、多角色先攻/轮次、状态自动 tick/expire、物品装备、技能法术/冷却、GM 私密结果、数值对抗、结构化审计、同群及跨群状态一致性、stale lock、归档恢复失败回滚。
- `npm test` 共 547 项：523 通过、23 跳过、1 失败；唯一失败为本机缺少既有 `/Users/shiqi/lib/puppeteer/puppeteer.js`，导致 `tests/umaRaceBalance.test.js` 文件加载失败，与本轮骰规则代码无关。
- 所有本轮相关文件 `node --check` 和 `git diff --check` 通过；仓库内没有残留 `.tmp` 或 `.lock`。未部署、未重启、未提交或推送。

---

# 2026-07-23 全局表达总结推理额度修复

## 目标
修复 `.表达学习 总结` 使用推理模型时，completion 额度全部消耗在 reasoning、最终 `message.content` 为空或 JSON 被截断的问题；同时避免自动总结失败后被每条新消息反复触发。

## 阶段
- [x] 阶段 1：从生产日志和脱敏响应元数据确认第一失败原因
- [x] 阶段 2：实现模型无关的输出截断识别、紧凑重试和明确错误语义
- [x] 阶段 3：实现自动总结失败退避并补单元回归
- [x] 阶段 4：本地与生产真实目录验证，使用线上同模型无落库探针确认
- [x] 阶段 5：备份部署、安全重启并核对真实命令链路

## 约束
- 不绑定 DeepSeek 或固定模型名；继续使用 `memoryAiConfig` 的可配置 URL、模型和密钥。
- 不把 `reasoning_content` 当最终规则解析，也不把模型思考内容写入日志或群消息。
- 只有拿到完整、可解析的最终 JSON 后才能更新表达规则；失败和重试不得部分落库。
- 保留用户现有工作区其他未提交改动，规划文件不进入业务提交。

## 第一失败证据
- 2026-07-23 15:57 的手动命令 HTTP 200，但响应 `finish_reason=length`、`completion_tokens=900`、`reasoning_tokens=900`、最终 content 长度为 0。
- 当前代码固定 `max_tokens: 900`，只读取 `choices[0].message.content`，因此把额度耗尽误包装为“不是可解析的规则 JSON”。
- 生产日志同日还出现多次截断 JSON，且自动总结失败后几十秒内连续重试，说明还缺失败退避。

## 错误记录
- 首次生产强制回退探针把第一轮额度设为旧值 900，成功触发第二轮，但第二轮 6000 token 仍复用 30 秒超时并被上游中止；正式规则未写入。修正方向为更紧凑的 4+4 条规则、4000 token 和独立 60 秒重试超时，并把 DOM timeout 归一为可理解的业务错误。

## 当前证据
- 新增总结回归覆盖推理耗尽、坏 JSON、双重失败原子性和自动失败退避；表达学习相关测试当前 15/15 通过。
- 首次请求默认 2400 token；完整性重试默认 6000 token、最多 20 条样本。参数均可在锅巴配置，不依赖模型厂商判断。
- 本地全量 530 通过、23 跳过、1 个既有 Puppeteer 环境失败；本轮无新增失败。
- 生产真实目录相关回归 16/16；强制 900 token 首轮失败后自动以 4000 token、20 条样本和最多 4+4 条规则重试成功，证明真实模型回退链路有效。
- 最终生产 4/4 文件哈希一致，服务 PID 1586632 active，加载 34 个插件，OneBot/NapCat 已连接，HTTP 2536 返回 302。

---

# 2026-07-23 生图比例与像素规格解耦

## 目标
让用户声明的 `1024×1024` 只表示 1:1 画面比例与渠道输出档位，不进入绘画语义、不参与人物位置/大小计算，也不用于强制拉伸或裁剪参考图；最终发送和记录以渠道实际返回尺寸为准。

## 阶段
- [x] 阶段 1：追踪尺寸从用户文本、工具参数、提示词编译、生成/编辑渠道到返回处理的完整链路
- [x] 阶段 2：建立统一的 aspect ratio 与 provider size 解析/映射边界
- [x] 阶段 3：保持提示词原文、解除图生图固定正方形尺寸并记录实际产物宽高
- [x] 阶段 4：完成本地定向回归；按用户要求不部署、不重启线上服务
- [x] 阶段 5：按用户最新要求保留线上运行态不变；不部署、不重启，真实载荷验收留到下次上线窗口
- [x] 阶段 6：按 2026-07-24 最新授权同步比例修复到生产文件；完成哈希与静态加载验证，但严格不重启现有进程

## 约束
- 不删除用户真正的构图语义；例如“正方形构图”“竖版 9:16”应保留为比例意图。
- `1024×1024` 等像素规格只进入渠道请求适配层，不作为自然语言提示词片段。
- 图生图不得为了匹配请求 size 改变参考图宽高比；如渠道要求固定档位，只做等比适配或由渠道处理。
- 渠道返回尺寸可能与请求档位不同，结果记录、回复和后续编辑必须读取实际图片元数据。
- 保留工作区其他未提交改动，规划文件不进入业务提交。

## 当前证据
- 线上两个文生图 provider 都是 `gpt-image-2` 且 size 固定为 `1024x1024`；生成 payload 无条件复制该值。
- `bananaTool` 只有 prompt/images/provider 参数，没有结构化 aspect ratio；用户写 `9:16` 时提示词虽保留，但 API 仍收到正方形 size。
- 当前参考图只转成 multipart 文件，没有本地 resize/crop；当前主要变形来源是请求输出画布固定，不是插件预处理参考图。
- 当前生成响应只保留 URL/base64，没有读取实际图片宽高，后续无法区分请求档位与真实产物尺寸。

## 实施结果
- 新增统一比例解析器：支持 `9:16/16:9/1:1`、像素表达和竖版/横版/正方形；像素表达只归类方向，不作为精确输出尺寸。
- `bananaTool` 新增可选 `aspectRatio`，执行层同时从用户原话确定性兜底；每个 fallback provider 独立映射自己的 square/portrait/landscape 档位。
- GPT Image 默认映射为 1024x1024、1024x1536、1536x1024；其他渠道可在配置面板覆盖，未知渠道不会把冲突的固定正方形继续发送。
- 图生图未指定比例时不再继承文生图的固定 `1024x1024`；显式 edit 默认或 auto 配置仍可保留渠道兼容性。
- 生成提示词原文未改写；发送前从实际图片字节读取宽高并记录请求方向与真实产物尺寸。
- 本地定向回归 61/61；首次面板回归发现空覆盖字段污染旧配置并已修复，首次时间歧义回归发现 `12:30` 邻近“横版构图”误识别并已收紧为比例上下文。

---

# 2026-07-23 图片渠道参数信任边界修复

## 目标
禁止语义模型或工具说明凭空生成图片 provider 并冒充用户明确指定；只有用户原话中存在可验证的渠道选择表达时才启用严格渠道锁定，否则丢弃模型生成的 provider，按现有已配置渠道正常执行。

## 阶段
- [x] 阶段 1：用线上真实日志确认 `provider=gemini` 来自工具决策而非用户原话或配置
- [x] 阶段 2：实现共享的用户授权 provider 归一化，并去除工具描述中的厂商暗示
- [x] 阶段 3：覆盖 Google 编辑、Banana 生成/编辑、持久任务和显式渠道锁定回归
- [x] 阶段 4：本地定向与全量验证；按用户已有约束不部署、不重启

## 约束
- 用户明确说“用 Grok/Gemini/某渠道”时仍必须严格锁定，失败时不得偷偷换渠道。
- 用户没说渠道时，模型生成的 provider 不具有授权，必须在执行边界丢弃。
- 规则应复用于所有图片入口，不能只对字符串 `gemini` 打补丁。
- 保留工作区既有未提交修改；不部署、不重启线上服务。

## 错误记录
- 2026-07-24 首轮生产 BananaTool 测试从插件目录启动，TRSS 宿主配置使用相对路径而找不到 `config/default_config/`，导致 12 项均在模块初始化前失败；41 项纯模块断言已通过。改为从 `/opt/trss-yunzai` 宿主根目录重跑。
- 2026-07-24 并行 SCP 五个线上临时测试文件时有一条密码连接被服务器拒绝；业务部署文件未受影响。后续改为单连接核对缺失项并补传，避免并发认证触发限制。
- 2026-07-24 首次打包命令因包含 `rm -f` 被安全策略拒绝，未创建或修改业务文件；改用唯一时间戳归档名，不再预删路径。
- 首次搜索 provider 测试调用时，rg 模式中的反引号落入 zsh 双引号并造成 `unmatched \"`；未修改业务文件，后续使用不含反引号的单引号模式。
- 第二次尝试混合 shell 单双引号仍触发相同 `unmatched \"`；改为先搜索普通 `provider:` 再管道筛选名称，第三次成功，业务文件未受影响。
- 首次追加 provider 否定语义时把测试文件锚点误放进工具文件补丁段，`apply_patch` 整包拒绝且未修改；拆分为各文件真实锚点后重试。
- 服务器临时副本第一次验证时，批量 SCP 中工具文件连接被关闭，导致纯解析代码已更新但两个工具类仍是旧哈希，类级断言失败；核对哈希后逐文件补传，第二次真实依赖验证全部通过，线上插件目录和进程未改变。

## 当前证据
- 共享解析、图片回归和 Tool Skill 定向测试 51/51；扩大图片回归 51 通过、19 项仅因本机缺 TRSS 宿主依赖跳过、0 失败。
- 服务器临时副本使用真实 TRSS 依赖验证：共享解析 26/26，Google/Banana 类级信任边界 2/2；验证后临时副本已删除。
- 本地全量 `npm test` 共 566 项：540 通过、25 跳过、1 失败；唯一失败仍是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本轮图片渠道断言无失败。
- 线上 `trss-yunzai.service` 保持 active，PID 始终为 1586632；未同步生产插件目录、未部署、未重启。

---

# 2026-07-23 Steam 图片识别超时诊断与修复

## 目标
还原“图片我收到了，但这次识图等了太久仍没返回结果”对应的线上请求，区分图片下载、主识图渠道、备用渠道和总超时的第一失败原因；修复可泛化的问题，并让最终失败信息保持自然、真实且足够可定位。

## 阶段
- [x] 阶段 1：从生产日志还原最新请求及各阶段耗时和错误类别
- [x] 阶段 2：核对识图 provider 配置、超时边界、fallback 与 ToolOutcome 传播链路
- [x] 阶段 3：实现第一失败修复及自然错误反馈，补回归
- [x] 阶段 4：本地与服务器隔离验证；遵守当前不重启约束

## 约束
- 不根据 Steam 常识或群历史补造图片内容。
- 不在日志、回复或测试输出中泄露图片临时 URL、密钥或图片内容。
- 优先修复共享识图链路，不为 Steam 单独增加关键词分支。
- 保留工作区其他未提交修改；未经用户明确改变约束，不部署、不重启。

## 第一失败证据
- 18:58 的真实请求已成功下载 1830x324、28 KB 截图，下载仅约 0.13 秒；旧识图端点在 25.97 秒后超时。
- 19:02 线上配置被热更新为 SDK `base_url=https://api.krill-ai.com/codex/v1`；GoogleAnalysisTool 将其当最终地址直接 POST，隔离探针稳定得到空 HTTP 404。
- 同一 SK、模型、图片和提示将地址规范化为 `/codex/v1/chat/completions` 后，5.16 秒内 HTTP 200 并返回有效内容，证明 SK 与模型可用，根因是 base URL 未适配。
- 原失败回复在共享 ToolOutcome 文案中硬编码 Steam，导致 Minecraft 等其他图片问题也会串题。

## 错误记录
- 首次业务补丁因附带的 `findings.md` 章节锚点不存在而整包拒绝，未修改任何文件；拆出真实业务文件后补丁成功。
- 首轮新回归 5/7；两项仅是旧测试仍断言旧机械文案，实际输出已按要求改成自然且不串题的“不乱猜”，更新语义断言后继续验证。
- 首次热改生产超时使用了不符合真实 YAML 块格式的正则，差异为 0、配置未变化；随后按解析确认的字段位置精确替换并验证只有 timeout 一行变化。

## 当前证据
- 当前线上识图 URL 已热修为 `https://api.krill-ai.com/codex/v1/chat/completions`，同一真实截图在当前配置下 HTTP 200；SK 鉴权正常。
- 当前线上识图 timeout 已由 25000ms 调整为 45000ms；两次配置修改均有独立备份并核对只变化目标行，无需重启即可由现有工具下一次调用读取。
- 本地相关回归 14 通过、1 项因本机缺 `moment` 跳过、0 失败；服务器真实依赖临时副本 10/10，GoogleImageAnalysisTool 类加载成功，临时副本已删除。
- 全量 `npm test` 共 568 项：542 通过、25 跳过、1 失败；唯一失败仍是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本轮新增断言无失败。
- 线上 `trss-yunzai.service` 保持 active，PID 仍为 1586632；未重启，业务代码未同步生产目录。

---

# 2026-07-23 长任务进度回复拟人化

## 目标
消除“我还在查，结果出来就发。”这类脱离当前语境的固定状态模板；长任务需要进度提示时，由当前可配置对话模型根据用户原话、任务类型和已完成阶段生成一条简短自然回复，模型不可用时保持安静并等待最终结果。

## 阶段
- [x] 阶段 1：定位所有固定进度文案、触发计时器和发送入口
- [x] 阶段 2：设计有超时和缓存边界的上下文进度生成器，避免拖慢主任务
- [x] 阶段 3：接入搜索延迟进度链路并补回归；即时图片确认保持现有独立语义
- [x] 阶段 4：本地与隔离环境验证；遵守当前不重启约束
- [x] 阶段 5：按用户最新授权部署并安全重启，完成线上运行态验收

## 约束
- 进度模型只能基于当前请求和确定的执行阶段，不得假装已经找到结果或编造百分比。
- 生成进度提示不得阻塞或延长实际工具任务；短超时、无模型或两次生成均不合格时保持静默，不允许退回另一句固定模板。
- 继续使用可配置模型路由，不绑定某个厂商或模型名。
- 不让每个工具各自维护一套相似模板，优先收敛为共享能力。

## 错误记录
- 最终只读核对命令把含反引号的搜索文本放进 shell 双引号，意外触发了一次额外 `npm test` 并使随后 `rg` 表达式报错；未修改业务文件，额外测试结果与上一轮相同，后续搜索改用单引号。

## 当前发现
- 精确文案“我还在查，结果出来就发。”只存在于 SearchInformationTool 的 2.5 秒延迟计时器；图片工具的固定句是任务开始前即时确认，不是本次中途突兀插话的来源。
- 首轮已新增共享上下文进度生成器：优先使用工具规划模型给出的 `progressText`，否则走可配置 `taskAiConfig.progress`，未配置时复用 chatAiConfig；有独立短超时、输出事实守卫和调用方取消信号。
- 搜索完成会同时取消尚未发出的模型进度回复，避免结果已返回后再补“还在查”。
- 模型不可用或输出不合格时不发送任何中途模板，保证出现的进度回复一定来自当前话题的模型判断。
- 本地定向回归 13/13；服务器真实依赖隔离回归 10/10。线上配置探针最终生成“最近更新的魔法模组有点多，我再顺一遍排序。”和“国标里红灯绿灯的波长区间，我再核实一下。”，均未编造结果或额外条件。
- 本地全量 `npm test` 共 578 项：552 通过、25 跳过、1 失败；唯一失败仍是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本轮进度回复测试全部通过。
- 2026-07-23 已与识图开场修复一起部署；生产目录目标回归 15/15，真实配置无发送探针生成“魔法模组的前五，我把更新时间再核对一遍。”。

---

# 2026-07-23 图片识别开场拟人化

## 目标
移除“嗯嗯，我先看看这张图。”等随机固定识图开场；由模型结合用户实际想看什么生成一句自然接话，并与图片下载/识别并行执行。没有合格模型输出时保持安静，识图完成时取消迟到开场。

## 阶段
- [x] 阶段 1：定位固定文案、发送入口和阻塞关系
- [x] 阶段 2：复用共享上下文进度生成器，接入图片识别并发与取消
- [x] 阶段 3：补固定话术、上下文生成、静默降级和完成竞态回归
- [x] 阶段 4：本地及服务器隔离验证；遵守当前不重启约束
- [x] 阶段 5：按用户最新授权部署并安全重启，完成线上运行态验收

## 约束
- 只能基于当前用户原话、识图任务和真实阶段接话，不得猜图片内容或声称已识别到结果。
- 进度生成不能位于图片下载和识图的串行关键路径。
- 不再维护另一组随机固定句；模型不可用或输出不合格时直接等待最终识图结果。
- 继续复用可配置进度模型路由，不绑定厂商或模型名。

## 当前发现
- 固定句来自 `GoogleAnalysisTool.js` 的四句随机数组，不经过主回复模型或 persona。
- `func()` 当前 `await sendProgress(e)` 后才开始规范化、下载和调用视觉模型，固定开场本身还会增加识图串行延迟。
- 已有共享 `contextualProgressReply` 能处理可配置模型、事实守卫、取消和静默降级，图片识别应复用而不是再造一套文案。
- 最终移除全部四句随机识图开场；工具规划模型可直接提供 `progressText`，否则共享进度模型根据当前用户原话生成。没有合格输出时静默，不使用固定兜底。
- 图片处理与进度生成并行，工具所有完成/失败出口通过 `finally` 中止迟到接话；服务器行为测试证明永久挂起的进度 Promise 不会阻塞图片下载失败返回。
- 识图前事实守卫会拒绝用户未提及的报错、文字、按钮、界面、人物、颜色或卡住状态，并对“还在对/还在看”等生硬状态句要求重写。
- 线上配置隔离探针最终生成“Steam安装这块，我仔细看看问题在哪儿。”和“错误码得看清具体是哪一个，我仔细辨认下。”；未向群发送消息。
- 本地全量 `npm test` 共 583 项：553 通过、29 跳过、1 失败；唯一失败仍为既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`。服务器真实依赖类级与并发回归 4/4。
- 生产已备份为 `/opt/trss-yunzai-backups/bl-chat-contextual-progress-20260723-205024.tar.gz` 并部署 4 个精确业务文件；旧固定识图/搜索开场已从对应生产工具文件移除。
- 安全重启退出码 0，PID `1586632 -> 1651465`；34 个插件加载完成，OneBotv11/NapCat 已连接，2536 端口由新进程监听，启动后无模块/语法/加载错误。
- 生产目录直接回归 15/15；真实配置无发送探针生成“图里的错误码我仔细辨认下具体是哪一串。”。staging、临时测试和 AppleDouble 均已清理，图片任务为 0。
- 用户已于本轮明确要求“部署重启”，此前不重启约束被本次明确授权覆盖；部署仍只包含搜索/识图进度链路的必要业务文件。

## 错误记录
- 服务器临时副本首次从插件目录启动测试，TRSS 依赖按相对路径找不到 `config/default_config/`；改从 `/opt/trss-yunzai` 根目录启动后业务断言通过。
- 服务器完整组合测试因宿主依赖保留后台句柄未自然退出；中断后改用 Node `--test-force-exit` 运行目标文件，最终 4/4 完整收口。

---

# 2026-07-24 文生图上下文污染隔离

## 目标
让文生图只使用当前绘图请求和确实被用户承接的必要画面信息；“上午/下午”等当前画面修饰词正常保留，无关群历史不得进入最终绘图 prompt。

## 阶段
- [x] 阶段 1：还原当前消息、上下文衔接、语义决策到 BananaTool 最终 prompt 的全部入口
- [x] 阶段 2：建立当前绘图请求与可证明承接上下文的边界，修复共享构造链
- [x] 阶段 3：补当前时段保留、相关上下文承接、无关历史隔离和队列持久化回归
- [x] 阶段 4：完成本地及线上磁盘隔离验证；未经本轮明确授权不重启
- [x] 阶段 5：按用户最新授权安全重启生产服务，验证新进程加载、连接与回归

---

# 2026-07-24 骰娘完整性审计

## 目标
从实际跑团用户和维护者视角审计固定骰点、COC、人物卡、自定义规则包 V1/V2、团务、权限、持久化、帮助与文档，给出按影响排序的缺口清单；本轮只分析，不修改业务代码。

## 阶段
- [x] 阶段 1：建立入口、命令、状态、权限与文档功能矩阵
- [x] 阶段 2：审计固定骰娘和人物卡的用户流程与数据边界
- [x] 阶段 3：审计自定义规则与团务 V2 的可用性、权限和可靠性
- [x] 阶段 4：运行定向测试并核对线上启用配置/运行态
- [x] 阶段 5：按严重度输出缺口、根因和建议优先级

## 约束
- 本轮只读审计，不修改骰娘业务代码、不部署、不重启。
- 跑团命令保持固定语序触发，不把自然语言 Agent 化当作缺陷。
- 区分代码能力、文档声明、线上配置和真实用户入口。
- 保留工作区所有既有修改。

## 约束
- 不用关键词黑名单删除“上午/下午”等正常绘图内容。
- 不让模型自行复制整段群历史进 prompt；只有当前消息明确省略或指代前文时才允许补充最小必要上下文。
- 不改写用户当前绘图描述，不引入所谓提示词自动修复。
- 保留工作区其他未提交修改。

## 错误记录
- 只读 SSH 的 BatchMode 因服务器未配置密钥登录而失败；改用交互式密码登录后成功。首次输入意外带尾随空格被拒，第二次精确输入成功，未触发任何线上写操作。
- 生产隐藏应用副本上传成功后，紧接着上传临时测试文件的密码连接再次被服务器拒绝；正式应用尚未覆盖，运行进程未受影响。测试改为支持环境变量指定隐藏应用模块，后续降低连接频率后单次补传。
- 首次读取生产绘图合并日志时 SSH 密码连接被服务器临时拒绝；没有修改线上状态。代码级链路已能确定第一根因，后续生产验证改为低并发单连接执行。

---

# 2026-07-24 “樊思睿是谁”回答失败诊断

## 目标
还原点名提问“樊思睿是谁”为什么进入通用回答失败兜底，区分模型请求、上下文/成员资料、记忆工具和发送层；本轮只诊断，不修改业务代码、不重启。

## 阶段
- [ ] 阶段 1：定位固定失败文案及调用链
- [ ] 阶段 2：核对线上对应请求的第一异常与耗时分段
- [ ] 阶段 3：判断是否与成员资料/记忆能力有关并给出修复边界

## 约束
- 不把通用兜底文案当作根因。
- 没找到人物资料只能如实说没找到，不能触发或解释成回答服务失败。
- 不修改线上状态，不向群发送测试消息。

## 状态
- 用户已中止本诊断，改为优先修复前述骰娘问题；阶段 2-3 不再继续，本轮未查询线上对应请求。

---

# 2026-07-24 骰娘完整性修复

## 目标
修复完整性审计中已证实的安全、权限、假成功、状态事务、团录、迁移与核心规则缺陷；保持固定命令语法，不接入自然语言 Agent。完成本地回归和文档更新，未经用户明确授权不部署或重启线上。

## 阶段
- [x] 阶段 1：建立共享命令网关、总开关、回复开关、权限和统一错误边界
- [x] 阶段 2：修复人物卡锁定、成长落卡、时区/SAN、自动名片与状态安全存储
- [x] 阶段 3：修复 log 文件交付、历史、团务/log 原子性和先攻状态机
- [x] 阶段 4：修复 V2 secret 可见性、成员边界、私密投递、完整审计和安全迁移/回滚
- [x] 阶段 5：补规则能力缺口、锅巴配置、教程与完整定向/全量回归

## 约束
- 保留中英文句点、紧贴/空格两种固定命令语法。
- 不把固定跑团命令改造成 Agent 或自然语言自动执行。
- 不覆盖工作区现有非骰娘修改；新增行为必须有回归。
- 安全失败必须如实，不再返回“已锁定/已关闭/已导出”等假成功。
- 未经本轮明确授权不部署、不重启线上服务。

## 最终验证
- 骰娘定向回归：57/57 通过。
- `apps/DicePlugin.js`、骰娘核心/规则运行时文件均通过 `node --check`；全工作区 `git diff --check` 通过。
- 全量 `npm test`：623 项中 589 通过、33 因当前 checkout 缺宿主依赖而跳过、1 项既有 UmaRace 文件级失败；唯一失败是本机缺少 `/Users/shiqi/lib/puppeteer/puppeteer.js`，不在骰娘路径。
- 本轮只完成本地实现与验证；未部署、未重启、未提交或 push。

---

# 2026-07-25 群知识转述回复 AI 感审计

## 目标
还原“告诉斯卡蒂，沃基的教派是什么”为什么先出现泛化搜索进度、再生成带模板腔和无证据尾巴的回复；从进度提示、工具路由、群知识证据、点名转述、最终提示词和发送形态全链路检查，并扫描其他同类入口。本轮只分析，不改业务代码或线上状态。

## 阶段
- [x] 阶段 1：定位示例消息在线上日志中的真实调用链和证据来源
- [x] 阶段 2：审计进度提示、群知识、成员艾特与最终回复的组合边界
- [x] 阶段 3：全仓扫描相同固定句式、无证据发挥和重复表达控制缺口
- [x] 阶段 4：按用户视角给出根因、影响范围与系统性修复建议

## 当前发现
- 线上 `2026-07-25 09:30:39` 的真实请求位于群 `953676639`，总耗时 18764ms；语义分类器耗时 6868ms，并以 `confidence=0.91` 强制选择 `searchInformationTool`，理由是“较冷门的事实信息，需要搜索核实”。
- 分类器同时生成 `progressText=正在搜索“沃基的教派”相关信息`；共享进度守卫拒绝“正在查/查询/检索”却漏了“正在搜索”，因此这句模型生成的模板腔原样发出。
- 搜索结果把“沃基”误解为英文 `Woke`，并明确说不是宗教教派；最终模型没有依据该结果回答，而是从固定人设/身份绑定/长期记忆取出“福尚愈足、沃基是教主”，又从上一条群聊取出“逆天主教”补成玩笑。
- 最终请求虽然附有“只能陈述本轮工具结果事实”的 grounding 规则，但同一请求的更早 system prompt、人设、长期记忆和历史仍保留互相冲突的信息，实际未形成可执行的数据边界。
- 本轮没有调用 `mentionMembersTool`；最终模型输出文本 `@3671201171`，由发送层转换成真实艾特。问题在规划/生成边界，不在艾特发送工具。
- 表情包策略把所有不超过 60 字且未命中排除项的消息标为 `casual_conversation`；这个候选会启动前置 Planner，而 Planner 看到全部注册工具。最终按消息意图过滤工具发生在 Planner 之后，导致本应只开放表情包的普通群聊可被误选为搜索或其他工具。
- Planner 输入不包含已经注入主模型的身份绑定、群知识和长期记忆，所以它无法知道“沃基”是群内实体、内部已经有答案；当前没有“内部高权威证据命中后禁止外搜”的裁决层。
- 搜索工具把任意非空模型文本当成功结果；通用 `classifyToolResult()` 也把非空文本视为 success，没有查询实体、结果实体、来源或语义一致性校验。
- 转述请求没有独立的 `recipient + proposition` 表达计划；模型自由组织时会重复称呼、产生代词歧义，并把上一句群友玩笑追加为无依据尾巴。最终清理器只做固定短语替换，无法发现这些语义问题。
- 终态工具和工具内直接发送仍是同类旁路：`mentionMembersTool`、`mentionAdminsTool` 的自由 `message` 参数会直接发送，搜索/绘图进度及部分媒体工具也直接 `reply/sendMsg`，不经过统一的事实与表达验证。
- 近 77 次语义分类中有 39 次最终只是 chat；普通 chat 分类平均约 5391ms、P95 约 7337ms。工具结果又固定增加复杂度 4 分，会让简单转述在误调工具后继续升档慢模型。

## 修复优先级
- P0：让 Planner 只看到候选工具；表情包可用性不得启动全工具 Planner；内部身份/群知识命中时先完成证据裁决，默认不外搜。
- P0：把工具成功从“非空文本”升级为结构化结果，并对搜索做查询实体、结果实体和来源一致性校验；冲突证据在最终生成前裁剪，不能只追加 grounding 提示。
- P1：把点名转述结构化为收件人和命题，发送层负责真实 @，正文默认不重复昵称、不追加评价或承接上一条玩笑。
- P1：统一所有用户可见出口；终态工具的自由文案、进度提示和失败回复都通过同一事实/语气/重复检查，长任务才发送进度。
- P1：简单工具结果不应无条件触发慢模型；补群知识问答、转述、同名外部概念、上一句玩笑污染、重复 @ 和无依据尾句回归矩阵。

## 约束
- 区分模型原话、插件固定文案、工具结果和群知识事实，不凭回复表面猜来源。
- “AI 感”必须落到可复现的句式、提示词或调度机制，不能只做主观评价。
- 检查同类入口而非只为“教派”增加关键词补丁。
- 本轮只读，不部署、不重启、不向群发送验证消息。

---

# 2026-07-25 绘图开场任务语义修复

## 目标
修复“画群友”时后台自动使用群友头像作外观参考，却向用户谎称“图里的重点我看到了、开始改图”的问题；绘图开场必须由当前模型结合用户原话和真实任务类型生成，且不能阻塞实际出图。

## 阶段
- [x] 阶段 1：定位示例固定句及群友头像参考进入 BananaTool 的真实链路
- [x] 阶段 2：统一文生图、头像参考生图和真正图片编辑的进度语义与事实守卫
- [x] 阶段 3：补精确原话、错误任务类型、非阻塞/取消和既有绘图队列回归
- [ ] 阶段 4：完成本地定向与全量验证；不部署、不重启

## 当前根因
- `resolveAvatarDrawReference()` 正确识别“群里的翠月”并自动取得 QQ 头像；`getImageGenerationReferenceImages()` 将头像交给 BananaTool 作外观参考。
- BananaTool 仅按 `images.length > 0` 选择 `REFERENCE_IMAGE_PROGRESS_MESSAGES`，其中精确硬编码了“嗯，图里的重点我看到了，我先按你的要求改成一张新的。”。
- 内部参考图片不等于用户发来待编辑原图；任务仍是生成一张新图。当前实现同时把进度文案和内部 provider 操作都压成了 `hasReferenceImages` 一个布尔值，丢失了用户任务语义。

## 约束
- 不删除“画群友时按需使用头像参考”的既有能力。
- 不把真正的图片编辑说成文生图，也不把文生图/头像参考生图说成识图或改图。
- 模型不可用或两次生成均不合格时保持静默，不退回随机固定句。
- 进度生成与图片请求并行，图片任务结束后取消迟到进度。
- 本轮不部署、不重启线上服务。

---

# 2026-07-25 群 763694201 昨晚会话审计

## 目标
按北京时间还原群 763694201 在 2026-07-24 的夜间会话：先覆盖 18:00-24:00，并因机器人在 00:34 才入群而补查 00:00-06:00，完整关联希洛全部出站消息。区分普通聊天、命令插件、媒体搬运、进度提示和失败兜底，从用户视角分析参与时机、上下文理解、事实性和表达自然度。本轮只读，不改业务代码、不部署、不重启。

## 阶段
- [ ] 阶段 1：抽取完整群消息与全部机器人出站消息，确认时间范围和数量
- [ ] 阶段 2：逐条关联触发消息、引用/前后文、调度决策和工具/模型链路
- [ ] 阶段 3：归纳系统性问题、严重度和真实影响，区分正常不插话与异常漏回
- [ ] 阶段 4：给出证据化分析和修复优先级，不实施修改

## 约束
- “希洛说的话”以真实出站日志为准，不把内部 Planner 文本或其他群友发言算作回复。
- 同时检查群聊自然度和工具直发内容，标明各自归属。
- 不凭单句猜上下文；每条结论必须能对应到前后消息和运行日志。

---

# 2026-07-27 群聊会话链路系统性修复

## 目标
修复群 763694201 审计确认的系统性问题，并覆盖同类群聊：明确绘图请求必须进入图片工具；同一图片不得因 URL 变体重复计数；迟到的主动回复在话题推进后不得插入；群知识等已完成副作用不得被后续聊天模型失败改写成“整体失败”；Repeat、媒体、命令和 Agent 出站共享可观测、节奏与事实边界；普通短聊避免无价值的额外工具分类等待。

## 阶段
- [x] 阶段 1：为真实绘图语序、图片重复、迟到主动回复和副作用成功补失败回归
- [x] 阶段 2：修复意图优先级、素材身份归一化和短聊 Planner 快路
- [x] 阶段 3：实现发送前新鲜度校验与结构化动作结果优先回复
- [x] 阶段 4：收敛独立出站出口的统一治理和可观测性
- [x] 阶段 5：运行定向/全量回归，部署并在安全窗口重启验证

## 约束
- 保留现有表情包自主选择、主动参与、媒体合并转发和命令插件能力。
- 迟到校验只取消未点名的主动插话；明确点名、引用、命令和工具交付不得被误取消。
- 不用新关键词补丁代替能力注册与结构化状态；明确任务优先依据已注册工具能力。
- 所有用户已有未提交改动必须保留。

## 错误记录
- 一次只读远端 `node -e` 提取脚本末尾多写了 `}`，报 `SyntaxError: Unexpected token '}'`；未修改任何文件，后续按分段时间窗重跑，不重复原命令。

## 错误记录
- 一次只读 `rg` 把不存在的 `tests/index*` 交给 zsh 展开，出现 `no matches found`；未修改文件，后续改用明确测试文件名。
- 首次本轮线上 staging 从插件目录自身作为 cwd 运行图片工具测试，TRSS 宿主按 cwd 查找 `config/default_config/`，导致 17 项环境失败；改为从 `/opt/trss-yunzai` 根目录运行后图片队列、进度和取消测试全部通过。
- 隐藏 staging 最初仍位于 `plugins/` 下，TRSS 重启时把它识别成第二份插件，出现两次初始化和两套消息管道；已将 staging 移到 `/opt/trss-yunzai/backups/` 后再次安全重启，最终只初始化一次。

## 最终验证
- 本轮本地定向回归 46/46；完整 `npm test` 为 640 项，603 通过、36 因本机缺 TRSS/Chromium 宿主依赖跳过、1 个既有 UmaRace 文件级失败。唯一失败仍为缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，与本轮无关。
- 线上正式目录定向回归 46/46；真实 TRSS 根目录下上一轮图片进度、Banana 队列与 Google 图片编辑断言全部通过。
- 正式文件备份：`/opt/trss-yunzai/backups/shiloh-plugin-20260727-104000`；已验证 staging 移至 `/opt/trss-yunzai/backups/shiloh-plugin-staging-20260727-104000-validated`。
- 最终服务 `trss-yunzai.service` 为 active，`shiloh-plugin` 初始化 1 次，消息管道启动 1 次，启动日志无语法/模块/加载错误；NapCat OneBot 已连接并持续收到群消息，活动图片任务为 0。
- 本地运行图片工具级测试时有 15 项因 checkout 缺少 TRSS 宿主文件 `/Users/shiqi/lib/common/common.js` 跳过；共享进度层 10 项全部通过，工具级断言需在服务器临时 staging 复跑。
- 新增两条竞态断言后本地为 27 项：10 通过、17 因同一宿主依赖跳过；7 个相关文件 `node --check` 与全工作区 `git diff --check` 均通过。
- 服务器免密 SSH 探针返回 `Permission denied (publickey,password)`；下一次改用既有授权密码进入临时 staging，不重复免密尝试。
- 首次服务器 staging 位于 `/tmp`，`fileUtils.js` 按插件层级推导宿主根目录时解析成 `/lib/common/common.js`，导致工具级 17 项继续跳过；改用 `/opt/trss-yunzai/plugins/` 下独立隐藏 staging，保持真实目录层级后复跑，不再重复 `/tmp` 方案。
- 真实层级 staging 首轮为 26/27；唯一失败的“明确 Grok”测试只在工具参数填了 provider，事件原话没有 Grok，触发了既有的不可信模型参数清理。修正测试夹具加入真实用户原话，不放宽生产渠道信任边界。
- 测试夹具首次补成“用 Grok 把这张图……”仍不符合现有显式渠道语法；确认解析器要求渠道名后直接接“改/去掉/生成”等动作。本轮将夹具改为规范的“用 Grok 改一下”，不顺带扩大无关渠道语法范围。

## 当前实现
- `contextualProgressReply` 新增 `image_generation`、`reference_generation`、`image_edit` 三种任务语义；生成新图时禁止声称已经看过、收到或正在修改原图。
- Banana 文生图和群友头像参考生图、GoogleImageEdit 真编辑均改用共享上下文进度生成；进度与图片请求并行，任务先结束会取消迟到进度，生成失败不回退固定模板。
- 群友头像参考在调度层标记为 `referencePurpose=member_avatar`，保留按需读取头像的能力，但不再把它解释为用户提供的待编辑原图。

---

# 2026-07-28 音乐合并转发完整文件交付

## 目标
修复音乐提取成功后合并转发只有说明文字、QQ 丢弃伪文件节点的问题。群聊必须通过 OneBot 完整文件上传接口交付可下载的音频文件，并保留音乐信息与可播放音频；只有收到成功回执后才能声称文件已发送。

## 阶段
- [x] 阶段 1：确认下载、本地文件、合并转发节点与 QQ 文件上传链路
- [x] 阶段 2：抽取共享完整文件交付能力并接入音乐发送
- [x] 阶段 3：补成功、失败、清理与回执回归
- [x] 阶段 4：本地验证、线上部署和真实运行态检查

## 约束
- 不把语音段、URL 或被 QQ 丢弃的 `file` 转发节点称作可下载文件。
- 临时 MP3 只能在完整文件上传和可播放音频发送都完成或明确失败后清理。
- 保留工作区内所有既有未提交改动，不顺带重构无关媒体链路。

## 最终验证
- 本地音乐定向回归 11/11，服务器真实 TRSS staging 与正式目录的工具级集成测试均通过；MP3 完整字节进入 `upload_group_file`，上传发生在合并信息和试听之前。
- 正式文件 3/3 哈希一致；部署前图片任务为 0。备份位于 `/opt/trss-yunzai/backups/shiloh-plugin-music-file-20260728-150518`。
- 安全重启后服务 PID=1016027、状态 active，`shiloh-plugin` 与 MessagePipeline 均只初始化一次；OneBotv11/NapCat 已连接并重新收到群消息。
- 本地全量测试 643 项：605 通过、36 跳过、2 失败。失败为既有消息归档临时路径未生成及 UmaRace 缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，音乐相关断言无失败。

---

# 2026-07-28 图片响应体卡死恢复

## 目标
修复图片接口已经建立 HTTP 连接、但响应正文永久不结束时，绘图任务绕过超时并永久停在 `tool_running` 的问题。超时必须覆盖完整响应读取；超时后任务必须正常失败、清理 Redis 队列和活动状态，后续绘图可继续执行。

## 阶段
- [x] 阶段 1：还原群 953676639 卡死任务并定位第一阻塞点
- [x] 阶段 2：把图片请求超时延伸到响应体读取
- [x] 阶段 3：补正文卡死与队列清理回归
- [x] 阶段 4：部署、恢复当前卡死任务并验证

## 约束
- 不重试或改写用户的原始绘图提示词。
- 单个候选的完整请求有确定上限；超时后才允许进入既有 fallback。
- 不能直接删除当前持久任务；必须由受控恢复/收尾流程清理，避免丢图或重复发送。

## 验证记录
- 服务器真实 TRSS staging 定向回归通过：原有 Banana 队列、编辑 fallback、渠道选择均保持通过；新增“响应头成功但正文卡死”在约 28ms 被中止，持久 job/queue/activeTask 均在 finally 中清除。
- staging 的一次复合 SSH 命令遇到认证抖动，正式目录未触及；随后拆为独立传输和测试连接，验证成功。
- 生产已同步正文超时补丁并安全重启：任务 `18240208` 被恢复执行，durable job、队列和 active task 均已清理。该次 Krill 返回的是 `error/type` 而非图片，因而终态为真实 `tool_failed`。
- 已补齐恢复任务的终态通知：重启恢复后不再沿用初始请求的 `notifyFailure=false`；若最终失败，系统会向原群发送真实图片失败说明。生产真实依赖定向回归 17/17，通过端到端模拟该群发送分支；业务补丁已加载，OneBotv11/NapCat 已重连。

## 最终验证
- 服务器真实宿主定向回归 27/27，相关图片回归 84/84，均为 0 失败、0 跳过。
- 本地全量 `npm test`：627 项中 590 通过、36 因当前 checkout 缺宿主依赖跳过、1 个既有 UmaRace 文件级失败；唯一失败是缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，不在本次图片链路。
- 相关文件 `node --check` 与全工作区 `git diff --check` 通过。

# 2026-08-03 受限磁链下载工具

## 目标
让希洛能处理用户明确提供的 magnet URI：先取得种子元数据，在总大小、单文件大小、文件数、元数据时间和下载时间均通过硬限制后再下载，并以 QQ 可下载文件交付。不能让模型、链接或种子内容绕过这些限制。

## 阶段
- [x] 阶段 1：核对现有工具注册、完整文件发送契约和线上下载器依赖
- [x] 阶段 2：实现磁链校验、元数据解析、配额守卫和后台下载工具
- [x] 阶段 3：接入工具 Skill、配置与回归测试
- [x] 阶段 4：验证运行依赖、部署并以受限样例验收（线上工具注册、受限夹具已验收；等待下一条真实磁链确认群交付）

## 约束
- 默认总大小、单文件和最终 ZIP 均不超过 50 MB（50,000,000 bytes），最多 3 个文件；配置只能收紧，不能越过代码硬上限。
- 必须在内容下载前解析 `.torrent` 元数据并校验；缺元数据、超时、超限、无文件、路径危险或交付失败均不得宣称成功。
- 只接受显式 `magnet:?xt=urn:btih:` 输入，不接受 `.torrent` URL、HTTP 下载链接或模型猜出的 hash。
- 下载完成后只生成一个 ZIP，通过现有 `sendCompleteLocalFile()` 获取真实 QQ 文件上传回执；再发送含目录清单和 ZIP 说明的两节点合并转发，最后清理临时文件和元数据。
- 有效 BTIH 磁链与 B 站/抖音分享一样是自动媒体事件：必须绕过闲聊 TimingGate，并用确定性工具参数直接进入受限下载器；多条磁链不得被工具请求合并成一个任务。

## 错误记录
- 首次追加规划文件时选取了过期锚点，`apply_patch` 未修改任何文件；已改用当前文件末尾锚点继续，不重复原补丁。
- 首轮磁链回归的多文件 bencode 测试夹具少了模板表达式闭合符，导致测试文件无法加载；已修正夹具，不代表运行下载器存在语法错误。

# 2026-08-09 YouTube 与 Pixiv 自动媒体解析

## 目标
把 YouTube 视频与 Pixiv 作品接入统一媒体事件、媒体 outbox、合并转发和消息归档链路。链接自动识别后先获取可信元数据；仅在可下载、时长、页数、文件大小与内容限制都满足时发送本体，失败时保留页面并说明已确认的原因。

## 阶段
- [x] 阶段 1：审计现有媒体事件、富化、归档、outbox 与交付契约
- [x] 阶段 2：实现 YouTube/Pixiv 链接识别、可信元数据与归档表示
- [x] 阶段 3：接入统一媒体 outbox、受限下载与合并转发呈现
- [x] 阶段 4：补测试、部署和真实运行态验证

## 约束
- 复用既有按群顺序交付、跨群媒体工件复用与单条合并转发，不新建游离的命令分支。
- YouTube 默认只取最低可用的单文件 MP4；超过 30 分钟、付费/登录限制、没有 `yt-dlp` 或没有实际文件时绝不伪造视频本体。
- Pixiv 只信任 pixiv.net 与 i.pximg.net；默认限制页数和单图/总字节，并对受限作品保留信息而不自动传图。
- 归档保存稳定页面 URL 和可读元数据，不保存短效下载 URL、Cookie 或鉴权信息。

## 错误记录
- 生产机已确认 `ffmpeg` 存在、`yt-dlp` 不存在；首次使用 `python3 -m pip install` 安装失败，原因是系统 Python 未安装 pip。下一步改走系统包管理器或受控二进制安装，不重复 pip 命令。
- 生产 YouTube 元数据探针在强制 IPv4、显式 socket timeout 下仍于 18 秒超时，未得到上游响应；已将该情形映射为“YouTube 元数据请求超时”，而非虚构的搬运成功。下载器已安装，待服务器具备可用 YouTube 出网/代理后会自动恢复完整能力。

## 验证记录
- 本地 `node --check` 通过；定向回归 41/41，通过链接识别、元数据整理、稳定归档、受限 Pixiv 不传图、图片/视频大小限制、跨群 artifact 复用和真实失败原因。
- 已部署 9 个运行模块及新增平台模块，备份位于 `/opt/trss-yunzai-backups/shiloh-plugin-youtube-pixiv-20260809-152627/runtime-before.tar`；生产 `yt-dlp 2025.04.30` 与 `ffmpeg` 已验证存在。
- 生产目录语法检查和无发送解析探针通过；既有媒体/消息管线回归 28/28。最终 `trss-yunzai.service=active`，`shiloh-plugin` 与 MessagePipeline 初始化成功，日志未见 `SyntaxError`、`ReferenceError`、`ERR_MODULE_NOT_FOUND` 或插件载入错误。
# 2026-08-19 合并转发聊天记录无损回放

- [completed] 追踪 OneBot 转发节点从事件采集、归档到聊天记录查询的结构丢失点。
- [completed] 归档递归转发节点快照，并在查询时重建 QQ 合并转发节点。
- [completed] 补充顶层/嵌套转发、图片和旧归档回退的回归测试，完成本地及生产验证。
