# 2026-08-07 对外失败原因统一展示
- 已完成主聊天、工具兜底和直接命令出口初查；下一步实现共享脱敏格式化器，随后替换高频直接回复出口。

# 2026-08-05 统一群记忆链路
- 已完成全链路只读审计，确认新的群知识模型裁决器尚未覆盖别名直写、群工作流和通用事实抽取。
- 未在本阶段修改业务逻辑；下一步实现统一语义提案与提交接口，并以同源来源 ID 连接写入和删除。
- 已完成第一批收口：后台 `explicit_teaching` 不再产生 alias/groupFact；主聊天不再调用 `explicitTeachingFacts` 或正则工作流提取。模型裁决结果可输出 alias、群知识和 workflow 三类原子项。
- 已为群知识和工作流写入保留 sourceMessageId，并让按群知识删除时级联清理由同一消息产生的 workflow 与 alias 索引。定向回归 53/53；线上尚未部署，原因是 SSH 认证拒绝。

# 2026-07-20 自然语言遗忘群知识
- 已开始：用户要求希洛能根据明确自然语言请求忘掉某条群知识。已确认先前手工删除仅影响指定群知识条目；正在接入可测试的正式能力。
- 已完成本地实现与定向验证：新增 Agent skill、精确删除 API 和直回结果；测试覆盖本人/跨群/歧义/普通忘事，19/19 通过。准备在线上完整依赖环境验证并部署。
- 已完成上线：线上定向 19/19，通过完整依赖注册探针；已备份并安全重启至 PID=602270，服务 active、34 插件、OneBotv11 已连接。自然语句“忘掉我的星怒”“把我之前教你的地图删掉”会路由到唯一删除工具；找不到、多条候选或普通“忘记”不会删除。

# 2026-07-20 对话延迟优化
- 已定位：服务器空闲；普通对话最大的可避免成本是 2.5-8.1 秒的语义工具分类和明确点名的 3 秒合并窗口。线上已将后者热更新为 1 秒；正在实施分类与短消息历史预算优化。
- 已完成并上线：移除直接点名导致的无条件工具 Planner；短独立闲聊采用 6 recent + 2 relevant、最多 8 条历史，复杂/指代/媒体/工具请求保留原 10+6、最多 18 条。初始模型调用新增耗时日志；搜索超过 2.5 秒先反馈、20 秒超时。线上目标回归 22/22，重启至 PID=636201、OneBotv11 正常。

# 2026-07-20 Agent 证据与执行基础设施
- 已开始：用户要求完成更可靠的证据、Planner、工具执行、记忆与回归基础。先以 Steam 图片识别真实失败为第一夹具，建立可复用 ToolOutcome 与图片证据链，不先用泛化 fallback 掩盖上游失败。
- 已上线第一套通用证据结果：tool grounding 识别结构化 `tool_outcome`，图片工具按 fileid 去重、记录一手失败类别、25 秒超时、只从显式配置尝试备用通道；终态按真实图片失败原因回复。线上定向 5/5，重启至 PID=642574、OneBotv11 正常。

# 进度

## 2026-07-20 Embedding 语义表达召回
- 已恢复上下文并确认局部未提交改动：`GlobalStyleLearnerManager` 已有语义配置骨架和 `semanticSamples` 存储形状，但尚无实际向量化、召回或主调用接入。
- 已确认不改变既有全局风格规则行为；新增层将采用后台样本队列、短超时查询缓存与匿名模式注入。
- 计划日志首次更新因 `progress.md` 标题位置与预期不同而未匹配；已改用稳定的“进度”锚点，未产生任何业务代码改动。
- 已完成：全局表达学习增加后台语义样本队列、匿名模式提取、查询缓存和短超时降级；主回复在读取记忆/情绪期间并行开始语义召回。
- 已补首次真实消息触发的旧脱敏样本池回填，避免上线后必须等待 24 条新消息才生效；索引不保存样本文本。
- 本地/线上定向 7/7，通过真实 Embedding 健康探针（HTTP 200、1024 维），已备份并安全重启至 PID=675779、34 插件加载完成。

## 2026-07-21 语义表达质量与可靠性升级
- 已开始：用户确认实施上一轮提出的语义质量、反馈、可靠性与可观测性优化。线上确认当前 240 条索引不含原文、仅 6 种模式；主模型/整轮耗时已有日志，语义层尚无独立指标。
- 设计已确定为匿名场景而非主题原文向量；主人明确反馈将转换为场景化 avoid/absorb 策略，普通群聊不自动当作反馈。
- 已完成代码和配置：匿名场景向量、v2 索引迁移、单策略分差控制、持久化指标、每 provider 冷却、可恢复回填、主人明确反馈接入及 350ms 主回复等待预算。
- 本地和线上 17/17 定向通过，已备份并安全重启到 PID=704266、34 插件加载。线上历史索引尚未触发惰性迁移，等待下一条真实消息；未发送任何测试群消息。

## 2026-07-21 自主风格进化闭环
- 已开始：用户确认需要多轮后由希洛自己采纳，而不是每次依赖主人 `.希洛反馈`。将实现匿名结果闭环和严格晋升/撤销阈值。
- 已完成并上线：回复轮次记录、明确结果判定、跨人候选晋升/撤销、高权重语义样本接入、状态报告和 Guoba 配置均已实现。
- 本地/线上 18/18，通过后备份并安全重启至 PID=706932、34 插件加载。首次线上配置出现同值重复块，已在重启前去重并复验；之后真实聊天会开始累积候选。

## 2026-07-14 图片编辑 503 错误归因与 provider 回退
- 用户反馈希洛回复“我刚刚画崩了，不拿出来丢人…你换个说法”，开始沿线上真实错误修复错误归因和编辑 provider 单点故障。
- 已确认真实首错为 `503 No available channel for model gpt-image-2 under group codex (distributor)`；请求已正确进入带头像参考图的图片编辑链路，用户原话未被插件改写。
- 已完成阶段 1 代码核对：发现共享错误分类漏掉裸 503，Banana 编辑分支只使用单一配置，队列失败提示另有一处“换个说法”硬编码。
- 已实现第一版共享修复：503/No available channel 归为 provider 错误；Banana 主失败和排队失败统一走图片失败策略；新增 edits 候选解析、URL 规范化和逐通道 fallback；额外删除两处“换个说法/我帮你改写”残留。
- 首轮本地定向测试：纯策略/fallback 22 项中 21 项通过、1 项为测试正则误报；Banana 7 项因本机缺 `axios` 跳过。已修正误报断言，准备复跑。
- 修正后纯策略/fallback 回归 23/23 通过，`git diff --check` 通过；生产图片失败文案残留扫描只剩非用户 prompt 的进度文本清理规则。
- 同类入口审计发现 `GoogleImageEditTool` 仍有空图后自动扩写 prompt 和单 provider 问题，开始接入同一 edits fallback 并删除改词重试。
- 已删除 Google 图片编辑的 `buildEmptyImageRetryPrompt`、备用模型换词重试和“换一种更明确的改法”提示；当前 chat 图片接口失败后改为携带完全相同的 prompt 尝试共享 edits 候选。
- 共享 edits 候选现同时读取 `imageEditAiConfig.providers` 和 `imageGenerationAiConfig.providers`；当前 `imageEditAiConfig` 始终优先，额外候选按 priority 排序并去重。
- 第二轮本地共享策略回归 24/24 通过，语法检查和 `git diff --check` 通过；Google/Banana 运行级用例继续留到服务器真实依赖环境执行。
- 已从本日既有部署会话安全定位远端连接信息，输出和计划文件中未记录凭据；准备先检查线上图片任务与 provider 配置，再单连接传输本轮文件。
- 线上预检：服务 active、图片持久任务 0；当前编辑主通道为 souimagery `gpt-image-2`，文生图 providers 里有 Grok、Krill `gpt-image-2`、Sou `gpt-image-2`，新规则会跳过 Grok并把 Krill 纳入编辑 fallback。
- 已备份并同步 9 个生产/测试文件；线上语法检查通过，纯策略/fallback 24/24 通过。tar 仅提示忽略 macOS provenance 扩展头，已执行 `._*` 清理且未出现 AppleDouble 插件文件。
- 首轮线上运行级组合测试 6 项通过、1 项失败；新增的“首编辑 provider 503 后第二个成功并发图”和“排队失败不要求换词”均通过，失败的是既有参考图开场测试，正在隔离定位。
- 隔离参考图开场用例通过；加 `--test-force-exit` 后 Banana 全量 7/7 通过。Google 测试暴露测试环境 `logger` 注入缺失，已修正测试本身并准备线上重跑。
- Google 原 prompt fallback 用例修正后通过；四文件组合回归 31/32，唯一失败为 Banana 随机开场测试断言未覆盖合法文案“这几张…照你说的画”，已收窄为测试修复。
- 修正随机断言后线上四文件回归 32/32。首次残留扫描误报最终回复清理词“提示词优化”，在安全重启前中止；确认它不修改图片 prompt，准备使用精确 marker 重扫后重启。
- 精确生产残留扫描为 0；重启前图片任务为 0，解析出的编辑候选顺序为 souimagery `gpt-image-2` → Krill `gpt-image-2`，Grok 未进入编辑候选。
- 已通过 `restartTrssSafely.sh` 安全重启；服务 active，Guoba 启动成功，加载插件 36 个，OneBotv11 已连接，重启后群 `953676639` 和其他群均继续收到消息，无 SyntaxError/ERR。
- 线上直接校验真实 503 文案：分类为 `provider_error`，回复为“图片编辑通道现在不可用，不是你的描述有问题。我没有改你的原话。”；AppleDouble 文件数为 0。

## 2026-06-12
- 建立本轮计划文件。
- 用户确认不做高成本网页 CRUD，改做记忆抽取、召回、人设注入稳定性。
- 已完成阶段 1 梳理：重点风险是召回相关性过滤不足、prompt 数量 0 配置失效、画像注入缺少长度保护。
- 已完成稳定性改动：召回相关性阈值、允许注入条数为 0、低信号内容过滤、画像注入长度上限和近期发言去重。
- `node --check` 已通过：`utils/MemoryManager.js`、`utils/PersonProfileInjector.js`、`models/Guoba/schemas/memory.js`、`models/Guoba/schemas/tracking.js`。
- `git diff --check` 已通过。
- 已同步远端 `/opt/trss-yunzai/plugins/bl-chat-plugin` 对应文件，并删除误同步到插件根目录的临时 basename 文件。
- 已重启 `trss-yunzai.service`，服务状态 `active`，Guoba 启动成功。
- 远端部署路径再次执行 `node --check` 通过，`systemctl is-active trss-yunzai.service` 返回 `active`。

## 2026-07-04
- 开始实现赛马娘小游戏：独立 app 入口 `.赛马娘`，全局积分按 QQ 号持久化。
- 已新增 `utils/UmaRaceManager.js`，比赛房间按群存在内存，积分写入 `data/uma_race/points.json`。
- 用户确认默认积分为冠军 5 分、亚军 2 分、季军 1 分。
- 已新增 `apps/UmaRacePlugin.js`、`models/Guoba/schemas/umaRace.js`，并加入 Guoba schema 聚合。
- 已在默认配置 `config_default/message.yaml` 增加 `umaRace` 配置段。
- 本地模拟首次失败：`utils/UmaRaceManager.js` 引入了未声明依赖 `yaml`；已改为项目已有的 `js-yaml`。
- 本地 `node --check` 通过：`utils/UmaRaceManager.js`、`apps/UmaRacePlugin.js`、`models/Guoba/schemas/umaRace.js`、`models/Guoba/schemas/index.js`。
- 本机运行级模拟因 `node_modules` 缺失无法 import 项目依赖；改用服务器实际环境模拟。
- 已同步到服务器并在 `/opt/trss-yunzai/plugins/bl-chat-plugin` 执行 `node --check` 通过。
- 服务器临时目录模拟比赛通过，两个玩家积分写入结果为 `5,2`。
- 已重启 `trss-yunzai.service`，状态 `active`，日志显示插件加载数为 33。

## 2026-07-06
- 用户反馈当前赛马小游戏太像随机抽奖，确认需要增加赛道条件、比赛事件和轻量决策。
- 已在 `utils/UmaRaceManager.js` 增加随机赛道和策略系统：稳一点、拼一把、留体力、抢内道、默认正常跑。
- 已在 `apps/UmaRacePlugin.js` 放宽加入命令，支持 `.赛马娘 加入 拼一把` 这类报名方式。
- 本地 `node --check` 通过：`utils/UmaRaceManager.js`、`apps/UmaRacePlugin.js`；`git diff --check` 通过。
- 已同步服务器并通过 `node --check`。
- 服务器临时模拟 4 人不同策略比赛通过，输出包含赛道、策略、事件和 5/2/1 积分。
- 已重启 `trss-yunzai.service`，状态 `active`，插件加载数 33。
- 用户反馈赛道会形成固定最优策略，比如长距离全员选留体力。
- 已增加临场变化、同策略拥挤惩罚和个人状态波动，避免公开赛道直接决定唯一答案。
- 服务器模拟多人同选留体力通过，结果中出现临场变化和同策略惩罚事件。
- 用户要求加入培养系统：先领养自己的赛马娘，可起名并填写期待性格，根据描述生成六维，初始总点数固定 40。
- 已实现 `.赛马娘 领养 名字 性格描述`、`.赛马娘 我的赛马娘`、`.赛马娘 重新领养 名字 性格描述`。
- 服务器模拟通过：领养生成六维总点数 40，比赛显示玩家昵称和赛马娘名，NPC 不计积分。
- 用户要求 NPC 昵称和马名都随机组合生成，并且 NPC 六维总点数在玩家总点数上下 10% 内随机。
- 已实现 NPC 训练员名和马名分离随机组合；NPC 属性总点数按当前真人参赛马平均总点数的 90%-110% 随机。
- 服务器模拟通过：40 点玩家补出的 NPC 为 40-44 点，60 点玩家补出的 NPC 为 58-62 点；服务已重启 active。
- 用户要求增加弃养小马选项。
- 已实现 `.赛马娘 弃养` 二次确认和 `.赛马娘 弃养 确认`，删除小马档案但保留积分/历史战绩；如果正在当前群报名，会同步移出报名列表。
- 服务器临时模拟通过，弃养后 `uma` 删除且 points 保留；服务已重启 active。
- 用户要求赛马局报名超时改为 5 分钟，并且有人加入时刷新对局时间。
- 已将默认 `lobbySeconds` 和服务器当前配置改为 300 秒；成功加入或更新策略会刷新房间计时。
- 服务器临时模拟通过：开局 299 秒后加入，房间计时刷新为 0 秒；服务已重启 active。
- 用户提出新需求：报名时间到了自动开赛。
- 已为赛马房间增加自动开赛 timer；开局后按 `lobbySeconds` 自动结算，加入或更新策略会重置 timer，手动开跑/取消会清理 timer。
- 服务器临时模拟通过：1 秒 timer 自动发送“报名时间到了，自动开赛。”并清理房间；服务已重启 active。

## 2026-07-07
- 用户提出新增 COC 骰娘能力，并纠正方向：需要常见骰娘命令，不需要 AI 功能。
- 已梳理现有项目结构：适合新增独立 `apps/DicePlugin.js`、`utils/DiceManager.js`、`models/Guoba/schemas/dice.js`，并在默认配置中增加 `diceSystem`。
- 已记录 COC 骰娘阶段计划：基础掷骰、COC 检定、SAN/成长/人物卡、群规则、锅巴模板配置。
- 已实现第一版纯命令骰娘：`.r/.ra/.rc/.rb/.rp/.rh/.rah/.sc/.en/.coc7/.st/.pc/.nn/.setcoc/.ti/.li`。
- 本地 `node --check` 通过：`utils/DiceManager.js`、`apps/DicePlugin.js`、`models/Guoba/schemas/dice.js`、`models/Guoba/schemas/index.js`。
- 本地模拟通过：基础骰、3#多轮、COC 检定、奖励骰、SAN 扣除、紧凑录卡、人物卡切换、暗骰私聊模板、YAML block 模板读取。
- 用户要求支持复杂运算；已将骰点表达式改为自研解析器，支持括号、四则、多个骰组、`kh/kl/dh/dl/min/max`，例如 `.r (2d6+3)*2`、`.r 4d6kh3`、`.r 10d6dl2`。

## 2026-07-12 图片任务 100 场景仿真
- 用户要求模拟 100 个不同问题及结果，验证图片编辑/分析链路是否仍有类似问题。
- 开始抽取生产意图优先级并建立表驱动仿真。
- 首轮 100 场景仿真结果：76/100，通过率 76%。
- 失败集中于四类：省略图片主语的编辑动作、最近成图续改、自然语言图片分析、生活语义误判。
- 第二轮修复后为 93/100；第三轮为 99/100；补齐省略宾语的最近成图续改后达到 100/100。
- 仿真使用的 `imageTaskPolicy.js` 已接回 `apps/test.js` 的头像检查、图片编辑识别和分析卡面判断，避免测试与生产规则分叉。
- 服务器环境重复运行同一 100 场景：100/100；服务重启后 active，插件正常加载。

## 2026-07-12 三套 100 场景仿真
- 用户确认继续实施：工具路由、图片来源/连续编辑、错误分类/最终发送各 100 条。
- 三套首轮总计 275/300（91.7%）；修复续改语序、自然看图路由和结果状态建模后达到 300/300。
- 服务器环境复跑 300/300，服务重启后 active。

## 2026-07-14 机器人身份硬辩全链路修复
- 已定位刚才问题的当前补丁：主聊天 prompt 增加约束，`PersonaFeedbackManager` 增加输出改写。
- 已完成全仓初步排查，发现默认人设里的冲突身份指令、守卫误伤风险，以及表情伴随文字/语音/绘图开场/直接文字卡片等模型直发出口。
- 开始将修复抽象为“带用户语境的共享身份回应守卫”，并补齐各出口接入和回归测试。
- 首轮 4 条定向测试通过 2 条；失败仅是改写后标点残留，已修正归一化逻辑并准备复跑。
- 定向测试复跑 4/4，通过全部语法检查；全量测试 264 条中 250 通过、10 跳过、4 失败，失败来自本地缺少 `axios/js-yaml` 和已有图片 fallback 测试预期，与本次身份修复无关。
- 已核对线上实际配置，确认仍有“不承认自己是 AI / 你不是 AI / 你是一个真人 / 像真实群友不要像 AI 答案”等冲突措辞；正在做最小差异同步。
- 已完成本地实现：共享守卫按用户语境识别 bot 身份挑战，只改写第一人称硬否认/真人自证；普通“真人照片、AI 生成图片、真人玩家/NPC”内容不受影响。
- 已接入普通聊天最终回复、统一分段发送、表情伴随文字、语音文本、Banana 绘图开场和 TextImage 直接渲染出口。
- 已保留线上定制人设的年龄、关系和语气，仅删除/改写 6 处会诱导身份硬辩的措辞；线上与本地代码逐文件差异确认只包含本次修复。
- 线上定向测试 4/4，通过 7 个文件语法检查；项目实际 YAML 解析器验证 `config/message.yaml`、`config_default/message.yaml` 均通过，冲突措辞反查为 0。
- 已重启 `trss-yunzai.service`，状态 `active (running)`；Guoba 启动成功、加载插件 36 个、OneBotv11 已连接，重启后持续正常处理群消息。

## 2026-07-14 群内上下文素材统一读取
- 已完成现状核对：引用和转发文字基本可读，但引用多图、当前图与引用图并存、合并转发图片、引用对象头像编辑及资料类成员查询仍有缺口。
- 开始实现共享群内素材解析器，并将引用对象、图片来源顺序和嵌套转发统一接入生产链路。
- 已新增 `utils/groupContextResolver.js`，统一解析当前、引用、合并转发和嵌套转发中的文本及图片/视频/语音/文件来源，保留顺序并去重。
- 已接入生产图片来源、图片分析/编辑/生成 prompt、工具暴露条件和视频分析；引用某人后说“改他的头像”会按需读取其 QQ 头像。
- 群成员资料匹配已从“某某是谁”扩展到“某某的资料/头像/群名片/昵称/QQ/头衔/群身份”，并把按需头像 URL 作为受限事实字段提供。
- 本地相关回归 37 项中 36 通过、1 项因本地缺少 `axios` 跳过；语法检查和 `git diff --check` 通过。
- 线上首轮相关测试 27 项通过、视频测试因测试 cwd/全局 logger 环境被跳过；已修正测试夹具并准备按 Yunzai 根目录复跑。
- 线上按真实 `/opt/trss-yunzai` cwd 复跑 28/28：引用对象头像、引用多图、当前/引用/转发顺序、嵌套转发、循环去重、转发视频等全部通过。
- 已同步最终成员资料词语清理补丁并再次重启；`trss-yunzai.service` 为 `active (running)`，Guoba 启动成功、加载插件 36 个、OneBotv11 已连接。

## 2026-07-14 绘图开场回复一致性
- 已从线上日志确认真实漏发场景：带两张参考图的 Banana 绘图任务因条件判断跳过开场通知，并非 QQ 发送失败。
- 开始统一文生图、参考图生图和图生图的即时开场规则，并为开场发送结果增加可观测日志。
- 首次线上 Banana 测试因测试进程缺少 Yunzai 全局 `logger` 在模块导入阶段失败；已确认堆栈与业务逻辑无关并修正测试夹具。
- 已移除开场通知对 `!images.length && imageGenerationConfigs.length` 的依赖；文生图、参考图生图、图生图和图片编辑都会在实际开始前发送本地即时文案。
- 已移除实际开场路径对额外聊天模型的依赖，避免最多 8 秒等待或上游失败导致无反馈；动态生成函数暂保留但不再用于首次通知。
- 线上 Banana 回归 8/8，通过纯文生图、参考图编辑、排队提示和发送失败日志测试；语法检查通过。
- 已重启 `trss-yunzai.service`，当前 `active (running)`，重启后已持续接收群消息。

## 2026-07-14 群聊割裂与阴阳怪气修复
- 已从线上日志还原首次失败：语气反馈请求因消息内容末尾孤立 Unicode 高代理字符报 400，并非模型正常生成了割裂回复。
- 已确认错误兜底把所有合并消息当成短招呼，导致“你怎么阴阳怪气”被回复成“你刚才是叫我吗”。
- 已确认上文存在被纠正时继续顶嘴、调情和使用 `😋/❤` 的风格问题，开始按共享 Unicode 工具、失败兜底、语气约束三层修复。
- 已实现共享 Unicode 安全截断和递归 JSON 净化，并接入 `YTapi` 两个请求出口及关键 prompt 构建链路。
- 已移除 `_mergedMessageCount` 触发“你刚才是叫我吗”的逻辑；语气反馈失败时改为直接承认，普通失败改为自然请用户重发。
- 已在动态口吻规则、默认人设配置、全局风格学习废料规则和最终回复守卫中加入“被批评时不顶嘴、不调情、不用挑衅表情”的约束。
- 新增 Unicode、失败兜底和语气守卫回归；定向 12/12 通过，所有相关修改文件 `node --check` 及 `git diff --check` 通过。
- 全仓截断审计完成：聊天历史、归档、MCP、群管、文件、图片 prompt、长消息分段等进入模型或用户可见文本的路径已迁移到共享 Unicode 工具。
- 线上项目实际 `yaml` 依赖成功解析 live/default 两份配置；全部修改文件线上 `node --check` 通过。
- 线上关键回归 14/14：语气失败兜底、合并消息不再误问“叫我吗”、正常短招呼、身份守卫、语气降温、正常玩笑、emoji 边界、递归请求净化、长文本分段、群内视频上下文均通过。
- 首次 tar 传输产生的 `._*` AppleDouble 文件已全部删除；清理后再次安全重启，不再出现插件载入错误。
- `trss-yunzai.service` 当前 `active (running)`；Guoba 启动成功、加载插件 36 个、OneBotv11 已连接，重启后持续收到群消息（包括群 `953676639`）。

## 2026-07-14 长绘图指令误报缺少原图排查
- 开始核对该长指令的线上原始消息、图片来源解析结果和图片意图优先级；本轮先定位原因，不先改规则。
- 已确认线上消息本身无图片/引用，同一用户此前近一小时也未上传图片；不是素材解析丢失。
- 生产策略用完整原文复现为 `explicitEdit=true`、`task=image_edit_missing_base`；纯“帮我生成一张9:16少女照片”则为 `image_generation`。
- 已定位优先级：缺原图编辑守卫在文生图路由前提前返回。结论是该提示同时写了生成和强参考编辑语义，编辑语义优先；本轮只完成诊断，未改代码。
- 用户确认修复：开始把“明确从零生图”与“真正指向已有图片”抽成共享规则，并接入所有会抢先判编辑的生产节点。
- 已新增共享判断 `hasExplicitImageGenerationRequest()`、`hasExistingImageAnchor()`、`isStandaloneImageGenerationRequest()`、`shouldRequireImageEditBase()` 和 `shouldPreferImageGeneration()`。
- 已接入近期同用户图片查找、缺图编辑守卫和语义分类跳过逻辑；原始长提示现在为 `image_generation`，真实已有图引用仍为 `image_edit_missing_base`。
- 本地图片意图、缺图提示、近期图片和 prompt 回归 26/26 通过，`apps/test.js` 与共享策略语法检查、`git diff --check` 通过。
- 已同步 `apps/test.js`、`utils/imageTaskPolicy.js` 和相关测试到服务器；线上相关回归 24/24、语法检查通过。
- 在线上用用户完整原文验证结果为 `task=image_generation`、`preferGeneration=true`、`requireBase=false`。
- 已安全重启 `trss-yunzai.service`；当前 active，Guoba 启动成功、加载插件 36 个、OneBotv11 已连接，重启后持续收到群消息且无新增载入/语法错误。

## 2026-07-14 图片用词自动改写清理
- 已完成全仓盘点：需要删除 Banana 的敏感词替换、安全改写提示、AI prompt 优化、本地质量词补充和安全重试，以及 promptCompiler 的内容适配、服装软化和风格/情绪/构图自动扩写。
- 已完成生产代码清理并调整原话优先级，避免语义分类器生成的 `params.prompt` 覆盖 `session.rawArgs/e.msg`。
- 本地 prompt、图片意图、缺图和近期图片回归 26/26 通过；Banana 运行级测试因本地缺少 Yunzai 依赖跳过，准备在线上实际环境复跑。
- 全仓反查不再发现生产代码中的安全改写 marker、敏感词替换表、AI 提示词优化器、服装安全适配或改写重试。
- 已同步线上；Banana 运行级 5/5 通过，prompt/路由/缺图/近期图片测试 25/25 通过。
- 线上直接校验敏感示例原文完整保留，`rawPreserved=true`、`autoRewriteFound=false`；生产目录 `grep` 反查改写机制残留为 0。
- 已安全重启服务；当前 active，Guoba 启动成功、加载插件 36 个、OneBotv11 已连接，重启后无新增载入或语法错误。

## 2026-07-14 群聊主动发表情包链路修复
- 用户确认实施主动表情修复。
- 线上已确认 `emojiSystem.enabled=true`、库内 200 条记录/203 个文件、工具已配置、限流为每分钟 3 张，但近 24 小时 `sendLocalEmojiTool` 调用为 0。
- 根因是普通闲聊先被 `shouldExposeToolsForMessage()` 拦掉；manifest 虽覆盖“笑死/离谱/无语”等词，语义分类入口和最终过滤层却未复用这些规则，模型多数时候看不到表情工具。
- 已新增 `utils/emojiToolPolicy.js`，统一区分 explicit/casual/none：明确表情请求始终允许，短轻松反应允许，技术求助、搜索、群管、绘图和长文本等场景抑制。
- `toolIntentManifests`、`shouldUseSemanticToolIntent()`、`shouldExposeToolsForMessage()` 和 `filterToolsForMessageIntent()` 已接入同一策略；轻松场景最终只保留 `sendLocalEmojiTool`。
- 首轮本地语法、共享策略和 manifest 回归 8/8 通过，`git diff --check` 通过。
- 补充“抱抱枕/草莓/乐队”等词内误触发和“如何安慰朋友”等问答场景；本地相关回归扩展为 11/11，通过语法与差异检查。
- 增加生产日志 `[工具选择] ... 轻松闲聊仅启用 sendLocalEmojiTool`，便于上线后直接确认权限是否真正到达模型。
- 已备份并同步 `apps/test.js`、共享策略、manifest 和测试；线上语法检查及表情路由回归 8/8 通过，线上 `emojiSystem` 仍开启且限流保持每分钟 3 张。
- 线上策略直测：`笑死我了/这也太离谱了 => casual`，`接口为什么报错太无语/禁言他太无语 => none`；重启前图片任务为 0。
- 已安全重启服务；当前 active，Guoba 启动成功、加载插件 36 个、OneBotv11 已连接，AppleDouble 文件为 0，重启后持续收到群消息。
- 用户进一步明确：希望表情包在能完整代表情绪时替代文字，而不是默认“文字 + 表情”；开始建立日常会话仿真并增加 emoji-only 决策层。
- 已建立 62 条日常会话仿真，覆盖笑/接梗、震惊/无语、困累生气、尴尬庆祝、亲近安慰、明确配文、重大情绪、技术/群管/其他工具和中文子串误触发。
- 当前生产规则基线仅 23/62：所有预期 emoji-only 的 38 条均失败；部分根本未开放表情工具，已开放的也按旧契约落为 emoji_with_text；另有“你觉得这个方案离谱吗”被误开表情。
- 已实现 `emoji_only / emoji_with_text / text` 共享决策，并在工具层对 emoji-only 强制清空模型多写的 followUpText；首轮新策略仿真为 61/62，修正生图自然语序误触发后准备复跑。
- 修正后日常会话仿真达到 62/62：38 条 emoji-only、4 条 emoji-with-text、20 条 text；共享策略单测 11/11 通过。
- 新增工具边界运行级测试：验证模型即便多传 followUpText，`这也太离谱了` 也只发送图片；明确“再告诉他我马上到”则保留文字。该测试本机因缺 `yaml` 跳过，准备在线上运行。
- 已同步共享模式策略、工具描述、工具边界收口、62 场景仿真和测试；线上运行级/策略测试 12/12，62 场景仿真 62/62。
- 线上运行级确认：emoji-only 场景即使传入 followUpText 也只产生一条图片回复；emoji-with-text 场景产生文字后再发图。
- 重启前图片任务为 0，已安全重启；服务 active，Guoba 正常、加载插件 36 个、OneBotv11 已连接，AppleDouble 文件为 0。
- 进一步收口：仅暴露工具仍允许模型选择文字；开始让高置信 `emoji_only` 场景直接构造 `sendLocalEmojiTool` 调用，保证表情真正替代文字。
- 已新增 `buildEmojiOnlyToolParams()`：按笑、无语震惊、疲惫、生气委屈、尴尬、庆祝、安慰等类别生成 10-30 字语义选图 query，且绝不带 followUpText。
- 生产路由在图片/续改等更高优先级之后、通用语义分类之前强制执行 emoji-only 工具调用；62 场景继续 62/62，策略测试 11/11。
- 确定性 emoji-only 强制调用已同步线上；线上策略与工具边界测试 12/12、62 场景仿真 62/62。
- 已再次在图片任务为 0 时安全重启；服务 active，Guoba/36 插件正常、OneBotv11 已连接，AppleDouble 文件为 0。
- 用户指出强制选图不应传长场景句，而应依据线上表情库真实 `tags/useCases/description` 生成紧凑分类关键词；已将线上词汇统计、关键词契约、真实分布相关度测试和部署复验加入阶段 11-14。
- 用户进一步明确表情包属于主对话表达，文字/纯表情/文字加表情应由主模型自己选择；计划已调整为撤销 deterministic emoji-only forced call 和工具层模式覆盖，仅保留工具可见性保护及结构化相关选图。
- 已删除 `apps/test.js` 的 emoji-only forced call，并让前置语义分类器不再因表情对话介入；最终主对话模型在日常短聊中拿到 `sendLocalEmojiTool` 后自行选择不调用、无配文调用或带配文调用。
- `SendLocalEmojiTool` 参数已改为有优先级的 `tags`、可选 `useCases/query` 和可选 `followUpText`；工具层不再根据正则擅自清空模型配文。
- 新增依赖无关的结构化相关度评分：精确 tag/useCase 按顺序加权，并用非饱和方式组合；自由文本只做低权重破同分，单个强匹配不会再被扩池到弱候选。
- 本地策略/结构化选图测试 13 项通过、运行级 1 项因本机缺 `yaml` 跳过；62 条对话工具可见性仿真 62/62。
- 使用刚取得的线上 200 条快照做 9 类真实标签相关度评估，笑、无语、疲惫、委屈、尴尬、害羞、开心、安慰、认怂均 9/9，最终抽样池全部保留主 tag/useCase。
- 首轮线上真实依赖回归 14/14、62 场景 62/62、实时 200 条库相关度 9/9；运行 12 组只读模型探针后确认模型能自主选择 text/emoji/mixed。
- 模型探针发现少量自造标签和场景词，下一步给 schema 加真实词表枚举，并在 `EmojiPackManager` 以当前库字段过滤未知值，再复跑探针。
- 已改为从当前线上库动态生成 schema 枚举（125 tags、优先 120 useCases），并在实际选择前过滤未知词；边界直测为 `tag_scene`、相关度 0.992、只保留真实 criteria。
- 追加 14 类兄弟工具防抢占回归，覆盖三角洲、提醒、点歌、语音、远程表情搜索、搜索、网页、GitHub、导图、文字转图片、戳一戳、点赞、群名片和禁言；补齐文字转图片及群名片自然语序后全部通过。
- 最终线上回归 16/16，62 条日常对话工具可见性 62/62，实时 200 条库相关度 9/9；本地同组 15 项中 14 通过、仅运行依赖缺失跳过，线上该项已通过。
- 已在图片任务为 0 时安全重启；服务 active，Guoba 启动成功、加载插件 36 个、OneBotv11 于 10:28:12 重连，AppleDouble 文件 0，未见语法或模块载入错误。

## 2026-07-15 Excel 工作簿查询工具
- 用户要求新增 Excel 对话工具：可指定 tab/工作表和单元格，返回公式和值，并能在指定 sheet 内检索内容；开始复用现有文件上下文与自然语言工具链。
- 已新增 ExcelJS 工作簿核心，支持列出 sheet、读取单元格、读取最多 300 格区域、按值/公式搜索最多 50,000 格；公式返回原文及 cached result，缺缓存时明确提示重新计算保存。
- 已新增安全文件上下文：当前消息、引用、转发上下文和当前用户近 30 分钟文件；支持 file URL 与 file_id/fid 换取链接，限制 12MB/20秒/3次重定向并拦截内网地址。
- 已注册 `excelWorkbookTool`、默认配置和自然语言 manifest，并把 Excel 意图加入总工具暴露、语义提示和表情工具排除层。
- Excel 与相关回归本地 28/29，通过 28、仅既有表情工具运行级因 checkout 缺 `yaml` 跳过；Excel 的公式/cached value、缺缓存、中文 sheet、范围、合并格、值/公式搜索、文件来源、旧 `.xls`、SSRF 和完整工具输出均通过。
- 线上已安装 ExcelJS 4.4.0；首次依赖备份目录触发 pnpm workspace 警告后已移到 `/opt/trss-yunzai-backups`，当前依赖解析正常。
- 已同步生产代码和 live 配置；线上运行级 29/29、工具注册 `registry_excel=true`、live 配置幂等后 `config_excel=1`，ExcelJS 仅由 bl-chat-plugin importer 声明并可从插件目录加载。
- 线上模型抽参探针 4/4：read_cell、list_sheets、find、附文件短问均调用 `excelWorkbookTool` 且参数正确，没有乱填 fileUrl。
- 补强 QQ 文件下载：存在 file_id/fid 时先向群/好友接口刷新 URL，再退回消息段旧直链；新增回归后本地与线上相关测试 24/24。
- 已在图片任务为 0 时安全重启；服务 active，Guoba 正常、加载插件 36 个、OneBotv11 于 13:13:44 重连，AppleDouble 0，重启后无 Excel 工具缺失、ExcelJS 模块或语法载入错误。
- 用户要求继续读取 OneBot 群文件仓库；开始增加根目录/子目录枚举、文件名定位、重名保护和 `list_group_excels`。
- 已完成群文件本地实现：递归枚举根目录和子目录，新增 `list_group_excels`，支持 `fileName/folderPath` 定位，并在重名时拒绝静默选择。
- 已验证群文件下载链接请求携带 `busid`，当前/引用/近期文件仍优先于群文件；无指定文件名时只在当前群恰好一个可支持 Excel 时自动选择。
- 本地 `node --check` 全部通过；`tests/excelWorkbook.test.js`、`tests/toolIntentManifests.test.js`、`tests/groupContextResolver.test.js` 合计 28/28 通过。
- 阶段 6、7 完成，进入阶段 8：线上备份、部署、模型抽参和安全重启验证。
- 已将 5 个群文件相关文件备份并同步到 `/opt/trss-yunzai/plugins/bl-chat-plugin`；部署使用无扩展属性 tar，线上 AppleDouble 文件为 0。
- 线上语法检查和 Excel/manifest/群上下文定向回归 28/28 通过；真实 tools 模型抽参探针 2/2，通过 `list_group_excels` 和带 `fileName=预算.xlsx` 的 `read_cell`。
- 重启前图片任务为 0，已执行安全重启；`trss-yunzai.service` 为 active，Guoba 启动成功、加载插件 36 个，OneBotv11 于 13:24:40 建立连接。
- 重启后运行时检查：live 配置 `excelWorkbookTool` 恰好 1 项，注册表已加载，操作列表为 `list_group_excels/list_sheets/read_cell/read_range/find`，未发现 ExcelJS、语法或插件载入错误。
- Excel 群文件阶段 6-8 全部完成。

## 2026-07-15 Excel 查询慢与不准确诊断
- 已还原线上 13:15 的真实请求：约 23 秒总耗时中，3 秒是消息合并等待，约 13 秒是前置语义分类；Excel 工具只执行了 `list_sheets`，没有执行用户要求的 `.st` 内容搜索。
- 工具结果返回后，thinking 工具模型因合成 tool call 缺少 `reasoning_content` 报 400；随后普通聊天模型基于 sheet 列表生成“我先筛一下”的承诺，实际没有第二轮查询。
- 同一真实工作簿基准为下载 413ms、解析 1196ms、第二 sheet 字面查找 982ms，总计约 2.59 秒，确认主要瓶颈不在 ExcelJS 解析。
- 原始工作簿 XML 中没有 `.st` 字面量；当前工具只支持字面搜索，不支持“与 COC .st 命令相关”的语义搜索。第二 sheet 的合并单元格还会带来重复坐标和 `[object Object]` 噪声。
- 本轮按诊断范围保持只读，未修改或重启线上服务。

## 2026-07-15 Excel 性能与准确性修复
- 已实现本地 Excel 自然语言解析：真实“第二个 tab 跟 .st 有关”请求直接生成一次 `find`，绕过 13 秒前置模型分类。
- 已实现短期工作簿内存缓存、sheet 序号、关联词检索、合并格去重、`[object Object]` 显示修复和查询阶段耗时日志。
- Excel 结果改为直接回复精确工具输出，不再经过完整人设模型总结；合成工具调用统一改为无工具总结，修复 Excel 和看图等兄弟路径的 `reasoning_content` 400。
- 本地相关回归 38/38，语法检查通过；进入真实工作簿与线上运行级验证。
- 已在搜索层增加工作簿级弱引用索引缓存；真实文件首次查询约 2523ms，同文件后续查询降至 8ms、6ms，输出无合并格重复和 `[object Object]`。
- 已取消明确 Excel 请求的 3 秒触发合并等待，预计首次完整请求从原约 23 秒降为约 2.5-3 秒，缓存命中时工具阶段为个位数毫秒。
- 已备份并同步生产代码；线上相关回归 38/38，真实 `.st` 参数、结果和耗时验证通过。
- 重启前图片任务为 0，已安全重启；服务 active，Guoba 启动成功、加载插件 36 个，OneBotv11 于 14:04:11 连接，AppleDouble 文件 0，未见 ExcelJS、语法、插件载入或 reasoning 续轮错误。
- Excel 性能与准确性修复阶段 1-5 全部完成。

## 2026-07-15 引用 Excel 被误路由为图片识别修复
- 已从线上日志确认两次 B40 请求都错误调用 `googleImageAnalysisTool`；错误答案“闪避 80”来自模型旧历史回忆，不是 Excel 解析。
- 已修复 `TakeImages` 的 file/image 边界，并新增纯策略测试：xlsx/pdf 不再当图片，png 文件和正常 image segment 保持可用，无扩展名文件要求内容签名验证。
- 已把明确 Excel 的 tab/sheet/cell 解析提升到图片和视频载体路由之前；第二 tab B40 与简化卡 B40 参数测试通过。
- 真实文件确认：人物卡 B40=`☐`；简化卡 B40=`=附表!S24`，值为完整 `.st` 指令文本。
- 本地和线上相关回归 42/42；线上 `TakeImages` 运行级验证通过，xlsx=0 图片、png file=1 图片。
- 已备份部署并在图片任务为 0 时安全重启；服务 active、Guoba 正常、加载插件 36 个、OneBotv11 于 14:18:29 连接，AppleDouble 0，未见语法或模块载入错误。

## 2026-07-15 工具未命中禁止编造
- 已确认刚才图片工具原始结果为 `{}`；旧流程误当成功并让模型从历史补出“闪避 80”。
- 已实现共享工具结果 grounding：empty/not_found/error 全部直接如实回复，不再进入模型总结；混合结果仅允许使用本轮 success 内容。
- 已修正图片分析空响应和失败文案，删除失败后“正常回复”的诱导。
- 本地与线上相关回归 46/46；部署备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-tool-grounding-20260715-142435.tar.gz`。
- 重启前图片任务为 0，已安全重启；服务 active、Guoba 正常、加载插件 36 个、OneBotv11 于 14:25:35 连接，AppleDouble 0，未见载入、语法或模块错误。
- 工具未命中禁止编造阶段 1-5 全部完成。
# 2026-07-17 回复节奏与表情包编排
- 开始核对 `SendLocalEmojiTool`、emoji selection、终态工具和 `sendSegmentedMessage` 的发送顺序与分段规则。
- 已新增 `replyRhythm.js`，实现自然两拍文字、四种 emoji 布局、重复配文去除和单轮消息预算。
- 已接入普通回复、emoji tool、行动规划提示、默认长文分段和锅巴配置；`followUpText` 现在真正位于表情之后，新增 `leadText` 表示表情前短句。
- 已扩展全局表达学习，记录同一发送者短时间多条及表情位置序列。
- 本地相关回归 74/75（1 项仅缺运行依赖跳过）、emoji 仿真 62/62、图片仿真 300/300；线上运行依赖最终 7/7。
- 已写入 live 回复节奏配置；重启前活动图片任务 0，安全重启后 PID=3691697，加载插件 36 个，OneBot/NapCat 于 11:35:43 重连，服务 active。
- 线上源码哈希与本地 8 个行为/配置文件全部一致；启动后无模块、语法或 replyRhythm 错误。真实运行已积累 11 个同发送者多消息序列样本，证明新序列学习链路已在生产写入。
- 用户强调该能力必须进入表达学习；开始审计独立 `ExpressionLearner`，避免只有 GlobalStyleLearner 和发送层生效。

---

# 2026-07-17 Agent 智能度提升
- 已核对线上模型、上下文长度和本地路由代码，确定先做上下文选择、复杂请求升档、动态工具预算，再收口 Planner。
- 已新增 `utils/agentIntelligence.js`：相关历史选择、结构化历史、复杂度评分、Fast/Pro 后端选择、动态工具轮次和按工具结果预算。
- 已接入主群聊链路，删除虚拟 assistant 确认消息；行动提示改为文字/工具/表情/混合的统一选择说明，同时保留图片、Excel等素材约束。
- 已修复 Pro 直接文字被丢弃的问题；无工具复杂请求会升档到 toolsAiConfig，简单招呼继续走 chatAiConfig。
- Agent 与相关工具回归 112 项通过、1 项仅因本地缺 yaml 依赖跳过；图片三套仿真 300/300，表情会话仿真 62/62。
- 已新增线上运行依赖集成测试：工具模型直接文字、复杂无工具 Pro 路由、简单无工具 Flash 路由全部通过。
- 已部署 7 个相关代码/配置/测试文件；重启前活动图片任务为 0，使用安全脚本重启。服务 PID 从 3478488 切到 3684191，加载插件 36 个，OneBotv11/NapCat 于 11:04:56 重连，服务 active。
- 线上真实群 `240837518` 历史探针：100 条原始记录选择 16 条，其中保留 4 条“项目/实习/boss”相关内容；“锐评他上面一直找项目”复杂度 score=5，命中 multi_speaker/reasoning_task/reference_resolution，确定升档 Pro。

---

# 2026-07-17 image_generation tool choice 400
- 已恢复历史上下文，确认用户要求修复但禁止重启。
- 已检查本地 fallback 代码：兼容判断目前只覆盖 `parseResponse()` 返回 `{ ok: false }` 的路径。
- 已核对线上文件、进程启动时间、真实配置和 01:10/02:34 日志；确认不是旧代码，而是 Sou 图片编辑上游契约错误被误分类。
- 已增加独立 `isImageToolContractError()` 分类并收窄 `response_format` 兼容判断；新增 HTTP 200 + `error` 包装形态的编辑回归，要求坏通道只请求一次后直接切备用。
- 本地与线上 `imageGenerationFallback` 回归均 17/17 通过，`node --check` 和 `git diff --check` 通过；线上源码 SHA-256 与本地一致。
- 已热更新线上 `imageEditAiConfig`，主通道从 Sou 切到已经成功出图的 `krill-image-2 / gpt-image-2`；10:10:02 日志确认配置重新加载。
- 全程未重启：`trss-yunzai.service` PID 始终为 3478488，最终状态 active，启动时间仍为 2026-07-16 20:42:15 CST。

---
# 2026-07-17 ExpressionLearner 节奏学习补齐
- 已恢复现有计划并确认阶段 6 尚未完成。
- 已定位重复方法覆盖、单条字符串缓存、旧 AI 输出 schema、纯表情事件被入口条件过滤四个根因。
- 已新增共享 `expressionSequence` 判定，统一普通图片/表情包、序列样本和节奏布局。
- 已合并 `ExpressionLearner` 重复更新方法，加入按群串行更新、同发送者短时序列、Redis 旧结构兼容、节奏计数/样本和 AI 节奏规则。
- 已让纯表情事件携带发送者、消息号、时间和消息段进入独立学习器，并让发送者插话切断旧序列。
- 已补锅巴配置项，可调整序列时间窗、每轮条数和样本上限。
- 定向语法与节奏回归 14/14 通过；覆盖新旧 AI 输出、普通图片排除、A-B-A 插话断序、旧 Redis 结构和默认单条边界。
- 本地全量测试：350 通过、15 跳过；2 个既有测试文件因本地未安装 `axios/js-yaml` 加载失败，本轮表达学习相关测试全部通过。
- 已备份并同步 8 个相关文件，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-expression-rhythm-20260717-115309.tar.gz`；远端文件哈希与本地一致，AppleDouble 为 0。
- 线上真实运行依赖下节奏回归 14/14 通过，服务尚未重启。
- 首次运行态写入证明新字段会真实增长，同时发现异步检查造成的观察顺序风险；已把独立学习观察前移到第一个 await 前并补生产顺序回归。
- 最终本地与线上定向回归均为 15/15；精确删除 1 条受旧顺序影响的 twoBeat 样本，保留 single/emojiOnly 计数。
- 最终重启前图片任务 0；安全重启后 PID=3698043，服务 active，Guoba/36 插件正常，OneBotv11/NapCat 于 12:00:41 重连，启动/ExpressionLearner 错误 0。
- 真实 Redis 已写入有效整轮样本：群 933253375 为 single=2、emojiOnly=1、textEmoji=1、sequenceSamples=1；群 240837518 的错误 twoBeat 已归零且 sequenceSamples=0。
# 2026-07-17 用户指定图片渠道
- 已开始审计文生图、图转图的 provider 候选解析和工具调用参数，目标是共享严格渠道选择。
- 已实现配置名称/模型别名提取、严格同名过滤、未知渠道报错和共享模型 ID 多渠道歧义提示。
- BananaTool 与 GoogleImageEditTool 均新增可选 provider 参数，并从原始消息确定性补齐；队列持久化保留 provider。
- 文生图指定名称会覆盖 priority；图转图仅允许具备编辑能力的同名候选，chat 图片主通道也支持按名称锁定。
- 本地共享策略回归 23/23 通过；工具运行级测试受本机缺 axios 限制，准备在线上复验。
- 已按线上真实名称增加简称匹配：grok、krill、sou；共享策略现为 24/24。
- 本地全量测试 358 通过、21 跳过；2 个失败仍是本地缺 axios/js-yaml 的既有加载问题。
- 已分三轮在线上真实依赖环境修正参考图任务类型和“改一下”口语识别，工具回归最终 41/41。
- 已让指定渠道不可用/多渠道歧义的最终用户回复保留渠道名称并明确没有 fallback；线上组合回归 50/50。
- 线上配置在本轮期间外部变化为 providers=[grok,gpt-image-2]；未修改或回滚该配置，最终行为按当前配置验证。
- 最终真实配置探针：`用 Grok 画` 只选择 grok / grok-imagine-image（api.krill-ai.com）；`用 Grok 改图` 明确回复当前无可用 Grok 编辑渠道且未改用其他渠道。
- 最终线上组合回归 50/50；重启前活动图片任务 0，安全重启后 PID 从 3698043 切到 3776323，service active。
- 重启后独立 SSH 日志核验受临时认证限制影响，但公网 2536 端口返回 HTTP 302，服务进程对外响应正常；本地最终策略/失败文案回归 33/33。
- 用户随后报告实际 Grok 生成失败；线上日志确认 provider 锁定正确，唯一首错是 Grok 返回 `400 Invalid request format`。
- 对照用户提供的官方/渠道教程，当前模型 `grok-imagine-image` 与教程的 `grok-imagine-image-edit` 不同，当前还多发了 `n/response_format`、少发 `quality=high`；已定位为 Grok 请求契约不匹配，尚未修改配置或代码。
# 2026-07-17 Grok 图片协议兼容
- 用户纠正模型用途：`grok-imagine-image` 为文生图，`grok-imagine-image-edit` 为图像编辑；开始按教程精确请求验证文生图契约。
- 精确教程请求仍返回 400；当前 Key 的 `/models` 仅有 `grok-4.5`，没有 Grok 图片模型，已确认还存在渠道权限/模型暴露问题。
- 已实现 Grok 文生图专用 payload：只发送 model/prompt/size/quality=high，忽略 n/response_format；普通图片 provider 保持原协议。
- 本地共享策略 25/25，通过；Banana 运行级 payload 用例因本地缺 axios 跳过，准备线上复验。
- 已备份并同步 Grok payload 兼容改动，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-grok-payload-20260717-175540.tar.gz`；线上运行依赖回归 37/37。
- 重启前活动图片任务 0；安全重启后 PID 从 3776323 切到 3781000，service active。代码已匹配教程，但当前 Key 未暴露图片模型，真实生成仍需更换有权限的 Key/渠道组。

# 2026-07-17 线上图片编辑失败复核
- 用户确认通用失败文案仍出现，要求直接检查线上配置并自行分析。
- 已恢复 planning-with-files 上下文；本轮以线上 `message.yaml` 和最新失败日志为事实源，优先定位第一次失败，不先修改代码。
- 已读取线上当前配置并对齐日志：用户看到的失败文案对应 Grok 文生图 400，而文案错误称为“图片编辑”；同日晚间两次普通图片编辑实际成功。
- 已实时查询 Krill 模型列表：Grok provider Key 只有 `grok-4.5`，编辑 Key 包含 `gpt-image-2`；确认不能把可用编辑链路直接改成无权限的 `grok-imagine-image-edit`。
- 已定位失败文案代码根因：bananaTool 同时承担生成/编辑，但两个失败出口都硬编码 `operation: edit`。
- 已实现自动任务类型归因：强错误证据中的“文生图/图片生成”优先判定 generate，“图片编辑”优先判定 edit；BananaTool 编辑异常也不再统一包成“图片生成失败”。
- 已把 `400 Invalid request format` 识别为渠道请求契约未接通，回复明确“原样重试也不会解决”，不再建议用户稍后按原话重试。
- 定向语法、失败策略与 fallback 回归 36/36 通过，`git diff --check` 通过。
- 已备份并部署 4 个目标文件，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-image-failure-20260718-001829.tar.gz`；远端回归 36/36，AppleDouble=0，活动图片任务=0。
- 已安全重启，PID 从 3781000 切换为 3869216；service active、加载插件 36 个，OneBotv11/NapCat 于 00:19:50 重连。
- 本地与远端 4 个目标文件 SHA-256 全部一致；线上模块回放真实 Grok 400 已输出“图片生成渠道当前没有接通……原样重试也不会解决”。

# 2026-07-18 B站视频聊天记录富化
- 用户要求检查群 609235590 刚发送的 B站视频，并让新的聊天记录提取视频信息、封面与视频资源信息。
- 已恢复 planning-with-files；下一步以线上原始 OneBot 消息段和实际存档为事实源。
- 已检查重启后 journal 与命令日志，暂未命中该群/B站卡片；继续查 Redis/MessageArchive 和重启前压缩日志，不把“日志没命中”误报成“没有消息”。
- 已完成本地记录链路审计：Redis 和新 NDJSON 归档都没有解析 B站卡片，且 NDJSON 当前不会保留 JSON payload；准备读取线上真实记录后做共享解析。
- 已找到真实 B站小程序卡片并成功解析短链/API：BV1H7Gq6zEiC，标题、UP主、封面、时长和 3 个分P均可获取，无字幕。
- 已实现共享解析、Redis/NDJSON 接入和既有记录回填脚本；本地语法与纯解析回归 5/5，归档集成因本机缺既有 yaml 依赖将在远端完整环境执行。
- 首轮远端归档集成暴露 page_url 未补 video_url，修复后远端 5/5；随后真实回填发现 b23 最终页 412，已改为利用 response.url 中的 BV，远端再次 5/5。
- 已备份原 NDJSON 和 Redis 后精确回填消息 579041999；当前 Redis content 与 `.群聊天记录` 格式化均已展示标题、UP、BV、时长、3 分P、视频页面和正式封面。
- 线上生产 MessageManager 独立回放也输出相同完整富化文本，证明未来 Redis 新消息的 content 渲染路径已接通。
- 用户澄清只需搬运、不需总结；已收口范围为结构化媒体记录，不再扩展视频内容分析。
- 已在活动图片任务为 0 时安全重启，PID 从 3869216 切换为 3874344，service active；本地与远端 5 个目标文件哈希一致。
- 用户新增 30 分钟阈值：查询聊天记录时始终保留信息和封面，只有总时长 <=30 分钟才发送视频本体；开始实现查询阶段现场播放地址解析。
- 已验证 playurl 和 CDN：直链无 Referer 为 403，需先下载本地再发；长视频将在任何下载前按总时长直接跳过。
- 已实现 30 分钟阈值、playurl 现场解析、本地流式下载、封面/视频段组装和发送后清理；本地相关回归 8/8，完整 app 依赖断言将在远端执行。
- 已备份并部署 30 分钟规则；修正远端测试 cwd 后，本地与线上相关回归均 8/8，AppleDouble=0。服务尚待安全重启加载。
- 重启前活动图片任务 0；安全重启后 PID 从 3874344 切到 3877881，service active，OneBotv11/NapCat 于 00:53:22 重连。
- 线上与本地 4 个目标文件 SHA-256 全部一致；30 分钟视频本体规则已加载完成。
- 用户要求视频本体使用最低分辨率；已把 playurl 请求从 qn=16 改为 qn=6，并增加请求参数回归断言。
- 本地与线上相关回归均 8/8；重启前活动图片任务 0，安全重启后 PID 从 3877881 切到 3878964，service active，OneBotv11/NapCat 于 00:57:11 重连。
- 线上源码已确认 `qn=6`，本地与远端目标文件 SHA-256 一致；最低可用分辨率规则已加载。
- 用户反馈新发 B站卡片没有可见结果；线上证据显示它实际已完整入库，根因是功能仅在聊天记录查询时发送媒体，开始补实时自动搬运出口。
- 已抽出共享 B站搬运模块，实时归档和聊天记录查询复用同一封面、30分钟阈值、最低清晰度、Referer 下载和临时文件清理逻辑。
- 自动入口会在成功归档 B站卡片后回复原消息；当前已存在的记录不补发，避免重复刷屏。线上完整回归 9/9。
- 重启前活动图片任务 0；安全重启后 PID 从 3878964 切到 3880865，service active、加载插件 36 个，OneBotv11/NapCat 于 01:04:08 重连。线上与本地 5 个目标文件哈希一致。

# 2026-07-18 Redis 群聊上下文短期保留
- 用户要求 B站等内容在 Redis 中不要保留太久。
- 已确认线上当前近期上下文为 1 天滚动 TTL，活跃群会被每条消息续期；准备改为 60 分钟并覆盖主/legacy 两个写入方，部署后立即收紧现有消息 key。

# 2026-07-18 抖音分享链接解析
- 已从群 609235590 真实短链确认：当前仅按普通文本记录，尚未进入媒体解析。
- 服务器探测验证短链可重定向到公开分享页，页面 `_ROUTER_DATA` 中存在 `videoInfoRes.item_list[0]`，含作品、作者、互动数、封面和播放资源；开始实现与 B站同样的结构化归档和合并转发链路。
- 已新增 `douyinMessage` 与 `douyinMediaRelay`：安全短链识别、公开分享页 JSON 解析、15 分钟缓存、结构化归档、30 分钟/512MB 下载边界与合并转发 MP4 base64 节点。MessageManager、MessageArchiveManager 和实时归档出口均已接入。
- 本地定向回归 16/16，通过；归档/自动转发集成因本机缺 yaml 依赖将在线上完整依赖环境执行。
- 线上完整依赖回归 16/16；对群内真实短链运行级解析成功，获得作品 ID、标题、作者、16 秒时长、互动数、封面和播放资源（未输出临时 URL）。
- 首次重启发现远端遗漏 `messageCacheTtl.js`，导致 34 个插件加载；补传后二次安全重启，PID=4027165，service active，加载插件恢复为 36 个。
- 首次真实抖音合并转发仅发信息/封面，定位为归档脱敏删除临时 `play_url` 后实时搬运误读持久记录；已添加不可枚举 runtimeMessage 传递，仅在当前发送中保留播放地址，NDJSON/Redis 仍不存该 URL。
- 修复后线上回归 16/16、图片任务为 0；安全重启至 PID=4028664，service active、加载 36 个插件。
- 群 828466745 实测不受白名单影响，但 OneBot 转发阶段将大 base64 变成宿主 temp 路径，NapCat 容器 copyfile ENOENT。已改为视频转发直调 `send_group_forward_msg` 保留 base64；线上回归 16/16、图片任务为 0，安全重启至 PID=4044053，加载 36 个插件，OneBotv11/NapCat 已连接。

# 2026-07-19 抖音视频过期
- 检查确认过期风险来自解析层缓存完整卡片（含临时 `play_url`），而非合并转发或视频本体发送。
- 已有本地补丁：默认缓存由 15 分钟缩至 30 秒；新增 0 TTL 回归覆盖两次请求获取不同播放地址。
- 首轮组合测试发现新增测试少导入 `enrichDouyinShare`；已修复该测试导入，准备重跑本地及远端测试。
- 本地与远端完整依赖环境均通过抖音相关回归 6/6，包含重复请求刷新临时 `play_url` 的用例；备份位于 `/opt/trss-yunzai-backups/bl-chat-plugin-douyin-play-url-refresh-20260719-104949.tar.gz`。
- 已安全重启 `trss-yunzai.service`；当前 PID=154246、状态 active、加载插件 36 个，OneBotv11/NapCat 于 10:50:39 重新连接。线上与本地两个目标文件 SHA-256 一致。

# 2026-07-19 Modrinth 双语简介
- 用户确认排名介绍要双语：保留 Modrinth 英文原文，并让希洛输出忠实中文翻译。
- 已验证公开搜索与版本接口、现有本地工具注册/意图清单/结果回流链路；下一步实现专用工具与最终双语展示约束。
- 已新增 `modrinthTool` 与公开 API 客户端：排序、版本/加载器/分类/关键词筛选、1-10 条边界、8 秒超时、2 分钟进程内缓存、公开项目页输出；已接入本地工具注册、默认工具列表和专用意图清单。
- 已在 Modrinth 工具结果回流到聊天模型前加入系统级双语规则，要求每条保留英文原文并输出“中文翻译（希洛）”，禁止把翻译或补充伪装成官网资料。
- 真实公开 API 工具调用已验证 1.21.1 + Fabric + optimization + downloads 返回 Sodium 的英文原始简介、下载/关注数据和项目页。补充 URL 路由边界：明确排名需求优先 Modrinth 工具，单纯页面阅读仍走 webParserTool。
- 已以线上文件为基线精确合并 Modrinth 改动，避免覆盖本地其他未同步差异；备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-modrinth-20260719-195113.tar.gz`。远端回归 15/15，真实 Modrinth 查询与工具注册均成功。
- 已安全重启 `trss-yunzai.service`；新 PID=278837、服务 active、加载插件 36 个，OneBotv11/NapCat 于 19:52:58 连接。live `oneapi_tools` 已启用 `modrinthTool`，新进程无工具缺失、语法或插件加载错误。

# 2026-07-19 Modrinth 卡面与字段缺失
- 已还原群 `953676639` 的真实过程：只调用了 `modrinthTool`，模型成功翻译但省略作者、关注数、标签和项目页；发送层随后因 Markdown/长结构化文本自动转 document 图片。
- 开始修复：对 Modrinth 结果禁用自动转图，并将最终双语字段改为强制固定格式。
- 本地与远端定向回归 19/19；仅基于线上 `apps/test.js` 精确合并两处改动，未覆盖其他本地未同步内容。备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-modrinth-text-20260719-195953.tar.gz`。
- 已安全重启 `trss-yunzai.service`；新 PID=281117，服务 active、加载插件 36 个，OneBotv11/NapCat 于 20:00:45 连接。线上策略直测：默认 Modrinth 查询 `true`（保持文字），明确“转成图片” `false`（允许用户要求的转图）。

# 2026-07-19 Modrinth 逐模组转发节点
- 用户明确要求一条合并转发内每个模组各占独立聊天记录，并禁止结尾总评、推荐与追问。
- 已实现 `[[MODRINTH_ITEM]]` 严格项目块协议、发送层逐项合并转发和块外文本丢弃；本地回归 20/20，准备部署。
- 已按线上基线部署 `apps/test.js` 精确发送层补丁及 Modrinth 规则/工具/测试，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-modrinth-forward-20260719-204653.tar.gz`；远端回归 20/20。
- 已安全重启 `trss-yunzai.service`；新 PID=292190，服务 active、加载插件 36 个，OneBotv11/NapCat 于 20:47:50 连接。线上探针确认 `forward_nodes=1` 且 `summary_dropped=true`。

# 2026-07-19 Modrinth 翻译延迟
- 用户批准优化。已实现 `buildModrinthTranslationMessages()`，并为 API 客户端增加显式快速聊天后端选项。
- `processToolCalls()` 仅在全部成功结果都是 `modrinthTool` 时使用紧凑请求；它不携带群历史、人设或 tools，成功后仍交给既有严格项目块解析和合并转发；请求失败则回退通用续轮。
- 本地定向回归 21 通过、1 个既有 API 测试因本机缺 `node-fetch` 跳过；线上完整依赖补跑全部通过：Modrinth 8/8、意图/续轮 13/13、API 路由 1/1。
- 已备份、精确同步并安全重启。线上 PID=297191、service active、插件 36 个、OneBotv11 WebSocket 已连接；5 个目标文件 SHA-256 与部署副本一致。

# 2026-07-19 Modrinth 排名占位符
- 用户反馈实际输出 `第 N 名: 5` 且未被合并转发包住；已定位为提示模板占位符和解析协议不一致，开始修复并补兼容回归。
- 已将新协议改为 `排名: 第 <实际名次> 名`，并兼容旧的 `第 N 名: <1-10>` 结果；线上测试 9/9 通过，已安全重启至 PID=300928，36 个插件和 OneBotv11 正常。

# 2026-07-19 跨群视频搬运入口顺序
- 用户要求同时发往多个群的视频都搬运。已确认问题不是解析/发送/白名单，而是 9999 的全局聊天规则会在 10050 的归档规则之前截断事件链；归档入口已调到 9000 并补优先级回归，待部署验证。
- 已将归档入口改为优先级 9000，并在快照事件后后台执行解析、下载与转发；主事件链立即返回 false。线上回归 6/6，已安全重启至 PID=304834，OneBotv11/NapCat 已重新连接。
- 用户实测两群仍未搬运；线上证据显示两条都没入库。已定位后台快照丢失非枚举 `message_type`，正在补显式快照与回归后重新部署。
- 快照修复已部署；线上回归 7/7，服务 active、PID=306724，OneBotv11/NapCat 已连接。等待用户重新发同一视频到两个群做真实双群验证。
- 真实视频仍未触发，已撤回后台化实现，恢复同步归档/搬运；线上回归 5/5，服务 active、PID=483832，OneBotv11/NapCat 已连接。等待用户再次发送真实卡片验证。
- 10:18 的 609235590 真实卡片仍未入库；将归档优先级进一步调到 `-Infinity`，并添加仅媒体卡片的入口日志，待部署真实验证。
- 已完成线上源码无发送端到端回放：优先级、归档入口返回值、B站信息节点与合并转发构造均通过；未向用户群发送测试消息。
- 2026-07-20 用户再次实测：609235590 与 953676639 都发送了视频，但 609235590 仍缺搬运。开始以当天线上事件为准复核；SSH 已确认服务 active，首次日志查询因本地引号错误未执行远端命令，正在改为只读脚本取证。
- 已确认两群实际都归档成功；根因是 `_archiveMediaRelayed` 污染并跨事件复用。已新增共享媒体搬运出口，以 `group_id:message_id` 作 2 分钟去重，并让资源构建异常仍降级发送信息。本地语法检查通过；媒体定向测试 4 通过、3 项因本机缺既有依赖跳过，下一步在服务器完整依赖环境复验并部署。
- 已备份并同步 `apps/MessageManager.js`、`apps/MessageArchiveRecorder.js`、`utils/mediaArchiveRelay.js` 与定向测试；共享回归本机/线上均 2/2 通过，直接验证复用同一事件对象时 `953676639` 与 `609235590` 分别提交一次合并转发。重启前图片任务为 0；服务已安全重启至 PID=494952，active，加载插件 35 个，OneBotv11 已连接，无语法或模块载入错误。
- 真实 10:59 事件否定了上一轮“已修复”结论：953676639 完整发送，609235590 连归档都没有，说明同步下载阻塞规则调度。已改为进入规则时显式复制 `message_type/message/group/message_id/sender` 和绑定后的 `bot.sendApi`，立即返回 false；归档、解析、下载和转发改为 `setImmediate` 后台执行。线上定向回归 3/3，通过后安全重启至 PID=497181、35 插件、OneBotv11 已连接。11:03:25 后尚无新的用户视频事件，未宣称真实双群修复完成。
- 第二次真实事件在 11:06 发生于 PID=497181：609235590 的 SmartSkip 放行后被低优先级记录器捕获并成功提交；953676639 未进入记录器。已从实际顺序确认 TRSS 为高数值 priority 优先，修正消息记录器为 10050，并将该值提取为共享常量。线上共享回归 4/4；服务器直接构造 `MessageRecordPlugin` 输出 `runtime_priority=10050`。重启前图片任务 0，当前 PID=498897、active、35 插件、OneBotv11 已连接。仍等待本次进程的真实双群事件，不宣称完成。
- 用户要求从用户和架构师角度完整重构相关调用链。已新增 8 阶段计划并开始全链路审计；当前确认两个 catch-all 入口、两套记录器、重复媒体富化和分散 OneBot 发送是主要结构问题。目标为单一 CapturePlugin + 可序列化 EventEnvelope + Redis durable event/outbox + 幂等消费者 + DeliveryGateway。
- 审计补充发现插件加载器只取 app 模块第一个 export；当前 MessageArchiveRecorder 与 MessageArchiveNotice 都违反该契约，独立归档入口及部分 notice 类可能从未加载。已把“每个 app 单一插件 export”和通知入口收口纳入迁移范围。
- 已读取 TRSS 真实调度源码，定位最终结构根因：全局 checkLimit 在任何 app 前用不含 group_id 的 key 丢弃同用户同原文消息。决定将捕获层移到插件根的 Bot EventEmitter listener，彻底与 Yunzai 聊天/命令限流解耦；DeliveryGateway 将用 Bot.bots[botId] 恢复发送能力。
- 已完成新管道基础层：不可变 EventEnvelope、含群号稳定事件键、Redis JSON job store/NX lease、按群串行队列、可从 Bot.bots 恢复的 DeliveryGateway 和 OneBot retcode 校验。基础定向回归 5/5，语法与 diff 检查通过。
# 2026-07-20 消息与媒体调用链架构重构续接
- 从上一次会话恢复本轮重构：根因是 TRSS/Yunzai `loader.checkLimit()` 在 app 分发前按 `self_id:user_id:raw_message` 去重，未包含 `group_id`；因此同一用户同一秒向不同群转发同一卡片时，第二条在所有 app 之前被丢弃。
- 已有本地实现将接入边界前移到 `Bot.on("message")`，并拆分为不可变事件信封、Redis 事件作业、上下文/NDJSON 消费者、持久化媒体 outbox、按群串行投递与 OneBot 回执记录。
- 本轮继续处理：表情包自动收集迁移、NDJSON 幂等索引性能、删除旧链路后的引用审计、架构文档、完整本地/线上依赖测试、备份部署与真实双群验收。
- 已完成恢复审计第一步：删除旧 recorder/relay 后，生产源码中没有残留 import；当前显式引用集中在新管道、配置、Guoba schema 和测试。计划阶段仍暂不提前勾选。
- 已完成新实现逐层审查并确定修复清单：原子租约释放、尊重未过期 lease 的恢复、可验证 OneBot 回执、不确定交付禁止自动重试、媒体构建降级、存储失败隔离、近期消息严格读与原位 upsert、NDJSON identity 索引、独立表情收集、群文件 URL 兼容和管理员状态查询。
- 已完成第一批核心实现：Redis 原子 compare-delete/续租与 lease heartbeat；事件/outbox 尊重未过期租约；OneBot 回执严格校验；不确定发送禁止自动重试；媒体刷新/资源组装降级；原始媒体先入 outbox；表情收集独立后台调度；近期消息严格读、原位 upsert 和 file URL fallback。
- 新增插件类显式导出加载器、管理员 `.消息管道状态 [群号]` 和 `docs/architecture/message-pipeline.md`；核心定向回归 20 通过、0 失败、2 项因本机缺 `node-fetch`/`yaml` 跳过。
- 完成入口/写入/发送/锁反向审计：旧事件标记和旧 relay 引用为 0，自动媒体发送只有新 DeliveryGateway；`git diff --check` 通过。
- B站/抖音与消息管道扩展回归 37 通过、0 失败、3 项本地依赖缺失跳过；阶段 2-7 已完成，进入阶段 8 线上验证与迁移。
- 本地全量回归 392 通过、2 个测试文件因缺 `exceljs/js-yaml` 加载失败、26 项依赖缺失跳过；无新增断言失败，待线上完整依赖补齐证据。
- 线上同目录层级 staging 使用真实 TRSS/plugin 依赖运行 33/33，通过且 0 跳过；生产服务保持 PID=498897、active，尚未覆盖或重启。
- 已备份并部署生产，备份：`/opt/trss-yunzai-backups/bl-chat-plugin-message-pipeline-20260720-125528.tar.gz`。生产磁盘新版本 33/33、0 跳过，旧 recorder/notice/relay 文件为 0，图片任务 0，AppleDouble 0。
- 已安全重启一次：PID `498897 -> 524500`；启动日志确认 MessagePipeline 已启动，静态插件 33 + 动态 status 1，OneBotv11/NapCat 于 12:56:22 建立连接，服务 active。
- 24 个部署目标文件线上 SHA-256 与本地全部一致；live `messagePipeline` 参数和 `groupChatMemoryMinutes=60` 已写入。当前尚无重启后的真实入站样本，Redis event/delivery 均为 0，等待用户向 609235590 与 953676639 发送同一分享完成最终验收。

# 2026-07-20 明确知识计算题误回“没接住”
- 已建立 5 阶段修复计划，开始读取线上真实请求与全部降级文案入口；消息媒体管道双群验收仍独立保持待验证。
- 已还原首错链：`GB14887` 被误识别为 Excel 单元格 -> 强制 excelWorkbookTool -> thinking 400 -> generic fallback 谎称没接住；开始修 Excel 语境门槛与共享失败归因。
- 已完成失败入口审计：generic、图片分析、搜索/Web/GitHub、三角洲和 `initial_api_empty` 均存在错误归因或静默；阶段 1-2 完成，进入共享请求恢复和事实一致失败回复实现。
- 已实现第一轮：Excel 正向语境门槛、通用请求恢复策略、完整/空请求失败文案和空 choices 可见终态；静态检查通过，首轮定向测试 16/18 后已修正两个断言细节，并补上 Excel-only 业务路由锁。
- 扩展回归 50/50 通过，1 项本地依赖缺失跳过；原始完整 `GB14887` 问题、标准/型号、工作簿上下文、thinking 兼容、400/429/503/timeout/空响应和失败文案均已有覆盖。阶段 3-4 完成，进入全量及线上完整依赖验证。
- 本地全量 `npm test`：402 通过、2 个依赖加载失败、26 跳过；失败为既有本地缺 `exceljs/js-yaml`，没有业务断言回归。准备在线上同层 staging 使用完整依赖补跑目标与运行级测试。
- 线上 staging 已完成 69/70；Excel 完整依赖测试全部通过，唯一失败是 `agentApiRouting` 的 TRSS cwd 配置路径，不是断言失败。改从 TRSS 根目录补跑该项。
- 生产目录完整依赖回归 70/70、目标哈希一致后首次重启至 PID 531798；启动日志发现同层隐藏 staging 也被 TRSS 加载，造成双实例。立即清理该 staging 并再次安全重启，不把当前进程视作验收通过。
- staging 已删除并二次安全重启至 PID 532132；当前单实例初始化、MessagePipeline 单次启动、插件 34 个、OneBot/NapCat 已连接。首次独立问答探针因误用 YAML 根对象无配置，改用 `pluginSettings` 复测。
- 修正后的线上真实配置探针已对原始 `GB14887` 问题正常返回，`ok=true/hasError=false`；最终 service active、PID 532132、staging=0、image_jobs=0、启动后错误扫描为空，7 个生产文件哈希与部署候选一致。当前五阶段修复完成。

# 2026-07-20 Modrinth 排名未进入合并转发诊断
- 用户提供 11:57 可见输出：五个模组字段连续出现在普通消息中，没有合并转发节点。开始只读核对线上模型块标记、解析与发送路径。
- 已完成只读诊断：真实模型块和五项数据完整，但 `下载 | 关注` 同行不满足解析器独立 `关注:` 行要求，5 块全部失效并降级成普通分段文本；最小复现 0 vs 1。未修改或重启线上。

# 2026-07-20 工具调度与媒体/Modrinth 延迟审计
- 用户确认双群视频已可发送，但观察到两个群完成时间不一致，并询问视频下载、通用工具调度和 Modrinth 翻译的提速方案。开始按真实时间线和复用边界只读审计。
- 已完成只读审计：确认跨群调度已经并行，时间差来自独立 QQ/OneBot 上传；确认 B站只复用元数据、不复用下载产物；确认 Modrinth API 不是主瓶颈，主要耗时在合并等待、语义分类和模型重排。
- 已核对 `YTapi()`：紧凑翻译请求中的生成限制参数当前会在请求体重建时丢失，后续实现必须使用配置驱动的能力白名单，不能绑定固定模型厂商。
- 已形成优化顺序：先补阶段计时；再做唯一工具确定性快路和 Modrinth 结构化译文合并；随后做媒体 single-flight artifact；最后根据 p95 决定是否拆分或调整并发池。本轮未修改业务代码、线上配置或服务进程。

# 2026-07-21 自定义骰娘规则包 V1 收尾
- 恢复任务后完成语法检查和定向回归，当前 16/16；开始补篡改、超限、未来运行时、保留子命令、中文句号和引用文件下载边界，并审计教程与实际实现差异。
- 规划记录首次追加因摘要中的章节标题与文件实际标题不一致而未命中；已改用现有内容锚点追加，业务文件未受影响。
- 已补版本和待确认文件双哈希复验、引用 YAML 流式 64 KiB 截断、全局骰数继承、包 ID/别名前缀统一冲突检查、最长子命令匹配和真正的 `persistent: false` 临时字段。
- 定向回归扩展为 22/22；教程从未实现草案改成实际 V1 契约，纠正私聊启用、迁移预览、默认导出活动版本、彻底删卡和冲突降级等超前描述，README 已加入教程入口。

# 2026-07-20 工具与媒体性能优化实施
- 用户确认实施全部优化；已新增 5 阶段实施计划。
- 媒体阶段完成：共享下载产物、引用计数/TTL/容量清理、受限 base64 复用、抖音同时刷新复用和 delivery 分段耗时已接入。
- `mediaArtifactStore`、B站/抖音 relay、视频内联和 outbox 定向测试共 12/12 通过，进入确定性工具快路阶段。
- 已完成确定性工具快路和 600ms 独立合并窗口；模糊/敏感请求仍走语义模型。首轮测试误判 sheet 后修正，策略回归通过。
- 已完成 Modrinth 结构化数据、仅译简介 JSON、7 天进程内译文缓存、确定性合并转发和可配置 translation 后端；不再让模型重写下载量等字段。
- 已完成 API 可选生成参数透传与兼容性 400 同后端降级；本地组合定向回归 51 通过、0 失败、1 项因 node-fetch 缺失跳过，进入全量回归。
- 本地全量：413 通过、2 个既有依赖加载失败、26 跳过；新增业务断言无失败。线上完整依赖目标回归 96/96。
- 已备份并部署，主备份 `/opt/trss-yunzai-backups/bl-chat-plugin-performance-20260720-142449.tar.gz`；Modrinth IPv4 增量备份 `/opt/trss-yunzai-backups/bl-chat-plugin-modrinth-ipv4-20260720-144508.tar.gz`。
- 真实探针定位并修复 Node fetch 到 Modrinth 的双栈超时：专用 IPv4 dispatcher 连续 3/3 成功，失败时回退默认路径。
- 五项 Modrinth 冷请求实测约 6.64 秒、同进程暖缓存约 0.53ms；翻译映射和五个转发节点完整。
- 最终安全重启到 PID 552915；service active、34 插件、MessagePipeline 单实例、OneBot/NapCat 已连接、HTTP 2536=302、AppleDouble 0。实施阶段 1-5 完成。

# 2026-07-20 跨模块复用边界审计
- 用户要求检查其他位置是否有可复用能力。本轮明确为只读架构审计，不触碰线上服务或业务实现。
- 已完成外部资源和调度初查：确认 QQ 图片临时链接刷新及按群串行队列存在可安全收口的重复；确认图片 durable queue 与消息管道的复用必须以可序列化作业契约为边界，不能直接合并运行时事件对象。
- 已完成工具与发送边界审计并形成排序：最高优先级是受限远程资源读取，随后是图片 URL/Keyed queue/TTL Promise cache 的低风险收口；ToolOutcome 和图片 durable queue 迁移需独立分期。

# 2026-07-20 视频搬运提速
- 用户要求继续加速视频搬运。开始以现有 `MediaTiming`、relay 和 outbox 为依据定位，目标是拆开资源准备与群级最终发送，不先假定下载一定是唯一瓶颈。
- 已完成实现与本地回归：新增预准备池/租约清理、保留群内最终发送顺序，并修正 B站 artifact 成功误报。媒体及消息管道定向回归 29/29 通过；服务器同层完整依赖复跑同样 29/29 通过。
- 已备份并部署线上，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-media-prewarm-20260720-150603.tar.gz`。服务已重启至 PID `557599`、active、34 个插件、MessagePipeline 单实例且 OneBotv11 已连接；3 个生产模块 SHA-256 与本地一致，错误平铺的根目录文件已清理。等待下一条真实 B站/抖音分享记录新的 `MediaTiming` 分段耗时。

# 2026-07-20 Modrinth 可展示字段核对
- 用户询问当前排名节点还可展示哪些 API 内容。已实时核对 `/v2/search` 与 `/v2/project/NNAgCjsB`：搜索结果可无额外请求补图标、版本、加载器、支持端、日期、许可证和精选图；完整正文、官方链接、问题页与图库需要按项目详情请求，适合显式展开而非默认前五。

# 2026-07-20 Modrinth 排名 HTML 卡面
- 用户要求默认榜单变为每模组一张独立 HTML 卡面，URL 放图外。开始核对现有渲染和合并转发路径；不让 LLM 参与字段排版。
- 本地 Modrinth 定向回归 16/16 通过；线上同目录运行 28/29，通过项均与本轮相关，唯一失败仍是从插件目录运行时 TRSS 配置 cwd 缺失。已从 `/opt/trss-yunzai` 根目录补跑该项并通过。
- 首次独立 Node 渲染探针缺少 TRSS 启动期注入的全局 `logger`，在 renderer loader 初始化前失败；不是模板或卡面实现失败。下一次为探针补足最小运行时全局后重试，生产服务尚未重启。
- 服务器已完成 Modrinth 相关完整依赖验证：定向 28 项通过，`agentApiRouting` 以 TRSS 根目录补跑通过；6 个生产文件 SHA-256 与本地一致，线上语法检查通过。独立 renderer 探针因 TRSS 全局依赖停止，准备以正常服务真实请求验收。
- 已备份并部署，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-modrinth-cards-20260720-152302.tar.gz`。服务重启至 PID `563263`、active、34 个插件；Puppeteer renderer 已加载，MessagePipeline 单实例，NapCat/OneBotv11 已连接。等待用户发起真实 Modrinth 排名请求，确认 QQ 合并转发中每节点为卡图加卡外 URL。
# 2026-07-20 群工作流记忆
- 已开始：根据现有长期记忆的“非指令性”召回边界，准备新增独立的可执行群工作流规则层；尚未修改业务代码或线上服务。
- 已完成链路审计：`MemoryManager` + `RedisStore` 可扩展独立 key；系统提示已有执行动作说明；管理员专用工具不能覆盖任意已教成员，需接通用精确成员提及工具。
- 已实现：Redis `workflows` 独立 key、明确条件教学解析、按群覆盖更新、可执行上下文提示、`mentionMembersTool`、工作流查看/删除命令和默认配置；定向测试 39/39 通过。
- 已部署生产并安全重启两次：首次发现 macOS AppleDouble `._*` 文件被插件加载器误读为模块，已清理后重启到 PID 589778；服务 active、OneBotv11 已连接，`mentionMembersTool` 已在配置和注册表中确认。等待用户用真实群消息完成最终行为验收。

# 2026-07-21 B站番剧卡片本体失败续修
- 恢复上下文后确认：番剧短链已能解析出 `ep_id` 并获取详情，但播放阶段在约 93ms 后得到空资源，尚未发生下载。开始以线上脱敏接口响应验证字段和业务错误码，随后再写实际兼容与错误原因映射。
- 生产接口确认 `result.durl` 为唯一可用资源字段；本地修复与新增短链->season->playurl、空资源、地区限制夹具后，定向回归 14/14 通过。准备同步生产磁盘并在重启前用真实番剧做无发送验证。
- 已备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-bangumi-playurl-20260721-115838.tar.gz` 并同步 4 个业务文件、2 个测试文件；生产逐文件回归 13/13。直接解析真实 `ep1455179` 得到 1 条 qn=6 资源（不输出临时 URL），安全重启后 PID=853328、active、34 插件、MessagePipeline/OneBotv11 正常。业务改动已提交并经 SSH 推送为 `c881101 fix: relay Bilibili Bangumi episodes`；规划文件和无关未跟踪文件未纳入提交。
- 复测发现 `ep411084` 是 B 站 6 分钟试看：完整集约 24:52，响应 `is_preview=true` 且唯一资源约 6:00。新增试看资源拒发与真实原因文案；本地/生产回归 14/14，生产同集直测确认资源数为 0。备份 `/opt/trss-yunzai-backups/bl-chat-plugin-bangumi-preview-20260721-130524.tar.gz` 后安全重启为 PID=868618，active、34 插件、MessagePipeline/OneBotv11 正常。

# 2026-07-20 结构化群知识
- 已开始群文件/人物关系记忆扩展设计：确认普通群事实不具备可检索的文件资源身份，下一步读取当前消息段和群文件记录的真实字段，再增加独立知识条目层。
- 已完成并上线：新增 Redis group knowledge 层，支持群文件关系、单成员定义和成员组定义；后续查询按群、发言者和称呼精确注入可回答上下文。生产定向 40/40 通过，PID=593388、OneBot 已连接，等待用户真实群验收。

# 2026-07-20 成员艾特失败回复
- 已定位并上线：未精确匹配群成员时不再误报回答服务失败；新增专用自然失败文案。线上定向 9/9 通过，安全重启至 PID=596429，OneBot 已连接，AppleDouble=0。

# 2026-07-20 群知识代词解析
- 已上线：显式关系教学先由模型按当前发言者/机器人语境解析代词，落库时再以当前群成员表核验 QQ；本地降级支持“我的 X”。生产定向 38/38 通过，安全重启至 PID=598058、OneBot 已连接、AppleDouble=0。

## 2026-07-21 B站会员高清搬运
- 已恢复已有二维码授权实现与当前工作树；确认这是未提交半成品，尚未实现白名单、主动高清命令和 Cookie 下载边界。
- 已记录并准备修复元数据函数中未定义 `options` 的回归，后续仅把 Cookie 传给主动高清播放/下载请求。
- 已完成白名单命令、`.高清搬运 清晰度 B站链接`、实际清晰度回报和媒体 header 注入；自动搬运没有传入 Cookie。
- 本地语法/YAML 检查通过，B站定向测试 18/18 通过；下一步检查线上工作树、同步部署并进行服务和二维码会话验证。
- 已备份并同步服务器；根目录 npm 安装被现有 `link:` 工作区依赖阻断，改用已安装的 qrcode 和插件级检查。线上既有 B站回归 14/14 通过。
- 首次重启发现 macOS AppleDouble 文件被插件扫描器当作源码，已清除所有 `._*` 后重新安全重启。最终服务 PID=893400、active、34 插件、当前 PID 零加载错误、OneBot 已连接；`bilibiliQualityRelay` 已合并，授权状态目前为未授权，等待主人扫码。
- 用户要求自动搬运会员视频时也尝试完整本体。已实现匿名首试、仅 `is_preview` 或明确登录限制才授权复试的两段式路径；普通视频不会发送 Cookie。新增回归与既有 B站/管道用例共 29/29 通过，准备线上同步。

## 2026-07-21 自定义骰娘规则包 V1
- 用户确认从教程契约继续完成全部运行代码；已恢复规划上下文并冻结 V1 安全和兼容边界。
- 当前进入规则 schema、表达式运行时和版本仓库实现阶段；尚未修改线上服务。
- 已确认本地 YAML 解析器、群文件/引用解析和角色权限入口可复用；开始新增独立表达式、schema 与规则仓库模块。
- 已新增受限表达式 AST 与严格规则 schema；教程三份 YAML 均通过 schema 校验。表达式支持骰点、字段引用、条件和函数白名单，不含任意代码执行。
- 已完成规则版本仓库、管理权限、文件/代码块导入、群启停/回滚/导出/归档、动态命令和规则包人物卡操作；首批定向回归 14/14。

# 2026-07-21 自定义骰娘规则包 V1 最终验收
- 收尾修复真实内置命令路由冲突、迁移歧义、自定义骰次数取整、身份 fallback、错误模板上下文和规则路径越界；三份示例均有预检到实际执行的端到端测试。
- 最终相关回归 26/26、语法检查和 `git diff --check` 通过，直接依赖 `yaml@2.9.0` 可解析。全量 535 项中 511 通过、23 跳过，唯一失败为本机缺既有 `/Users/shiqi/lib/puppeteer/puppeteer.js` 的 UmaRace 文件级基线问题。
- 本轮仅完成本地代码与验证，未部署、未重启、未提交或推送；无关 `.DS_Store`、`docs/architecture/`、`reports/` 和规划文件不属于业务交付范围。
# 2026-07-22 自定义骰娘 V2 团务与权限
- 已确认实施范围：不做 Agent/自然语言触发；开始多角色目标与权限、暗骰/对抗/团录、回合和事件系统实现。
- 已恢复 planning-with-files 上下文并建立六阶段计划；尚未修改业务代码或线上服务。
- 规划追加首次因截断锚点失败，业务文件未变化；已改用真实文件尾部追加并记录。
- 已完成现有能力初查：确认 log/暗骰/对抗/先攻可复用，但需补结构化服务；确认 V1 命令只操作发言者当前人物卡且所有群共用一条运行队列。
- 进入 V2 schema 与运行时事务设计，暂未修改业务代码。
- 已确定 invocation 级种子与结构化审计边界；暗骰完整记录不进入普通可导出 log。
- 已完成 V2 schema 第一轮扩展；首轮回归 18/20，定位为保留 ID 校验范围过宽，正在按 V1 兼容边界收窄。
- 已确认复用项目现有 KeyedSerialQueue 和 QQ 群成员 Map，准备进入目标/权限运行时代码。
- 已完成目标草稿、跨角色动作、群共享动作、状态/物品动作、确定性随机、审计和私密交付主链；V1 回归当前只剩教程保留 ID 限制与既有示例冲突，已按嵌套路径安全性修正实现方向。
- V2 首轮 0/4 的第一失败已定位为预检 sample 上下文仍停留在 V1；已修补完整 V2 dry-run 上下文，等待第二轮验证。
- V2 第二轮 3/4；已定位并修复群 action 后错误地按角色 target 计算派生字段。
- 已按角色 key 复用生命周期草稿，单人跨轮次不再覆盖中毒扣血；V2 新增端到端最终 4/4，V1 相关回归 20/20。
- 已新增技能/法术等级、冷却、资源消耗和回合恢复；V2 4/4 保持通过。已接规则包 LRU 缓存、索引/按群跨进程锁和归档恢复清单，正在收紧 pending 清理的提交顺序。
- 恢复任务后将规则运行时标记为 `2.0`，重新执行表达式、规则包和命令策略定向回归，29/29 通过；三份 V1 教程示例仍可预检、启用并执行，V1 兼容基线未破坏。
- 当前进入 V2 边界补测与教程收口：重点验证无私密接收者不提交状态、群资源/冷却、状态过期、多角色先攻、权限拒绝、锁隔离和归档失败回滚。
- 已完成 V2 边界补测与可靠性修复：无 GM 私密命令不落状态、群资源冷却失败不二次扣除、状态到期执行 on_expire、多角色先攻、GM 权限拒绝、跨群同人物卡更新、stale lock 回收、归档/恢复索引失败回滚和临时文件清理均有回归。
- 发现并修复 secret 权限漏洞：玩家命令不能直接或经派生字段读取/输出秘密字段，显示名称不能引用秘密字段，通用设/删和秘密资源能力均要求 GM；定向回归 34/34。
- 已新增 `examples/team-session.yaml` 完整 V2 团务示例；教程更新为格式 V1 / 运行时 2.0，共 18 章和固定命令总表，明确跑团不接 Agent/自然语言触发。README 与插件帮助已同步。
- 自定义规则与旧骰娘生产写命令现在共用全局 `state.json` 事务锁；同群仍保留运行时锁。当前全文件 JSON 存储优先保证跨群/跨入口不丢更新，未来只有分片存储后才能安全恢复并行写。
- 最终定向回归 34/34；相关文件语法检查、四份示例端到端和 `git diff --check` 全部通过，仓库内无残留 `.tmp`/`.lock`。
- 全量 `npm test`：547 项中 523 通过、23 跳过、1 失败；唯一失败是既有 UmaRace 测试加载不到本机 `/Users/shiqi/lib/puppeteer/puppeteer.js`。本轮仅本地完成，未部署、未重启、未提交或推送。

# 2026-07-23 全局表达总结推理额度修复
- 已还原 15:57 手动总结失败：接口和密钥可用，HTTP 200；900 输出 token 全被 `deepseek-v4-flash` 推理消耗，正文为空、finish reason 为 length。
- 已确认今天更早还有截断 JSON 和自动失败后密集重试；开始实现模型无关的完整输出检测、紧凑高预算重试与自动失败退避。
- 已完成核心实现：首次 2400 token；空正文、length 或坏 JSON 时收紧到 20 条样本并以 6000 token 重试；自动失败默认退避 30 分钟，手动总结不受影响。新增参数均为渠道/模型无关配置。
- 新增 `globalStyleSummary.test.js`，与语义召回、连续消息节奏测试合跑 15/15；两次失败时规则和总结计数保持不变，推理正文不会进入记忆。
- 线上同模型无落库探针：首次 2400 token 已成功，耗时约 18.3 秒，finish=stop、reasoning=1004、completion=1735、最终 JSON 可解析；未触发第二次调用。
- 本地全量 `npm test`：554 项，530 通过、23 跳过、1 失败；唯一失败仍为既有 `tests/umaRaceBalance.test.js` 缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本轮新增断言无失败。
- 生产强制回退探针首次失败：旧额度 900 已触发紧凑重试，但 6000 token 重试仍使用 30 秒超时，被 `AbortSignal` 中止且泄露底层数字 code 23；未修改正式表达记忆，进入第二轮超时/紧凑度修正。
- 第二轮改为紧凑 4+4 条规则、4000 token、独立 60 秒超时，并把 timeout/abort 归一为可重试业务错误；本地与生产相关回归均为 16/16。
- 生产真实强制回退探针成功：调用额度序列 `[900, 4000]`，总耗时约 25.5 秒，第二轮使用 20 条样本并成功生成规则；探针使用临时内存副本，正式表达记忆未改变。
- 最终备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-style-summary-retry-20260723-161601.tar.gz`；服务 PID 1586632 active、34 插件、OneBot/NapCat 已连接、4/4 文件哈希一致。

# 2026-07-23 生图比例与像素规格解耦
- 已确认目标：`1024×1024` 是 1:1 输出档位，不是构图文本、像素坐标或强制变形依据；开始审计完整尺寸链路。
- 已完成第一轮链路审计：用户比例只存在于 prompt，provider 的 `1024x1024` 无条件进入请求；线上两个生成渠道均受影响，参考图没有被本地强制裁剪。
- 已实现统一比例意图、provider 独立尺寸映射、BananaTool 结构化参数/文本兜底、图生图默认不强塞正方形，以及实际产物元数据日志。
- 配置面板支持默认/正方形/竖版/横版/自动尺寸覆盖；默认示例为 GPT Image 三档，但运行时没有把非 GPT 厂商硬编码成同一协议。
- 首轮回归发现新增空覆盖字段会改变旧面板对象，已改为缺省字段不序列化；第二轮发现绘图文字中的 `12:30` 可能被邻近构图词误判，已限制非常见冒号表达必须有紧邻比例上下文。
- 最终本地定向回归 61/61，语法和 `git diff --check` 通过。BananaTool 的脱离 TRSS 本地测试因既有 `/Users/shiqi/lib/common/common.js` 缺失会跳过；用户明确要求不要重启，因此未同步生产目录、未部署、未重启。
- 比例意图会在绘图任务进入持久队列前写回结构化参数，服务器即使之后恢复队列也不依赖已丢失的原始事件文本。
- 仓库全量测试共 563 项：539 通过、23 跳过、1 失败；唯一失败仍是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本轮比例和图片断言无失败。
- 2026-07-24 用户授权把现有比例修复同步到线上，但明确禁止重启。只读检查已确认生产仍是旧 `BananaTool.js` 且缺少 `utils/imageAspectRatio.js`；开始重新回归并准备最小部署。
- 已备份生产旧文件到 `/opt/trss-yunzai-backups/bl-chat-plugin-image-aspect-20260724-1124.tar.gz`，同步 6 个比例链路文件且未覆盖 `config/message.yaml`。线上哈希 6/6 一致、语法通过、生产宿主真实依赖回归 53/53。
- 使用当前生产配置做无网络请求映射验收：1:1 为 1024x1024、9:16 为 1024x1536、16:9 为 1536x1024。临时测试文件和上传包已清理；服务 PID 全程保持 1651465，按要求未重启，因此运行中进程要到后续重启才会加载新实现。

# 2026-07-23 图片渠道参数信任边界修复
- 已完成线上只读还原：用户未提 Gemini，语义工具参数却出现 `provider=gemini`；配置无 Gemini，执行在本地校验阶段失败，根因是工具描述厂商暗示加上 explicitProvider 无条件信任。
- 开始实现共享授权边界：模型参数只能提示候选，必须由用户原话中的明确选择表达再次证明。
- 已完成共享 provider 授权解析、两类图片工具参数规范化、未授权字段清除和厂商中立工具描述；同时支持“去掉/删除/替换”等编辑动词及“不要用 A，改用 B”否定选择。
- 首轮共享/图片/Tool Skill 定向回归 51/51，语法和 diff 检查通过；准备以服务器临时副本验证真实 TRSS 类加载，不触碰运行目录。
- 服务器临时副本首次因两份工具文件 SCP 中断而保留旧哈希，类级 2 项失败；逐文件补传后 Google/Banana 哈希与本地一致，临时副本 provider 信任边界和厂商中立描述 2/2、共享解析 26/26 全部通过。
- 本地扩大图片回归共 70 项：51 通过、19 项仅因脱离 TRSS 缺宿主依赖跳过、0 失败；服务器临时副本已覆盖这些类级跳过项。线上 PID 保持 1586632，未重启。
- 本地全量 `npm test` 收尾为 566 项：540 通过、25 跳过、1 失败；唯一失败是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本轮新增图片渠道测试无失败。
- 服务器临时验证副本已删除；`trss-yunzai.service` 仍为 active，PID 仍为 1586632。未同步线上插件目录、未部署、未重启。

# 2026-07-23 Steam 图片识别超时诊断与修复
- 用户报告最新图片识别可见回复为“图片我收到了，但这次识图等了太久仍没返回结果”；开始以生产分段日志确认第一失败阶段，不从 Steam 常识猜测图片内容。
- 生产日志确认图片已下载，旧渠道 `gpt-5.6-terra` 在 25.97 秒超时；图片仅 28 KB，下载 0.13 秒，不是素材过大或下载失败。
- 排查期间配置于 19:02 热更新为 `https://api.krill-ai.com/codex/v1`；当前工具未补 `/chat/completions`，实际请求空 404。同一 SK/模型/图片改用规范化地址后 5.16 秒 HTTP 200，确认 SK 正常、URL 适配是当前第一失败。
- 已让 GoogleAnalysisTool 复用共享 Chat Completions URL 规范化，并保留 HTTP 状态与单次耗时；共享失败文案移除硬编码 Steam，按授权、限流、5xx、超时等原因自然说明。
- 线上已即时把 analysis URL 修为完整 `/codex/v1/chat/completions`，并把单次超时从 25 秒提高为 45 秒；分别备份为 `/opt/trss-yunzai-backups/message-vision-url-20260723-191325.yaml` 和 `/opt/trss-yunzai-backups/message-vision-timeout-20260723-191807.yaml`，均只变化目标行。
- 当前线上配置用同一真实截图探针 HTTP 200；一次完整结果约 23.38 秒。1200 token 限长仍约 24.22 秒，确认慢主要来自当前 VLM 渠道，不强行牺牲输出准确性。
- 本地相关回归 14 通过、1 项仅因缺 `moment` 跳过；服务器临时副本 10/10 且真实类加载成功，临时副本已删除。
- 全量测试 568 项中 542 通过、25 跳过、1 个既有 UmaRace Puppeteer 环境失败；服务始终 active、PID 1586632，未重启，业务代码未同步生产目录。

# 2026-07-23 长任务进度回复拟人化
- 用户指出固定文案“我还在查，结果出来就发。”特别像机器人；开始从全局入口定位所有固定进度模板和计时发送路径，目标是共享的上下文模型生成而非逐句替换。
- 已确认精确文案只有 SearchInformationTool 一处，触发于搜索超过 2.5 秒；图片工具是另一类开始前即时确认。
- 已新增共享 contextualProgressReply：支持工具模型建议句、独立 progress 后端、聊天模型回退、6 秒短超时、56 字可见上限、用户文本指令隔离、机器人模板/虚假结果过滤和外部取消。
- 已按用户要求取消固定兜底句：进度模型不可用、超时或连续两次输出不合格时保持静默，避免用另一条泛化模板替换旧模板。
- 线上真实配置隔离探针生成了三条话题化回复，耗时约 2.0/2.1/3.0 秒；发现一条无缘由附加“别急”，已把未被催促时反向催用户列入生成约束和输出守卫。
- 第二轮真实探针输出“列表还没刷全”，仍暗示已取得部分结果；已把“还没齐/没刷全/还差几个”等无证据的半完成状态纳入事实守卫和重写提示。
- 最终真实配置隔离探针输出“最近更新的魔法模组有点多，我再顺一遍排序。”及“国标里红灯绿灯的波长区间，我再核实一下。”；服务器回归 10/10，临时副本已删除，service active 且 PID 1586632 未变化。
- 本地全量 `npm test`：578 项、552 通过、25 跳过、1 失败；唯一失败是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，新增进度链路无失败。阶段 1-4 完成，本轮未部署、未重启。
- 最终只读 `rg` 核对因双引号中的反引号触发了一次额外全量测试，随后正则报错；没有写入业务文件，测试统计保持 552/578 通过、唯一既有 UmaRace 依赖失败。
- 用户指出识图固定开场“嗯嗯，我先看看这张图。”不自然；已定位为 GoogleAnalysisTool 四句本地随机数组，并确认当前实现会等待开场发送后才开始图片处理。开始改为共享模型生成、与识图并行、完成即取消。
- 已移除四句随机识图开场，新增可选模型 `progressText` 和共享生成器调用；进度生成使用独立 AbortSignal 并以 `void` 与图片处理并行，工具结束时统一取消。共享守卫同时拒绝只含“收到/看看图片”的同类空话。
- 线上真实配置首次识图探针生成“报错的那几行提示我再看清楚点”，但用户没有说图中存在报错文字；已新增识图未读前事实守卫，用户未提及的报错、文字、按钮、界面、人物、颜色和卡住状态不能出现在开场，违规时同预算重写。
- 第二轮探针中模糊 Steam 请求两次不合格后正确保持静默；明确错误码请求生成“这个错误码我还在对”，事实虽安全但语序生硬，已把“还在对/还在看/还在确认”等状态机式半句加入机器人话术守卫。
- 原始输出探针确认守卫命中的是模型真实添加的“报错提示”“字有点小”等细节，不是误报；提示词已加入模糊 Steam 与明确错误码的正反边界，降低连续两次被拒后静默的概率。
- 最终真实配置探针得到“Steam安装这块，我仔细看看问题在哪儿。”与“错误码得看清具体是哪一个，我仔细辨认下。”；新增挂起进度 Promise 的行为回归，验证图片处理不等待开场且结束会中止信号。
- 服务器目标回归最终 4/4；首次因错误 cwd 缺 `config/default_config/`，改从 TRSS 根目录并使用 `--test-force-exit` 处理宿主后台句柄后通过。临时副本已删除，service active、PID 1586632 未变化。
- 本地全量 `npm test`：583 项、553 通过、29 跳过、1 失败；唯一失败仍是既有 UmaRace Puppeteer 路径缺失，本轮识图和共享进度断言无失败。阶段 1-4 完成，未部署、未重启。
- 用户明确要求“部署重启”；开始为搜索进度与识图开场两项修复建立精确部署清单、生产备份和安全重启验收，此授权覆盖此前只针对旧轮次的“不重启”约束。
- 部署完成：备份 `/opt/trss-yunzai-backups/bl-chat-contextual-progress-20260723-205024.tar.gz`，4 个生产业务文件哈希与本地一致；候选 staging 15/15、生产目录直接回归 15/15。
- 安全重启成功，PID `1586632 -> 1651465`；服务 active、34 插件、OneBotv11/NapCat 与 2536 监听正常。生产配置无发送探针分别生成“魔法模组的前五，我把更新时间再核对一遍。”和“图里的错误码我仔细辨认下具体是哪一串。”。
- 已删除 staging 和临时生产测试文件；AppleDouble 0、图片任务 0，启动日志无 SyntaxError、ERR_MODULE_NOT_FOUND、加载失败或未捕获异常。搜索与识图阶段 5 完成。
- SearchInformationTool 已接入共享生成器，搜索先完成会取消迟到进度；搜索 URL 同时复用 Chat Completions base URL 规范化。
- 首轮相关回归 13 通过、1 项因本机缺 `moment` 跳过、0 失败；覆盖真实话题注入、模型建议句不二次调用、固定机器人句拒绝、取消不发和搜索完成竞态。

# 2026-07-24 文生图上下文污染隔离
- 用户报告文生图偶发把无关上下文带进画面；开始审计三类绘图路由与最终 prompt 构造，目标是保留当前消息内的时段等画面语义，只隔离无关历史。
- 已完成共享上下文模式、合并消息筛选和最近绘图线程收敛；当前图片相关定向回归 48 通过、14 项因本机缺 TRSS 宿主跳过、0 失败，新增纯策略 14/14。
- 新增 4 个最终 prompt 端到端场景：合并补充、继续最新绘图、显式画聊天内容、引用隔离普通历史；本机按宿主缺失跳过，准备在生产隐藏副本验证。
- 生产旧文件已备份到 `/opt/trss-yunzai-backups/bl-chat-image-context-20260724-1205.tar.gz`。隐藏应用副本与正式生产文件分别完成同一组 18/18 回归，覆盖最终送给生图渠道的 prompt；正式 `apps/test.js` 与 `utils/promptCompiler.js` 哈希均和本地一致。
- 生产临时应用/测试文件已清理，service active 且 PID 仍为 1651465，按既有约束没有重启；当前运行实例需到后续正常重启才加载新代码。
- 本地全量 `npm test`：593 项中 559 通过、33 跳过、1 失败；唯一失败仍是既有 `tests/umaRaceBalance.test.js` 缺少 `/Users/shiqi/lib/puppeteer/puppeteer.js`。本轮新增上下文策略与生产宿主断言均通过。
- 用户明确要求“上线重启”，此前不重启约束被本次授权覆盖；开始执行图片任务排空、安全重启和新进程验收。
- 上线完成：重启前图片任务为 0，关键 4 文件线上/本地哈希一致；安全重启退出正常，PID `1651465 -> 1854936`，service active/running。
- 新进程已加载 bl-chat-plugin、MessagePipeline、锅巴、34 个插件、5 个监听器和 7 个适配器；OneBotv11 已连接并收到真实群消息，2536 锅巴入口返回 302。启动日志无 SyntaxError、ERR_MODULE_NOT_FOUND、加载/初始化失败或未捕获异常。

# 2026-07-24 骰娘完整性审计
- 用户要求先自行分析骰娘不完善之处；已建立只读审计范围，不修改业务代码，开始整理固定命令与自定义规则/团务双系统功能矩阵。
- 已完成插件入口和命令路由首轮盘点：确认中英文句点与紧贴参数策略、固定命令清单、规则包管理权限分层、动态规则兜底入口和私密消息交付路径；开始审计固定骰娘的数据/群边界和实际用户流程。
- 已完成固定骰娘状态与权限首轮审计：确认总开关覆盖不完整、reply/lock 假成功、群状态写入无管理权限、人物/DND 状态跨群跨卡、成长不落卡、先攻与团录流程不完整，以及单文件损坏后可能被空状态覆盖等缺口。
- 已核对固定骰式、检定、SAN 与其它规则骰：发现统一异常回复缺失、关闭状态仍可走暗骰、SAN 日期/阈值语义不稳、WoD/DX 仅为简化实现，以及现有测试只覆盖路由不覆盖业务语义。
- 已完成自定义规则主执行事务审计：确认动作提交边界可靠，但私密结果发送在提交之后且不可重试；主开关不覆盖规则运行时、玩家规则数据跨群共享、失败文案与真实状态范围不一致，catch-all 还会给所有未知点命令增加同步索引读取。
- 已审计 V2 目标、秘密字段和内置团务入口：发现 GM 在群内查看/设置 secret 会公开返回值、离群显式 GM 仍可能接收秘密结果、显式 QQ 目标不要求仍在群内，以及多类内置状态变更未进入结构化审计。
- 已核对团务、先攻与 log 生命周期：发现会话与 log 分两次提交、会话会停止既有手工 log、先攻可制造未正式开始却 active 的会话、重复开始会重复执行生命周期，以及新团默认继承旧先攻等状态机缺口。
- 已对照 schema、教程和 V2 测试：确认 secret 安全模型只约束调用者、不约束群消息受众，教程对“每个团务固定命令都有审计”的表述高于实现，现有测试未覆盖这些隐私与状态机反例。
- 已审计规则包迁移、回滚与配置面：确认回滚只切索引、缺少直达迁移时会静默改版本、多跳/反向和群字段迁移缺失，启用不预检既有数据，锅巴也缺少运行状态、恢复与隐私投递管理能力。
- 已运行 `diceCommandPolicy`、`diceRuleExpression`、`diceRulePackManager` 定向回归，34/34 通过；另以临时目录探针实际复现 lock 假成功、总开关漏拦截、公开 secret 规则被接受和缺迁移静默改版本，未触碰业务数据。
- 已只读核对生产：服务 active、两个骰娘开关均为 true、当前 1 用户/2 群、无活动 log，且尚无规则包或 V2 会话；发现生产真实 log txt 发送被 NapCat 以“识别URL失败”拒绝，长日志导出会退化截断。
- 已从原始需求与长期跑团用户视角补查能力边界：自动群名片仍是空壳；技能/物品/NPC 主要是账本；团务没有战役/场次历史/快照/内容资产；表达式缺少可组合骰池原语。线上核心文件与本地 8/8 哈希一致。
- 已完成代码/用户入口/文档/线上四层矩阵与严重度排序。本轮只改审计规划记录，未修改骰娘业务代码、线上配置或服务状态，准备向用户报告结论。

# 2026-07-24 “樊思睿是谁”回答失败诊断
- 已定位可见文案为 `chatFailureReply` 的通用 unknown fallback；开始追踪调用点和线上原始异常，本轮只读诊断。
- 已确认“X 是谁”有群成员名称匹配与资料 prompt 注入，未匹配也不应触发回答服务失败；根因范围收敛到主模型请求/响应恢复链。
- 用户中止该诊断并要求优先修复前述骰娘问题；未继续查询线上请求日志。

# 2026-07-24 骰娘完整性修复
- 已确认骰娘业务文件当前无本地差异，可与工作区其他脏文件隔离修改；开始阶段 1 的统一网关、权限与错误边界实现。
- 已确定统一入口实现方式：构造时包装固定 handler，集中处理总开关、群 reply 静默和未捕获异常；`.reply` 仅绕过静默以允许恢复，但群级开关修改仍需管理权限。
- 已复核固定人物卡实现：lock 当前误写在用户层且完全未读取，成长只生成数值不落卡；下一步先用业务反例测试固定契约，再修改实现。
- 已新增共享 `diceCommandGateway` 并在插件构造期包装全部固定 handler：`enabled=false` 统一拦截，群 `replyEnabled=false` 真正静默，`.reply on` 保留恢复入口，未捕获骰式异常转为不假装成功的自然错误回复。
- 已给群级固定写操作补主人/群主/管理员权限：reply、房规、COC 规则、log 开停/导出、先攻清空/删除；普通玩家自己的掷骰和人物卡不受影响。
- 已把人物卡锁定迁移到卡级并兼容旧用户级状态；`.st`、成长、标签和删除均尊重锁定。`.en` 使用卡内技能时，成功成长会事务写回并显示前后值；临时显式数值不会暗改人物卡。
- 已把 SAN 日期分桶改为可配置时区（默认 Asia/Shanghai），并固定保留当日初始 SAN 作为累计五分之一阈值，不再随当前 SAN 缩小。
- 已实现 `state.json` 写前校验、最近有效 `.bak`、损坏主文件恢复和隔离；主/备份均损坏时明确停写。定向骰娘回归 41/41 通过。
- 已把 `.sn` 从空壳改为真实的按群/按用户自动名片：启用前验证 QQ 成功回执，切卡和 `.nn` 后同步，关闭时尝试恢复原名；权限或适配器失败不会假报开启。
- 已把固定 log 导出改为完整文件上传（OneBot `base64://` 优先、`file://` 后备），增加大小上限、历史团录索引和按序号导出；交付失败明确说明未发出，不再回退为 4500 字截断假导出。
- 新增自动名片、完整 log、历史团录和失败交付反例；骰娘定向回归现为 44/44。
- 已修复 V2 目标与接收者边界：显式 QQ、@ 和引用目标必须仍在当前群；离群显式 GM 不再接收秘密结果，当前群管理员仍可作为接收者。
- 已收紧规则 schema：读取 secret 不仅要求 GM+，还强制 private/gm 可见性；包版本与迁移来源/目标采用语义版本，并支持显式多跳、群字段迁移。没有迁移路径时停止访问且不再静默改 `_packageVersion`。
- 已实现可恢复私密投递：先把投递项与规则状态一起提交，成功后确认删除，失败后保留错误和次数；新增「投递 状态/重试」固定入口。内置秘密卡/群字段也改为私聊，不在群里公开值。
- 已把 V2 人物规则数据按群隔离，并对旧无作用域数据做首次访问群认领；现有并发测试更新为两个群分别保持 HP=8/7，而不是错误合并成 5。
- 已把团务/log 启停合并到同一状态提交，记录 log 所有权并增加 50 场历史；复用手工 log 时不会在团务结束时停止。先攻必须先正式开团，重复开始会被拒绝，进行中的当前行动者不能直接删除或清空。
- V2 直接人物卡、权限、NPC、群字段、先攻编辑开始接入统一审计；定向回归在结构调整后仍为 44/44。
- 已完成 DND 卡级运行态、长休法术位、`.ri` 自动先攻、战役/章节/记录/暂停/快照/回退、物品/技能事件、骰池函数和规则身份名片请求的第一轮回归；54 项中 53 项通过。唯一失败是新增测试把 `{sender.card}` 的实际值“测试员”误写成“调查员”，实现结果符合规则定义，已修正测试期望。
- 修正测试期望后骰娘定向回归为 54/54。随后把完整文件交付抽成共享能力：规则包导出与 log 一样使用群聊 base64 上传或 `file://` 文件发送，失败时不再回退为截断 YAML；`.jrrp` 也改为使用骰娘配置的时区日期桶。
- 完整文件与时区回归加入后，骰娘定向测试为 56/56。帮助文本已补战役、团务快照/历史、投递重试、`.sn`、`.reply` 和历史 log 导出。
- 校对战役权限时发现“玩家登记自己”的分支虽写了免 GM 判断，却被前置 GM 守卫提前拦截；已把本人登记移到权限分流前，替其他成员登记仍要求 GM，并增加普通玩家端到端回归。
- 已按运行时逐节重写教程中的迁移链、群数据隔离、secret 权限与受众、私密投递队列、成功骰池、自动群名片、物品/技能事件、战役/章节/记录、团务暂停/历史/快照/回退和完整固定命令表；README 与内置帮助同步更新。
- 全量核查总开关时发现自定义规则虽不再执行，却会对已启用包静默返回 `false`；已把自定义入口接到共享门禁：已启用包在总开关/自定义规则关闭时给出真实状态，群 `.reply off` 时静默消费，未知点命令仍不抢占其他插件。
- 最终骰娘定向回归 57/57；核心文件语法检查和全工作区 `git diff --check` 通过。最终全量 `npm test` 为 623 项、589 通过、33 跳过、1 个既有 UmaRace 环境失败，唯一失败仍是本机缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，与骰娘改动无关。本轮未部署、未重启、未提交或 push。
- 用户进一步指出 log 导出仍表现为普通聊天。已收紧交互：成功导出只交付完整 `.txt` 文件，不再追加“已导出”聊天或日志正文；只有文件通道失败才回复真实错误。发送后的临时导出文件会清理，历史原始团录仍保留。

# 2026-07-25 群知识转述回复 AI 感审计
- 已开始只读还原“告诉斯卡蒂，沃基的教派是什么”的完整链路；优先区分搜索进度、点名工具、群知识证据和主模型自由发挥。
- 已从生产日志还原 09:30:39-09:30:59 全链路：前置语义分类错误强制搜索，进度参数守卫漏掉“正在搜索”，搜索将沃基错义为 Woke，最终总结又混用固定身份、长期记忆和上一条“逆天主教”群聊。确认不是固定回复或艾特发送失败。
- 阶段 1 完成，继续审计工具候选/分类优先级、工具结果 grounding、上下文证据层级和其他同类 terminal/进度入口；全程不改业务代码、不部署、不重启、不发群消息。
- 已完成阶段 2-4：确认表情包短聊候选意外启动全工具 Planner、候选过滤时序过晚、Planner 不可见内部高权威证据、搜索非空即成功、grounding 仅为软提示，以及转述未结构化是主要系统性根因。
- 已完成全仓同类出口扫描：终态艾特工具自由 `message`、搜索/绘图进度直接发送、固定失败模板和最终 regex 清理都可能绕过或无法完成语义级事实/自然度检查。
- 已整理 P0/P1 修复顺序和回归矩阵；本轮仅更新调查记录，未修改业务代码、未部署、未重启、未向群发送消息。只读 SSH 会话已退出。

# 2026-07-25 绘图开场任务语义修复
- 已定位精确回复为 BananaTool 的 `REFERENCE_IMAGE_PROGRESS_MESSAGES` 固定第三项；触发原因是“群里的翠月”被正确解析为群友头像参考，但进度层错误地把所有参考图都称为看图/改图。
- 已确定共享修复范围：Banana 文生图/头像参考生图和 GoogleImageEdit 真编辑统一使用上下文进度模型，增加生成任务不得声称看过原图或开始改图的事实守卫，并保持非阻塞与取消。
- 已完成共享实现：三类图片任务分别标记为 `image_generation`、`reference_generation`、`image_edit`；群友头像通过 `referencePurpose=member_avatar` 明确为外观参考。
- 已移除两套随机固定开场，改为当前模型结合用户原话生成；不合格输出重写一次后静默，且不会阻塞或拖慢实际图片请求。
- 已加入用户精确原话回归，错误句“图里的重点我看到了，我先按你的要求改成一张新的”会被拒绝，符合任务事实的新句可被接受。
- 已完成首轮静态复核：相关文件语法检查由前序执行通过，旧固定句已从业务代码移除，`git diff --check` 当前通过；继续核对并行进度的运行级时序测试。
- 本地定向执行 `contextualProgressReply`、Banana 队列、GoogleImageEdit 进度共 25 项：10 通过、0 失败、15 因缺 `/Users/shiqi/lib/common/common.js` 跳过；精确错误句重写、两次不合格静默和取消路径均通过。
- 已补 Banana 与 GoogleImageEdit 的完成即取消运行级回归；本地 27 项中 10 通过、0 失败、17 跳过。相关 7 个文件语法检查及 `git diff --check` 通过。
- 首次服务器 `/tmp` staging 语法检查通过，但工具级 import 把宿主路径解析为 `/lib/common/common.js`，测试仍为 10 通过、17 跳过；已定位为 staging 层级问题，准备换到宿主 plugins 下的隐藏副本。
- 换到真实 plugins 层级后 27 项全部执行，首轮 26 通过、1 失败；失败为旧测试夹具缺少“用户原话明确指定 Grok”，已补 `e.msg`，生产逻辑无须放宽。
- 第二次夹具用“用 Grok 把……”仍未命中既有显式渠道语法；已定位后改为测试原意对应的“用 Grok 改一下”，避免本轮混入渠道解析功能扩展。
- 服务器真实宿主层级最终定向回归 27/27：共享事实守卫、精确用户原话、群友头像参考模式、真正图片编辑、既有绘图队列与两个完成即取消竞态全部通过；0 失败、0 跳过。
- 服务器相关图片回归再通过 84/84，覆盖群内素材解析、画幅、provider/fallback、意图优先级、缺图守卫、图片 prompt 上下文、近期图片续改和 provider 信任；0 失败、0 跳过。
- 本地全量 `npm test` 为 627 项、590 通过、36 跳过、1 失败；唯一失败仍是既有 UmaRace 测试缺 `/Users/shiqi/lib/puppeteer/puppeteer.js`，本次新增与相关图片测试无失败。

# 2026-07-25 群 763694201 昨晚会话审计
- 已确认审计范围为北京时间 2026-07-24 18:00-24:00；服务器服务保持 active，PID 1860669，前一任务临时 staging 均已清理。
- 初步日志显示群内主要是材质包、MC 模组、图片和群友间对话；主动判断层多次选择不插话。正在单独抽取全部机器人出站消息并关联上下文。
- 普通出站日志暂只找到 2 条文字：19:38 三角洲命令结果、20:57 “别搞”；继续核对 NDJSON 归档、媒体 Outbox 和表情/合并转发出口，防止漏计。
- 已还原“别搞”：3 名群友依次复读后，Repeat 模块发送第 4 句；并非模型回答。继续审计 4 次 B站 MediaOutbox 合并转发的节点内容与发送时序。
- 发现机器人于 7 月 24 日 00:34 才入群，开始补查 00:00-06:00 的初次夜间会话，避免对“昨晚”时间范围作过窄解释。
- 全日普通出站已抽取为 17 条；正在分段还原夜间 13 条的直接上下文。已确认“你好菜”后的回避式回复与单图识别声称“第二张没显示”两项问题。
- 已还原 01:18-01:20：发现旧话题回复迟到后与定向回答连发，以及 compact 上下文中的指代丢失。03:40 明确绘图请求被误当轻松闲聊，只发口头确认、未调用生图工具。

# 2026-07-27 群聊会话链路系统性修复
- 用户要求把审计发现全部修复。已确认工作区包含大量既有未提交改动，本轮将在现状上增量修改，不回退图片、骰娘、媒体和表达学习改动。
- 已建立五阶段实现范围：真实绘图语序、素材归一化、迟到主动回复、副作用结果一致性、统一出站与短聊延迟。
- 已增强共享图片任务策略，覆盖“画一个……的画/画面”等真实长语序；显式生图在语义 Planner 前直接强制 BananaTool，没有可用渠道时如实失败，不再降级成闲聊或表情。
- 已按 QQ `fileid/file_id/fid` 和规范化 URL 去重图片，同一素材的 `spec/rkey` 变体只计一张，群上下文和识图 prompt 不再虚构“第二张”。
- 已让 emoji-only 候选不再触发前置语义 Planner；普通短聊保留主模型自主选择表情的能力，但不额外增加一轮分类等待。
- 已为主动插话保存原始消息锚点，发送前若群里已有更新消息则取消；点名、引用、命令和工具交付不受影响。
- 已增加结构化 `actionOutcomes`：群知识或群工作流实际保存成功后，即使后续模型超时/空响应，也优先回复真实的“记住了”结果，不再宣称整体失败。
- 已把 Agent 文本、Modrinth 合并转发、复读、命令和媒体合并转发接到共享 `[Delivery]` 回执日志；`apps/test.js` 只在统一发送方法内部保留一处直接 `e.reply`。
- 已缩短通用失败话术，移除“你的问题我完整收到了……”模板，并覆盖空 choices、空 message 和异常分支。
- 本地定向回归 46/46；完整测试 603 通过、36 环境跳过，仅有既有 UmaRace 宿主文件缺失失败。线上正式目录回归 46/46，相关图片真实宿主回归全部通过。
- 已备份并部署，安全重启中发现 staging 位于 plugins 会被重复加载，已移出插件目录并再次重启。最终插件/消息管道各初始化一次，服务 active，OneBot 已连接并持续收包。

# 2026-07-28 音乐合并转发完整文件交付
- 已确认音乐源会成功下载为本地 MP3；当前缺失来自把 base64 `file` 段直接塞入 `makeForwardMsg`，没有调用 `upload_group_file`，也没有可验证的文件发送回执。
- 已确定复用现有完整文件上传契约：群聊 OneBot base64 上传优先，`sendFile(file://)` 后备；成功后再组织音乐信息，失败时不得声称文件已交付。
- 已实现共享 `sendCompleteLocalFile()`，支持 OneBot 群/私聊文件上传、适配器后备、文件大小与空文件校验、明确回执失败和脱敏错误日志。
- 音乐发送现在先上传完整 MP3，成功后再发合并信息和试听；合并节点不再伪造 QQ 不支持的新文件段。定向回归 11/11 通过，语法与 `git diff --check` 通过；工具级集成测试需在真实 TRSS 依赖环境复跑。
- 已从线上 12:46:45 日志还原原故障：4.3MB MP3 被编码为合并节点内的 `type=file`，QQ 随后仅展示合并信息与 `record`，证实平台丢弃了伪附件。
- 服务器真实 TRSS staging 和正式目录集成测试均通过；3 个部署文件哈希一致。备份为 `/opt/trss-yunzai/backups/bl-chat-plugin-music-file-20260728-150518`。
- 部署前图片任务 0；安全重启后 PID=1016027、服务 active、插件和 MessagePipeline 各初始化一次，OneBotv11/NapCat 已连接并恢复群消息接收。
- 本地全量 643 项中 605 通过、36 跳过、2 个既有非音乐失败：消息归档测试临时 NDJSON 未生成，以及 UmaRace 缺宿主 Puppeteer 文件。

# 2026-07-29 群 821466122 会话链路修复
- 阶段 1 完成：确认 `member_avatar` 的引用图被 `hasReferenceImages` 错判为编辑；durable scan 与内存队列并发；引用意图的已有解析处于工具过滤之后。下一步按 P0/P1 分层修改并补回归。
- 代码边界复核完成：头像参考应保留真实参考图输入并采用独立 reference-generation 语义；队列只需在首个 await 前声明 active 状态即可消除恢复扫描竞态。开始实现与回归。

# 2026-07-29 群 821466122 异常会话审计
- 阶段 1 完成：确认用户需要线上群会话审计，生产目录为 `/opt/trss-yunzai/plugins/bl-chat-plugin`；采用只读取证，不修改、部署或重启。
- 阶段 2 进行中：生产服务 active（PID 1281625）；已提取 21:44-22:15 的群日志，发现文生图误归类为编辑通道失败、失败任务恢复错配和暧昧人设回复三类独立问题，继续关联精确任务状态与调用链。
- 已完成两项核心调用链还原：`member_avatar` 参考触发错误图片编辑分支；内存排队与 durable 队列扫描并发导致同一任务被“恢复”并重复执行。正在汇总全部机器人出站和群表达状态。
- 出站汇总完成：确认竞态反复造成假重启、重复图片和排队提示；另确认群名片展示泄漏到排队文案，以及 persona/群表达状态共同放大暧昧语气。最后核对 21:44 的历史上下文串题后完成根因排序。
- 阶段 2-4 完成：21:44 的“执行一次上面的内容”已确认因当前短句过滤遮蔽引用内绘图意图，导致模型自由回复。已输出 P0/P1 修复序列；本轮只读分析，未改代码、未部署、未重启。

# 2026-07-28 图片响应体卡死恢复
- 已只读还原群 953676639 的当前任务：17:04:09 开始、17:04:11 已发送进度，之后没有成图或失败；任务仍持久化为 `tool_running`，不是正常排队等待。
- 已定位到 Krill 连接的响应体卡死：当前 120 秒计时在收到 HTTP response 后提前取消，`response.text()` 无时间上限，导致持久队列无法进入 finally 清理。
- 已让 `fetchWithTimeout()` 在 response body 被 `text()` 完整读取前保留 AbortController，并将正文中止规范化为“没有返回完整响应”；普通连接超时语义保持不变。
- 新增正文卡死回归与 durable job 清理回归；服务器 staging 通过，正文卡死约 28ms 即失败并释放 Redis job、queue 和活动状态。一次复合 SSH 认证抖动后改为拆分连接完成验证，正式目录尚未改动。
- 正式正文超时补丁已同步且哈希一致，生产定向回归 16/16；服务安全重启至 PID 1190874、`bl-chat-plugin` 和 MessagePipeline 各初始化一次、OneBotv11/NapCat 已连接。
- 原卡死任务已由 durable queue 恢复并收尾：job/queue/active-task 全清；Krill 返回 `error/type` 而不是有效图片，终态为 `tool_failed`。
- 已上线恢复后失败通知：恢复任务不再静默失败，端到端回归确认会向原群发送真实失败说明。生产真实 TRSS 回归 17/17、业务与测试文件哈希一致，`trss-yunzai.service` active（PID 1198400）、OneBot/NapCat 已连接；原任务已经结束，未重放原始绘图请求，避免重复提交。
# 2026-07-29 群 821466122 修复

- 已完成图片任务类型、durable queue 互斥、引用绘图路由、传输层昵称过滤和人格收敛的实现。
- 定向测试第一次执行仅失败于人格收敛对“有点不好意思”省略主语的写法未覆盖；已扩展正则后待重跑。
- 线上首轮备份在旧版本缺少新增测试 `imagePromptContextPolicy.test.js` 时停止，尚未传输业务文件；改为仅备份线上已有生产文件后继续。
- 已备份并同步 5 个生产文件和 3 个新增回归文件；首次线上加载暴露 `emojiToolPolicy` 缺少 `resolveForcedReactionEmoji` 导出，服务未重启。补齐该依赖并单独备份后，生产宿主回归为图片队列 20/20、路由/提示词 24/24、人格收敛 8/8，均通过。
- 已安全重启：`1281625 -> 3015852`。重启前后 durable 图片任务均为 0；服务 active、34 个插件、MessagePipeline 单实例、OneBotv11 已连接。四个运行文件 SHA-256 均与本地一致；新进程日志未发现 SyntaxError、模块缺失、未捕获异常或插件加载失败。

# 2026-07-29 群 981339693 多图识别超时修复

- 22:32:42 的真实请求引用合并转发，正确提取 3 张图片并强制走 `googleImageAnalysisTool`；主视觉模型 `gpt-5.6-terra` 在 46.421 秒后因 45 秒超时失败，下载阶段没有报错。
- 当前失败话术错误复用了旧 Steam 话题。下一步核对可用备用候选和多图降级，不能因一次视觉超时编造解释。
- 已实现多图逐张并行识别和部分成功标记；本地语法/格式检查通过。线上已备份并同步工具与回归文件，但第三次独立 SSH 认证触发服务端限流，验证尚未执行，运行服务未重启。
- 认证退避后，生产工具和测试哈希与本地一致；真实 TRSS 回归 10/10，通过多图逐张、部分超时标记、进度并发与结构化失败事实边界。准备重启加载。
- 已重启加载：`3015852 -> 3025224`，服务 active、34 个插件、MessagePipeline 单实例、OneBotv11 已连接，识图工具哈希与本地一致。重启时发现两项既有 durable 绘图任务；首项已恢复完成并成功发送图片，未重复提交。恢复提示早于 OneBot 就绪而发送失败，但终态交付正常。
# 2026-07-29 工具等待回复去机器人化
- 已复核：问题集中在紧凑的工具进度路径而非主聊天人设。当前开始收紧状态播报过滤，并为改图/识图补上既有 persona guard。
- 首轮定向测试的两处失败均为旧文案断言：图片空结果本来走识图专用分支，通用失败仍含“可靠结果”汇报词；已按真实分支修正并准备复跑。
- 定向回归完成：`contextualProgressReply` 和 `toolResultGrounding` 共 18/18 通过；4 个改动模块均通过 `node --check`，`git diff --check` 通过。改图/识图运行级测试在本地因缺少 Yunzai 的 `/Users/shiqi/lib/common/common.js` 依赖跳过，未把跳过误报为验证成功。
- 已备份并同步 4 个运行模块及 2 个定向测试到生产；生产定向回归 18/18，线上源文件 SHA-256 与本地一致。备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-tone-progress-20260729-232330`。
- 已安全重启 `trss-yunzai.service`：PID=`3056052`、状态 active、插件初始化一次并加载 34 个插件；OneBotv11/NapCat 于 23:24:51 重连，未见语法或模块加载错误。

# 2026-08-03 群 609235590 合并转发核查
- 已完成只读取证：最新转发 ID=`7669689123275068986` 未被展开且未持久化节点内容，根因是 forward-only 消息先走 `empty_content` 预筛。未修改、部署或重启服务。

# 2026-08-03 合并转发持久化修复
- 已实现消息管道展开合并转发并存入 `forward_context` 段；首轮 24 项核心回归通过。`apps/test.js` 语法检查发现已有同名 `extractForwardIdsFromSegments` 工具函数与新导入冲突，已改为复用本地函数后准备复跑。
- 修复后本地 24/24 通过，所有目标模块语法及 diff 检查通过。部署包已上传；首次远端执行因 SSH 密码临时限流失败，尚未修改生产文件。
- 补充 OneBot 未就绪时的可重试保护后，本地和线上定向回归扩展为 25/25。已安全重启并把唯一旧 forward 事件重置为只重建上下文；随后读取结果时再次遇到 SSH 临时认证限流，服务仍为 active，等待连接窗口恢复后验收。
- 首次旧事件重放完成但 `forwardContext=null`，日志显示 OneBot 在任务完成后才重连；原因是重连期间适配器返回空节点列表而非错误。已把“存在转发 ID 但零节点”纳入同一可重试边界，准备复测。
- 运行日志确认回调已注入；真正遗漏是 OneBot 未就绪时 `pickGroup` 抛出普通异常，`collectForwardContext` 外层未将其标为可重试。已把整个解析过程统一包装为 `forward_context_unavailable`，避免再被吞掉。

# 2026-08-03 受限磁链下载工具
- 已确认工具将复用 `sendCompleteLocalFile()` 交付完整文件，并以种子元数据作为下载前强制配额检查点。
- 线上只读检查确认没有可调用的 BT 下载器；后续实现先提供明确依赖错误，部署阶段再安装 `aria2c` 并做受限样例验收。
- 已新增受限下载工具、元数据 bencode 解析器与回归夹具；先元数据、后正文的总大小/单文件/文件数硬限制已接入后台终态工具与 QQ 完整文件上传回执。
- 改为单个 ZIP 交付：先上传真实可下载 ZIP，随后发送两节点合并转发（目录清单、压缩包说明）；不把 QQ 会丢弃的 file 段塞进合并转发。ZIP 上限也在下载前检查，避免超限内容被拉取。
- 定向语法、工具 Skill、语义路由和下载回归通过 33/33；覆盖超限不启动正文、ZIP 上限预检、模型不可替换磁链、错误 URL 脱敏和符号链接拒绝。线上尚未部署，运行时仍需安装 `aria2c` 与 `zip`。
- 大小配额已收紧为代码硬上限 50 MB（50,000,000 bytes）：总内容、单文件和最终 ZIP 都不能被部署配置提高到该数值以上。
- 已定位纯磁链被 `TimingGate` 当作普通分享而 `no_action`；本地改为有效 BTIH 磁链自动绕过闲聊门控、确定性调用下载工具，并禁止与其他磁链合并。待回归与部署。
- 已部署到 `/opt/trss-yunzai/plugins/bl-chat-plugin`：线上安装 `aria2c` 与 `zip`，生产目录定向回归 28/28、工具注册验证通过，服务已安全重启为 PID `1852499`。旧版本收到的两条磁链不会回放；等待新的真实磁链验证自动群交付。
- 首次真实自动调用确认路由正常，但部署包中的线上 `message.yaml` 只写入了工具列表、遗漏 `torrentDownload` 配置块，导致工具如实拒绝 `enabled=undefined`。已原子补齐配置并以 YAML 和 `TorrentDownloadTool.configProvider()` 双重验证 `enabled=true`、自动识别开启、50 MB 上限及 `aria2c`/`zip` 路径；无需重启。
# 2026-08-03 磁链元数据持久化解析

- 已开始：将一次性 81 秒发现改为持久化、低频退避的元数据解析任务；仅在成功、超限或任务明确不可恢复时通知群。
- 已完成首次授权探测：原磁链 DHT 25 秒无连接、无做种、无元数据；下一步实现跨重启低频重试。
- 已实现持久化任务核心：Redis 去重、重启恢复、低频退避以及元数据不可用与下载终态的分流；正在补持久化回归。
- 本地 21 条磁链回归通过；首次部署连接遭遇 SSH 密码认证临时拒绝，尚未向线上写入文件或目标磁链任务。
- 第二次 `scp` 传输仍被认证层拒绝，但同一时段的交互式 SSH 健康检查成功；改用单 SSH 连接的 tar 流传输，避免多连接认证抖动。
- 用户已澄清不需要持续解析；已在线上删除误创建的目标 Redis 任务并确认不存在，转为寻找和验证能立即解析该可用哈希的实际来源。
- 已找到可用种子缓存并定位工具未命中原因：Node fetch 拒绝该 HTTPS 源的重定向。下一步将改为仅允许 HTTPS 重定向、限制跳转次数、仍做 info hash 校验，然后把缓存发现前置。
- 已完成并上线：改用可直连的 HTTPS 缓存域名并将其前置；目标磁链真实元数据解析成功。已删除持久化重试代码与线上 Redis 记录，服务 active，线上回归 19/19。
- 已核对无需下载即可提取的字段范围；后续可将目录、单文件大小之外的创建信息、备注、制作工具、tracker/web seed 状态和分片信息纳入只读元数据卡片。
# 2026-08-03 磁链按文件选择下载
- 已恢复上轮上下文并核对 `utils/torrentDownload.js` 语法与 bencode 读取段落，未发现此前担心的重复 `valueEnd` 声明。
- 已确认生产既有解析路径和 50 MB 约束仍在；开始实现短期选择会话与定向下载。
- 已完成：文件索引、选择会话（群/发起人隔离、30 分钟 TTL）、重新验证 BTIH/元数据、aria2 `--select-file` 和仅包含选择内容的 ZIP。
- 已接入“下载 1,3 / 下载第2个”确定性工具路由；无待选清单、过期、歧义和超限组合均明确失败且不下载。
- 验证：磁链与意图定向测试 35/35 通过；全量测试的两项失败均为本机既有依赖/归档环境问题，未部署、未重启线上服务。
- 已上线：仅同步 `TorrentDownloadTool.js`、`torrentDownload.js` 和 `toolIntentManifests.js`，未覆盖线上配置；服务重启后 active（PID 1987584），34 个插件加载完成。
- 线上无发送探针确认“下载第2个,3”解析为 `[2,3]`、aria2 参数含 `--select-file=2`、待选 TTL 默认 1800 秒、选择后文件总大小正确。

# 2026-08-03 磁链清单展示优化
- 已仅修改用户可见清单样式：完整列出全部文件、每项独立换行显示大小，并标记可下载或不可下载及原因；底部只展示可用编号，避免把隐藏的大文件误认为可选。
- 本地 `tests/torrentDownload.test.js` 23/23 通过，示例输出确认 6.5 GB 种子仅显示“下载 1”。
- 首次线上同步尝试被服务器临时拒绝密码认证，尚未替换文件或重启服务；准备使用显式密码认证重试一次。
- 第二次使用显式密码认证同步成功；已备份线上旧文件至 `/opt/trss-yunzai-backups/bl-chat-plugin-torrent-selection-display-20260803-192623`，安全重启后服务 active（PID 1996381）、加载 34 个插件。
- 线上无发送展示探针通过：可下载/不可下载条目、GB/MB 大小、逐项换行和“只能填写可下载编号”的提示均已验证。

# 2026-08-03 磁链文件私聊交付
- 已改为群内磁链下载完成后将 ZIP 私发给原发起人；群内不再上传群文件或发送目录合并转发，只发送一条已私发确认。私聊触发保持在私聊交付。
- 本地磁链回归 23/23 通过，覆盖交付事件去除 `group_id`、保留触发人 `user_id`，确保文件上传进入私聊路径。
- 已同步线上并安全重启；备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-torrent-private-delivery-20260803-193351`，服务 active（PID 2004394）、加载 34 个插件；无发送探针确认交付事件为 private。
## 2026-08-03 单数群主艾特路由修复
- 已定位生产根因：语义分类把单数“群主”误映射到 all-admin 工具，并带 `includeOwner=true`。
- 已实现：新增纯角色路由模块，主链路以当前群 `role=owner` 成员构造精确成员艾特；待运行回归测试和部署验证。
- 本地定向回归 `node --test tests/mentionRoleRouting.test.js tests/mentionMembersTool.test.js tests/mentionAdminsTool.test.js tests/toolIntentManifests.test.js` 为 23/23；`node --check` 和 `git diff --check` 均通过。
- 线上首次整体同步入口文件因本地未同步依赖导致插件未载入，已立刻以备份恢复；之后以生产原文件为基准应用最小路由补丁和工具清单补丁。线上定向回归 23/23，最终重启后服务 active、PID 2096627、插件初始化一次、加载 34 个插件。
## 2026-08-03 科普回复文本化与结构化修复
- 已完成线上取证：群 609235590 的 MC Mod 科普被发送层强制转成长图；模型原文已从服务日志恢复。
- 已开始实现：科普请求将直接保留文本，技术解释策略补充 MC 模组的版本/加载器、目录与注册、运行侧和调试发布边界；待执行定向回归与最小线上补丁部署。
- 首次 `solutionExplanationStyle` 定向测试仅因既有断言仍匹配旧章节文案失败；实现输出已包含新结构，现已同步断言后重跑。
- 本地与线上回归均为 5/5。线上已部署最小入口补丁和 `solutionExplanationStyle.js`，最终重启后服务 active、PID 2114154、插件初始化一次、加载 34 个插件；未向群发送测试消息。
# 2026-08-03 科普知识卡版式修正
- 用户明确指出上一轮目标被误解：不是科普改为普通文本，而是图片卡面内部需要有规范、具体、易扫描的格式。
- 已确认错误入口为 `apps/test.js:getTextImageTemplateForFinalReply()` 的 `if (userAskedForEducation) return false`，以及 `solutionExplanationStyle` 中禁止自动转图的提示语。
- 修复计划：新增 `knowledge` 专用模板，按 Markdown 语义呈现标题、摘要、步骤、清单、提示和代码；教育类请求路由到 `knowledge`，代码/Markdown 保留 `document`。
- 已实现：`TextImageTool` 新增 `knowledge` 模板；教育类回复从错误的 `false` 路由到 `knowledge`，并同步撤回技术说明提示词中“禁止自动转图”的指令。
- 首轮定向测试唯一失败为旧测试仍断言已撤回的“自动转成图片卡面”文案；已更新为断言结构化知识卡行为。
- 本地：语法检查、`git diff --check`、两组定向测试通过；本机缺 Chromium，截图用例跳过。
- 线上：先用最小补丁替换三个运行文件，再以 Chromium 实测 knowledge 卡渲染成功（1960 x 2106 PNG），随后重启服务。启动日志中发现的 `SyntaxError` 来自 `apps/._test.js`；已确认及隔离全部 7 个 AppleDouble 文件，二次启动无加载错误。
- 线上定向测试最终通过：技术说明策略 5/5、文档/知识卡渲染 3/3；服务保持 active。
# 2026-08-04 MC 知识卡延迟诊断
- 已完成线上真实请求取证，不修改代码。两次相同 MC Mod 入门请求的端到端耗时为约 17.6 秒和 23.3 秒；首轮模型分别占 13.1 秒和 19.6 秒，知识卡截图与 QQ 图片发送约占 3.5-4.3 秒，消息合并固定约 1 秒。
- 根因不是服务器负载，而是没有工具需求的科普请求仍携带唯一表情包工具进入 `toolsAiConfig` 的工具模型。输入提示本身为 11k-12.7k token，模型直答但已承担工具模型路径与推理开销。
- 待用户授权实施时，优先做无工具知识请求的直聊模型路由，再补卡面 render/encode/send 细分指标；不得为追求速度改回纯文本或取消知识卡。

# 2026-08-04 知识解释执行路由治理
- 已从延迟取证确认当前问题是“候选工具存在”与“应走工具模型”被同一个 `session.tools` 数组混用。新增纯 `replyExecutionRoute` 策略，准备由入口传入响应类型、候选工具、强制动作和显式表情请求；该策略不包含任何具体业务工具实现。
- 已接入初始请求：强制/真实工具动作完成选择后，知识解释若只剩可选表情包工具，会显式切到 `tool_choice=none` 并清空本轮工具，进入既有聊天后端；模型耗时日志新增 `route` 和 `routeReason`。
- 已补知识卡分段观测：`textImageTool` 现在记录 render、QQ send 和 total，便于将模型延迟与卡面阶段延迟分开比较。
- 已部署：线上最小补丁预检、语法检查、TRSS 根目录 mock API 路由测试和 Chromium 知识卡截图测试通过；服务重启后 active、插件和 MessagePipeline 初始化成功、加载 34 个插件。备份：`/opt/trss-yunzai-backups/bl-chat-plugin-execution-route-20260804-150000`。

# 2026-08-05 统一群记忆链路续作
- 确认语义输出已覆盖 alias/knowledge/workflow，但 runtime 仍串行 await 裁决并分三次写 Redis。下一步新增单一提交网关、来源级删除和授权冲突策略，再把裁决改为短窗口并发，不让正常回复等待完整模型超时。
- 已完成统一提交：新语义提案在 `commitGroupMemoryDecision()` 的单群队列中一次提交 aliases、knowledge、workflows；共享 `proposalId`/来源，并保留创建者。普通成员不能覆盖他人定义或创建执行工作流，群管理可维护工作流和冲突条目。
- 已完成删除收口：自然语言忘记支持同源定义、别名、工作流的级联删除；普通成员限本人，群管理可删除精确他人条目。歧义候选返回正确类型说明。
- 已完成非阻塞化：主链路最多等 180 ms，超过后继续正常回复且不说“记住了”；裁决总超时默认 900 ms。明确 @ 目标与说话人均进入候选身份。
- 回归：目标测试 72/72、语法和 diff 检查全通过。广测的新增断言已修正；其余 3 个环境/既有失败（`moment`、TRSS/Puppeteer、归档时序）未归因于本改动。线上仍未部署，因为现有 SSH 密码认证被拒绝，未重试或重启服务。
- 线上部署完成：认证恢复后先读取了生产旧链路，确认仍存在 `extractExplicitTeachingFacts` / 正则工作流直写。只在生产原始 `apps/test.js` 的记忆区块上生成定点补丁，未覆盖同文件其他功能；同步 `MemoryManager`、`groupKnowledge`、`extractor`、`aliasRegistry`、Forget 工具共 6 个运行文件。备份：`/opt/trss-yunzai-backups/bl-chat-plugin-group-memory-20260805-180000`。
- 线上 staging 六文件 `node --check` 与本地 SHA-256 一致；生产目录定向回归 59/59。重启首次连接在 SSH 认证层失败、未执行远端命令；改为仅密码认证后重启成功。服务为 `active`、PID 880762，重启后 MessagePipeline 连续处理新事件，启动后日志未见 `SyntaxError`、`ReferenceError`、`ERR_MODULE_NOT_FOUND` 或未捕获异常。
# 2026-08-07 对外失败原因统一展示（续）

## 2026-08-10 人设模板去重与模型路由范围修复
- 已确认身份话题的重复回复来自模型输出，不是 `repeatJoin`；生产日志和入口提示词均已定位。下一步审计实际模型配置和路由日志。
- 生产模型取证完成：聊天与工具模型配置不同，但普通闲聊由于仍附带可选工具而被 `YTapi` 强制送入工具模型。准备在入口路由边界修复并补回归。
- 根因细化：`sendLocalEmojiTool` 的 casual 候选被 `createTurnPlan()` 错当作 required capability，令“希洛也是 AI 吗”成为 `mode=tool`。修复方案是将非显式表情保持 optional，并为短、无工具、非复杂闲聊新增显式 `taskAiConfig.casual` 路由位；该位未配时保持主聊天模型回退。
- 已完成实现前路由审计：`toolsAiConfig` 只应承担实际工具调用和复杂升档；`trackAiConfig` 只应承担对话追踪，不能作为隐式对话生成后端。将新增可见、可配置的短聊后端。
- 复核 `createTurnPlan()`：显式表情、强制工具及锁定工具仍走工具模型；仅可选表情的短闲聊会在发请求前清空工具，使用 `taskAiConfig.casual`，未配置时回退 `chatAiConfig`。待以生产文件差异和定向回归完成上线验收。
- 本地定向回归：22 通过、4 跳过；跳过原因是本检出缺少既有 `moment` 依赖，导致 `agentApiRouting` 无法加载宿主依赖。`apps/test.js`、`turnPlan.js`、`apiClient.js`、Guoba schema 的语法检查与目标 diff 检查通过。首次线上只读 SSH 认证被拒绝，未执行任何远端写入或重启；将改查本机既有 SSH 身份与配置后再尝试。

## 2026-08-11 长对话卡面 Markdown 代码渲染
- 已确认需求：长对话的图片卡面应将代码块作为代码排版，而非将 Java 示例压成普通文本。入口初步定位为 `functions/functions_tools/TextImageTool.js`，下一步直接审计模板、解析器与 `textImageDocument` 回归。
- 一条宽泛 `rg` 命令因包含 Markdown 围栏符号产生 shell 的 unmatched quote；未发生文件写入或代码变更，后续使用目标文件读取和无围栏搜索。
- 审计完成：`TextImageTool` 已支持有效围栏代码，但用户消息中的 `java` 语言行及其 enum 代码没有围栏。当前无围栏识别仅支持整段 Python/JS/JSON/YAML/Bash，混合说明与 Java 代码会完整被当成正文。将以解析前规范化明确语言标记后的连续代码段实现，不改变有效 Markdown 的既有路径。
- 已实现：新增 Java 高亮和语言识别；`java`（也兼容被单引号或反引号包住的语言行）后面的连续 Java 代码会被规范化为内部围栏。document、knowledge 与聊天气泡均复用该解析路径，普通说明行不会被误判为代码。
- 本地验证：`node --check`、目标 diff 检查和 `tests/textImageDocument.test.js` 均通过（4 通过、2 跳过）。新回归使用用户的 `BizCode` enum，断言 document 和 knowledge HTML 都输出 `code-block` 与 `java` 标签。两个截图用例只因当前检出未找到 Chromium 而跳过，正在尝试系统浏览器路径完成视觉检查。
- 使用系统 Google Chrome 实际截图后发现 enum 常量以逗号结尾时会在 `public enum` 后被截断，其余行回落为正文。该缺陷已定位到 Java 行识别规则，正在修复并补充“全部 enum 内容必须在同一代码块”回归。
- 首次截图清理由环境拒绝 `rm -f` 命令，未影响工作区或源码；后续渲染通过测试内置的 `finally` 清理，避免使用该命令形式。
- 修复后验证：显式指定系统 Google Chrome，`tests/textImageDocument.test.js` 的 6 项全部通过，原先的两项 document/knowledge 截图测试也已实际运行。再以用户的 `BizCode` 示例生成 document 卡面并人工检查：说明和列表正常排版，完整 enum（包括逗号结尾的常量、字段和结束大括号）都位于同一个带 `JAVA` 标签的深色代码块中。
- 用户请求上线。已完成本地目标文件 SHA-256、diff 检查和部署前预检；生产 `root@124.223.95.142` 仍返回 `Permission denied (publickey,password)`。未同步文件、未创建远端备份、未重启服务。等待可用认证后执行最小部署：`TextImageTool.js` 与目标回归文件，随后运行生产语法/渲染检查并验证 `trss-yunzai.service`。
- 用户提供部署认证后，首次 `sshpass` 预检错误使用了非标准环境变量名，命令在本地因 `SSHPASS` 未设置退出；没有建立远端连接、备份、同步或重启。已更正为 `SSHPASS` 后重试。
- 认证成功，已创建生产备份 `/opt/trss-yunzai-backups/bl-chat-plugin-markdown-java-20260811-195514` 和临时上传目录。首次上传后校验错误引用了远端未定义的 `text_hash` 变量，在 `install` 前退出；生产运行文件未替换、服务未重启。将本地 SHA-256 作为校验常量传入远端后重试。
- 已上线：只同步 `functions/functions_tools/TextImageTool.js` 与 `tests/textImageDocument.test.js`。生产 SHA-256 与本地一致；生产 `/usr/bin/chromium` 下的 `tests/textImageDocument.test.js` 为 6/6 通过。安全重启后 `trss-yunzai.service=active`，最近启动日志未见 `SyntaxError`、`ReferenceError`、`ERR_MODULE_NOT_FOUND` 或插件加载失败。

## 2026-08-11 YouTube 授权后台配置
- 用户要求在后台可配置会失效后需要更换的 YouTube 授权。实现范围为 Guoba 密码字段和自动搬运实际传参，支持登录 Cookie 与 PO Token，且禁止把授权值写入日志、回归输出或群消息。
- 审计完成：生产 `yt-dlp` 已安装，但 YouTube 搬运链路完全匿名，且不存在授权配置。将新增 `youtubeRelay.cookieHeader` 与 `youtubeRelay.poToken` 的 Guoba 密码字段；两者通过临时私有文件用于 metadata 与下载，避免进程参数和日志泄露。
- 已确认主聊天、图片、搜索、网页解析、骰娘、知识库和三角洲工具已统一使用 `visibleFailure`。
- 正在收敛仍直接向用户回复通用失败或原始 `error.message` 的 B 站授权/搬运、音乐交付、消息记录、表情包管理与长消息投递出口；继续保持脱敏边界，禁止泄漏密钥、签名参数、磁链和内部路径。
- 已完成本地出口收敛：直接命令和统一 `runToolCall` 均使用同一格式化器，旧工具返回的失败字符串也会在进入模型前脱敏。定向回归 30 通过、4 项因本机缺少既有 `moment` 依赖跳过；目标模块语法检查和 `git diff --check` 通过，准备上线。
- 已上线到 `/opt/trss-yunzai/plugins/bl-chat-plugin`。首次包携带了 macOS AppleDouble `._*.js` 且漏传了既有新增依赖，主插件未加载；已将这批伪脚本移动至本次备份目录，补齐依赖并使用无 AppleDouble 包二次部署。最终服务 `active`、`bl-chat-plugin` 初始化成功、加载 34 个插件；无群消息脱敏探针通过，启动后日志无 `SyntaxError`、`ReferenceError`、`ERR_MODULE_NOT_FOUND` 或插件载入错误。

# 2026-08-07 上游过载话术补充
- 线上实际回复暴露无状态码错误 `Our servers are currently overloaded. Please try again later.`。当前 formatter 未识别该文本，导致英文直出并走未知错误模板；正在改为中文的“上游服务当前负载过高，请稍后重试”。
- 已完成：过载文本在 formatter 和请求恢复分类中归为 `upstream`，聊天回复改为“回答服务现在有点忙，这次请求没等到结果”。本地定向回归 16/16；仅同步三个相关模块，线上服务重启后 active，无群消息探针输出中文且不含英文原文或“消息没丢”，启动日志无模块加载错误。

# 2026-08-07 人格自主性与关系边界
- 已定位：系统只给了“害羞、熟人感、接梗”等风格，缺少关系不能由单句命令建立的原则；`persona.preferences` 未被主提示读取。正在接入人格自主性、稳定偏好和固定边界，并以回归覆盖。
- 已复核本地实现：`utils/personaTonePolicy.js` 使用 `persona.preferences` 与 `persona.boundaries`，`apps/test.js` 将实际配置传入。`node --test tests/personaTonePolicy.test.js tests/personaFeedbackManager.test.js` 为 12/12；`node --check` 和 `git diff --check` 通过。下一步读取生产覆盖配置后最小化同步。
- 线上已备份到 `/opt/trss-yunzai-backups/bl-chat-plugin-persona-autonomy-*` 并同步最小补丁。第一次远程修改入口时，自动文本定位没有命中；第二次定点插入缺少对象字段逗号，`node --check` 在重启前拦截，已将上一行补逗号后通过。远端 `apps/test.js` 和 `personaTonePolicy.js` 均 `node --check` 通过，线上已有定向回归 8/8。
- 已重启并验证：`trss-yunzai.service` 为 active，`bl-chat-plugin` 正常初始化并加载 34 个插件，日志未见语法/引用/模块加载异常。远端策略文件 SHA-256 与本地相同；本地最终人格提示探针通过。

# 2026-08-08 骰娘安全随机源统一
- 诊断完成：线上群 `953676639` 的六次 D12 结果高度聚集，但日志确认是独立消息和独立执行；当前普通骰直接使用 `Math.random`，未发现缓存、固定用户种子或全局覆写。用户已授权改为加密随机源，待实现和部署。
- 已完成本地实现：新增 `diceRandom` 加密随机模块，替换 `DiceManager` 的标准骰/检定随机、表达式默认随机和规则包表/表达式的后备随机。规则包调用级加密种子与审计记录保持不变。语法检查、`git diff --check`、三组骰娘回归 56/56 均通过；12 万次 D12 分布正常。待最小部署与运行态验证。
- 已部署：仅同步 `utils/diceRandom.js`、`DiceManager.js`、`DiceRuleExpression.js`、`DiceRulePackManager.js`；线上备份 `/opt/trss-yunzai-backups/bl-chat-plugin-dice-crypto-20260808-110349`。远端 `node --check` 通过，且在运行探针中让 `Math.random` 抛错后标准骰、表达式和规则包后备均成功执行。安全重启后 `trss-yunzai.service=active`、`bl-chat-plugin` 初始化成功、加载 34 个插件，未见语法/引用/模块加载错误。

# 2026-08-09 YouTube 与 Pixiv 自动媒体解析
- 已开始：用户确认第二个工具是 Pixiv 图片解析。将与 YouTube 一起接入现有 B 站/抖音自动媒体管线，而非新增命令式实现。
- 已完成第一轮入口审计：事件先 raw-detect 再进入 outbox，outbox 已有跨群 artifact reuse 与合并转发，但只硬编码 B 站/抖音。将以显式平台适配表接入新来源。
- 已完成归档与交付契约审计：归档需要同步新平台的稳定字段和可读文本；Pixiv 下载图片可直接加入信息节点，YouTube 继续走既有 video 段内联和 artifact reuse。
- 已完成核心实现：新增 YouTube/Pixiv 识别、富化、历史格式化和受限 relay；outbox 改用平台适配表统一选择元数据刷新、下载、清理和展示。待补回归并执行本地/生产依赖验证。
- 生产依赖核验：`ffmpeg` 存在，`yt-dlp` 不存在。首次以 Python pip 安装失败，系统未装 pip；将改用 apt 安装，不重复同一路径。
- 已改用 apt 成功安装并验证生产 `yt-dlp 2025.04.30`；本地定向测试 40/40 与静态检查通过，准备最小化同步运行模块。
- 已完成部署和最终修正：增加 YouTube 网络超时的可见真实原因与 yt-dlp socket timeout；本地定向回归 41/41，生产既有回归 28/28、无发送解析探针和语法检查通过。服务最终为 active，启动日志无模块加载错误。
- 线上完整规则包测试出现 5 个历史版本不一致的失败，未阻止本次上线：本地对应现行测试 56/56 通过，线上骰点/表达式相关测试与实际加密随机探针通过。
# 2026-08-11 YouTube 授权后台配置
- 完成阶段 2：新增 `utils/youtubeAuth.js`，将 Cookie/PO Token 写入 owner-only 临时文件，通过文件路径交给 yt-dlp，并在调用结束后删除；元数据和下载两条调用链均已接入。
- Guoba 消息管道页新增 `youtubeRelay.cookieHeader` 与 `youtubeRelay.poToken` 密码输入框；默认配置为空，不含任何凭据。
- 移除了项目未使用的 `SOFT_GROUP_END` schema 标记；临时目录改为 finally 中递归删除，覆盖认证文件写入中断路径。
- 本地验证通过：`node --check` 覆盖所有改动 JS，`node --test tests/youtubeMessage.test.js` 为 6/6；schema 导入断言确认两个 YouTube 授权字段存在。
- 部署尝试 1：生产目录没有既有 `tests/youtubeMessage.test.js`，备份命令在替换前退出；未改动运行文件。后续备份仅覆盖线上已有文件，测试作为新增回归文件部署。
- 阶段 3 完成：最小部署了 3 个既有运行文件、1 个新增授权辅助模块、Guoba schema、生产配置空字段和定向测试。备份位于 `/opt/trss-yunzai-backups/bl-chat-plugin-youtube-auth-20260811-203444`。
- 生产验证：`node --check` 通过，`node --test tests/youtubeMessage.test.js` 为 6/6，schema 断言通过，部署前后 SHA-256 一致；服务重启后 `trss-yunzai.service=active`，日志显示 `bl-chat-plugin`、MessagePipeline、Guoba 正常启动，无模块加载或语法错误。
- 清理尝试：执行环境拒绝 `rm -rf` 临时目录；部署未受影响，改用限定路径的 `find -delete` 清理。

# 2026-08-11 YouTube 失败卡片信息修正
- 完成阶段 1 的本地定位：失败卡片同时存在重复标题前缀和将不可用元数据渲染为“未命名视频”的问题；待读取生产原始 yt-dlp 错误后决定最精确的失败文案。
- 生产探针确认 `cookieConfigured=false`、`poTokenConfigured=false`。首次诊断脚本对空字符串调用 `replaceAll`，把错误文本全量遮盖；此错误仅在探针中，未影响插件或服务，后续仅对非空敏感值执行脱敏。
- 完成阶段 2：新增共享的 YouTube 访问失败分类，将“确认不是机器人”映射为 Cookie 验证要求；失败卡片不再显示“未命名视频”，成功卡片移除重复的 YouTube 名称。下载阶段复用相同分类。
- 阶段 3 完成：仅部署 `youtubeMessage.js`、`youtubeMediaRelay.js` 与 YouTube 回归测试，备份位于 `/opt/trss-yunzai-backups/bl-chat-plugin-youtube-message-20260811-204247`。生产 `node --test tests/youtubeMessage.test.js` 为 7/7，服务重启后 active、无模块加载错误。
- 生产无发送实测该视频输出为“视频信息未获取 / 视频:0JZtdAtJiyk”，原因是“YouTube 要求通过账号 Cookie 完成访问验证”。

# 2026-08-12 叙事卡片前言与正文呈现诊断
- 已确认该场景由 `splitNarrativeReply()` 和发送层显式拆分，不是单靠 prompt 或卡面渲染偶发造成。针对用户全角 `＃`/标题正文连写样式的兼容改动曾作为本地探针验证，随后已全部撤回，等待用户确认希望保留前言单发还是故事整体进卡面后再实施。

# 2026-08-12 卡面即时确认与完整正文
- 用户确认采用“即时确认 + 完整卡面正文”方式。阶段 1 已完成：故事、知识解释和明确文档请求可在模型调用前判定；下一步把确认从模型输出移到发送层，并为旧叙事格式保留兼容拆分。
- 完成阶段 2：卡面类请求在模型调用前发送确定性即时确认；故事/解释提示要求正文直接开始。最终层剥离旧模型前言但不再二次发送。叙事解析兼容用户示例的全角 `＃` 与标题、正文同一行格式。
- 静态复核发现新增卡面提示一度引用了稍后才初始化的 `currentIntentText`；已改用同一位置已存在的 `cardRequestText`，避免运行时 TDZ 错误。
- 阶段 3 完成：本地定向回归 18/18 通过；生产定点补丁回归 13/13、语法检查与旧格式分割探针均通过。仅部署 `apps/test.js`、`narrativeReply.js` 与两份回归，备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-card-presentation-20260812-153242`。
- 生产服务重启后 `trss-yunzai.service=active`；部署文件 SHA-256 一致，启动日志显示 `bl-chat-plugin`、MessagePipeline 与 Guoba 正常加载，无语法、引用或模块加载错误。
# 2026-08-19 合并转发聊天记录无损回放

- 已完成生产归档取证和 OneBotv11 适配器只读核对，开始实现结构快照与回放。
- 已上线最小补丁：生产备份为 `/opt/trss-yunzai-backups/bl-chat-plugin-forward-replay-20260824-104000`（第二版查询保护备份追加 `-v2`）。生产无发送探针验证顶层/嵌套发送者、图片段、归档持久化、旧记录文本回退和嵌套 ID 失效后的结构化展开；重启后服务 active，插件和 MessagePipeline 均正常加载。
